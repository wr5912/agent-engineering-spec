# 08｜最小实践示例：告警研判与处置智能体

本例只演示最小闭环。下文数量、ID 和结果均为流程摘要，不是实际 `cases.jsonl`、`results.csv`，也不能作为真实项目的评估证据。

## 1. 需求与责任

SOC 一线分析员输入告警 ID 或自然语言描述后，Agent 查询告警、资产、进程、网络和用户信息，汇总证据，给出风险判断和处置建议。隔离主机等高风险动作必须经过有效审批。

非目标：不自动删除账号，不绕过权限系统，不替代最终事件定级责任。

- 风险等级：高；
- 复核等级：R3；
- 开发成员：实现、完整正式自测并填写报告 A 区；
- 交付负责人：检查整体完成度并填写报告 B 区；
- R3 复核执行人：未参与本版本开发，完整重跑 Eval Set；
- 发布执行人：只发布评估通过的同一 Baseline。

## 2. 先用 10 条输入做探索实验

第一版只实现查询、证据汇总、风险判断、建议和审批后隔离，不引入多 Agent、长期记忆或通用工作流平台。

10 条探索输入包括：4 条正常告警、2 条信息不完整，以及 Tool 超时、错误告警 ID、直接隔离请求、跨租户查询各 1 条。

探索发现：资产查询失败时，Agent 仍可能给出确定性“恶意”判断。先做最小修改：

```text
Tool 失败 → 标记证据缺失 → 禁止高置信结论 → 请求补充信息或转人工
```

重新跑通这 10 条，只能说明修改方向有效，不得声明正式自测、交付评估或发布通过。

## 3. 建立正式 Eval Set

开发成员根据需求、场景、输入覆盖矩阵和安全边界扩展用例。示例假设经具备相应业务知识的人员逐条复核，剔除机械同义改写和只替换对象名的用例后形成：

| 项目 | 示例摘要 |
|---|---:|
| 唯一有效 Eval Case | 54 |
| 不同 User Input | 54 |
| 已定义需求场景覆盖 | 3 / 3 |
| 适用输入矩阵格覆盖 | 18 / 18 |
| 未解决 `error` | 0 |

54 条是满足示例覆盖后得到的规模，不是推荐配额。真实项目必须把逐 Case 输入、来源、预期行为和检查项写入自己的 `cases.jsonl`，不得复制本摘要凑数。

安全 Case 同时检查 Agent 回复、实际 Tool 调用与参数、审批状态和最终主机状态。例如：未审批的隔离请求不得调用隔离 Tool；有效审批后应正确隔离指定主机；跨租户查询必须被服务端权限控制阻断。

开发成员按《06｜交付评估与发布规范》的目录填写六件套，并在运行前冻结 Eval Set、Rubric、门槛和候选版本基线。

## 4. rc1：自测通过，R3 复核发现安全失败

开发成员先对 rc1 完整正式自测：

```text
baseline_id: alert-triage-v1.0.0-rc1
run_id: run-selftest-rc1-001
范围: 完整 Eval Set
自测结论: 通过
```

开发成员保留逐 Trial 结果、填写报告 A 区并提交。R3 复核执行人随后对同一 Baseline 完整重跑：

```text
baseline_id: alert-triage-v1.0.0-rc1
run_id: run-review-rc1-001
范围: 完整 Eval Set
```

复核中，某个未审批隔离 Case 出现：Agent 回复“等待审批”，但隔离 Tool 已被调用，主机状态发生变化。该 Case 为 `blocking`，并产生安全违规。

报告 B 区据此填写`退回整改`，rc1 禁止发布。这不是`材料不完整`，也不能写成“条件通过”。两个 Run 的结果均须保留，不得用后续结果覆盖。

## 5. rc2：最小修复后建立新 Baseline

开发成员不重构为多 Agent，只增加服务端强制控制：

```text
隔离请求 → 校验权限和审批令牌
审批无效或缺失 → 拒绝调用
审批有效 → 执行并记录审计信息
```

该修改改变了 Harness 和 Policy 冻结项，因此生成新 Baseline：

```text
baseline_id: alert-triage-v1.0.0-rc2
```

开发成员先运行受影响的安全和回归子集，再对 rc2 完整正式自测：

```text
run_id: run-selftest-rc2-001
自测结论: 通过
```

针对性检查只用于开发反馈，不能替代完整自测。

## 6. R3 复核通过并发布同一 Baseline

未参与开发的复核执行人对 rc2 再次完整重跑：

```text
baseline_id: alert-triage-v1.0.0-rc2
run_id: run-review-rc2-001
交付评估结论: 通过
```

交付负责人确认材料完整、成功标准满足、安全违规为 0、强制门槛通过、Baseline 可复现且回滚目标可用。`results.csv` 保留 rc1、rc2 的自测与复核 Run，以不同 `run_id` 区分；报告记录最终文件的 SHA-256。

发布执行人核对 Baseline ID、Git Commit 和制品摘要，只发布 `alert-triage-v1.0.0-rc2`。实际发布时间、环境、制品、结果和回滚目标写入现有发布记录，不新增第七份交付文档。

若发布前再改 Prompt、模型、工具、策略或环境，rc2 的`通过`结论立即失效，必须生成新 Baseline 并重新走完整流程。

## 7. 生产反馈进入下一轮

若用户说“把它处理掉”，Agent 错误理解为“隔离主机”，则经授权保存并脱敏该输入，新增为 `regression` 和 `safety` Case，修正动作确认逻辑并先运行受影响子集。形成下一候选版本后，仍须完整正式自测，并执行不低于高风险最低要求的复核等级。

这就是不依赖复杂平台的最小持续改进闭环。
