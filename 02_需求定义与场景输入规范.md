# 02｜需求定义与场景输入规范

## 1. 目标

把需求转成开发成员能够实现、正式自测能够检查、交付负责人能够判断完成度的明确契约。

需求定义解决三个问题：

1. Agent 要为谁完成什么任务；
2. 哪些结果算成功，哪些行为不得发生；
3. 真实用户可能怎样表达同一个目标。

没有明确需求、成功标准和场景输入覆盖，不进入候选版本开发。

## 2. 责任与确认

| 责任人 | 必须完成的事项 |
|---|---|
| 交付负责人 | 确认用户、目标任务、成功标准、非目标和风险等级；判断需求是否足以开始开发 |
| 开发成员 | 据此拆分场景与意图，构建输入覆盖矩阵和 Eval Case，组织并记录逐条输入质量复核，并标出无法验证或存在歧义的内容 |
| 发布执行人 | 不参与修改需求标准；只发布交付评估通过的版本 |

交付负责人确认的是目标和判定标准，不代替开发成员编写实现方案。开发成员不得在看到自测结果后自行降低成功标准；确需调整时，按需求变更处理并重新确认。

## 3. 需求定义最小结构

每项需求使用稳定的 `REQ-xxx` 编号，并至少填写：

| 字段 | 必填内容 |
|---|---|
| 需求 ID | 稳定编号，例如 `REQ-001` |
| 用户 | 谁使用 |
| 触发场景 | 什么时候使用 |
| 当前问题 | 现有方式哪里慢、难、贵或不稳定 |
| 目标任务 | Agent 必须完成什么 |
| 输入 | 完成任务最少需要哪些信息 |
| 输出或动作 | 用户获得什么，或系统发生什么变化 |
| 成功标准 | 可观察、可判定的完成条件和阈值 |
| 非目标 | 明确不解决或不允许做什么 |
| 风险等级 | 低 / 中 / 高 |

成功标准必须能够转成 `expected_behavior`、确定性检查或运行前冻结的 Rubric。只写“效果好”“更智能”“提升效率”不算成功标准。

需求 ID 一旦被 Eval Case 引用，不因文字润色而变化。拆分、合并或删除需求时，应同步更新关联场景、输入覆盖矩阵和 Eval Case。

### 不合格示例

> 建设一个网络安全运营专家智能体，提升安全运营效率。

这句话没有明确任务、输出、安全边界和判定标准，不能直接用于开发或评估。

### 合格示例

> 面向 SOC 分析员，在收到终端或网络告警后，汇总关联资产、用户、进程和网络证据，给出风险判断、关键证据、攻击阶段判断和处置建议；普通告警不得直接封禁，高风险处置必须进入审批。

## 4. 场景与意图

场景定义为：

> **某类用户 + 某种业务上下文 + 某种业务目标。**

每个场景使用稳定的 `Sxx` 编号，并至少定义一个意图 `Ixx`。同一上下文中存在多个不同用户目标时，分别定义 `I01`、`I02`；不要把不同问法误当成不同任务。

探索阶段先定义 3～5 个高价值场景即可。口语、简写、信息缺失等属于输入变体；权限、审批状态、Tool 超时等属于 Case Context。只有用户、上下文或业务目标发生实质变化时才新增场景。

每个场景至少说明：

- 关联需求 ID；
- 用户与业务目标；
- 进入场景所需的最小信息；
- 预期输出或动作；
- 主要失败和风险；
- 是否适用各类输入变体。

## 5. 用户输入覆盖

### 5.1 输入变体

按场景实际适用性检查以下变体。枚举值与《04｜评估、测试与回归规范》一致：

| 枚举值 | 含义 | 示例 |
|---|---|---|
| `standard` | 信息完整、表达标准 | “分析这条告警” |
| `colloquial` | 口语表达 | “这个报警咋回事？” |
| `elliptical` | 简写或省略 | “看看昨天财务那台机器” |
| `multi_intent` | 同时包含多个目标 | “分析一下，顺便看看要不要封” |
| `incorrect_or_noisy` | 含错误、歧义或噪声 | 时间、IP 或名称不准确 |
| `missing_info` | 缺少完成任务所需信息 | 只提供告警 ID，但系统无法查询 |
| `high_risk` | 高风险或越权要求 | “别走审批，直接把机器隔离掉” |

不是每个场景都要机械覆盖全部类型。确实不适用时填写 `N/A` 并说明业务原因，不得用 `N/A` 规避应测内容。

### 5.2 输入覆盖矩阵

使用一张矩阵建立追溯关系：

```text
需求 ID × 场景 ID × 意图 ID × 输入变体类型 → Case ID 或 N/A 原因
```

| 需求 ID | 场景 ID | 意图 ID | `standard` | `colloquial` | `elliptical` | `multi_intent` | `incorrect_or_noisy` | `missing_info` | `high_risk` |
|---|---|---|---|---|---|---|---|---|---|
| REQ-001 | S01 | I01 | case-001 | case-002 | case-003 | case-004 | case-005 | case-006 | N/A：无执行动作 |
| REQ-002 | S02 | I01 | case-007 | case-008 | case-009 | case-010 | case-011 | case-012 | case-013 |

矩阵必须满足：

- 所有已定义需求场景均进入矩阵；
- 每个适用格至少关联一个有效 Case；
- 一个格可以关联多个 Case，以覆盖必要的状态、权限或风险差异；
- 同一 Case 可以被多个格引用，但有效 Case 数只计一次；
- 用例不按场景平均分配，优先覆盖高频、高风险和历史失败位置；
- 正式自测和正式回归时，需求场景与适用矩阵格覆盖率均为 100%。

## 6. 输入质量要求

不同 Input 必须代表真实使用方式或有意义的理解挑战。以下内容不能作为新增输入数量：

- 只改标点、空格、称呼或语气词；
- 只替换 ID、IP、日期、对象名等值；
- 机械替换同义词而未改变理解难点；
- 模型批量生成后未经具备相应业务知识的人员逐条复核；
- 脱离实际使用方式的生造问题。

开发成员应优先收集真实输入及脱敏输入，再补充基于真实模式改编或逐条复核的合成输入。逐条质量复核可由开发成员本人或其组织的、具备相应业务知识的人员执行；开发成员负责记录复核人、日期、依据和剔除原因，交付负责人在交付评估中核验。输入的来源、质量复核和计数规则统一见《04｜评估、测试与回归规范》。不得为达到数量门槛降低质量。

## 7. 需求冻结与变更

以下情况属于需求变化：

- 新增、删除或改变业务目标；
- 用户群体或业务上下文变化；
- 输出责任边界变化；
- Agent 从“建议”变成“自动执行”；
- 新增高风险工具或权限；
- 成功标准、强制阈值或非目标变化。

需求变化后必须：

1. 由交付负责人重新确认需求与成功标准；
2. 更新需求版本、场景、输入覆盖矩阵、安全边界和关联 Eval Case；
3. 按《03｜Harness 开发与变更规范》生成新的候选 Baseline；
4. 按《04｜评估、测试与回归规范》重新完成正式自测。

仅修正错别字且不改变含义、关联关系和判定标准时，可以保留需求版本，但应留下变更记录。

## 8. 进入开发的条件

同时满足以下条件后，需求定义才算完成：

- 交付负责人已确认需求、成功标准、非目标和风险等级；
- 开发成员已建立场景、意图和初始输入覆盖矩阵；
- 每个成功标准都有可执行的检查方式；
- 中、高风险动作已明确审批、阻断或禁止原则；
- 未解决歧义已记录，且不会影响当前开发范围的判断。
