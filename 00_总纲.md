# 00｜智能体研发总纲

## 1. 目的与适用边界

本规范用于统一智能体从需求确认、实验、开发、自测、交付评估、发布到持续回归的工作方式，确保每项交付：

- 有明确需求、适用场景、成功标准和非目标；
- 覆盖真实用户的主要输入方式及关键边界；
- 在确定的能力与安全边界内完成任务；
- 有可复核的自测数据，而不是仅凭演示或主观判断；
- 能够定位实际运行版本，并可复现、审计和回滚。

本规范不限定 DSH、AgentScope、Claude Code、Codex、LangGraph 等具体框架，也不规定 Prompt 或 Agent 编排的唯一写法。

## 2. 基本原则

### 2.1 产品契约高于实现形式

智能体的产品契约是：

```text
需求 + 场景 + 输入分布 + 期望行为 + 评价标准 + 安全边界
```

Prompt、Context、Skill、Tool、MCP、Agent、Hook、Workflow、Plugin、Memory、Model Routing 和 Runtime 配置统称为 **Harness**。Harness 可以持续优化，但每次变化都必须接受与影响范围相匹配的检查；影响冻结项的变化必须形成新的候选基线。

### 2.2 用最小实验替代空想

新的复杂编排、长期记忆、多 Agent、自进化机制或管理平台，应先用 5～20 个真实或高度接近真实的任务样本验证关键假设。

```text
单次模型调用
→ 模型 + 明确上下文
→ 模型 + Tool
→ Skill / Workflow
→ 多 Agent
→ 长期记忆 / 自进化
```

只有前一级无法满足已确认需求，并且实验能证明新增复杂度的收益时，才进入后一级。5～20 个样本只用于探索，不得形成正式自测、交付评估或发布结论。

### 2.3 用证据判断完成度

是否完成交付，应同时依据需求符合性、场景与输入覆盖、能力结果、安全边界、材料完整性及基线可复现性判断。不得用“看起来可用”、单次成功演示或对实现代码的人工检查代替正式证据。

### 2.4 不穷举现实，但完整覆盖已定义范围

规范不要求穷举所有现实问法，也不得宣称“所有问法均已覆盖”。正式 Eval Set 必须覆盖全部已定义需求场景、适用的输入覆盖单元和高风险边界，并如实记录未覆盖项与剩余风险。

正式 Eval Set 必须同时包含不少于 50 个有效 Eval Case 和不少于 50 个经质量审查的不同 User Input。数量达不到时，应判定证据不完整并继续收集；禁止用重复改写、无业务差异的变体、脱离真实场景的输入或无法判定的样本充数。详细质量、计数和通过规则以[《04｜评估、测试与回归规范》](./04_评估测试与回归规范.md)为准。

### 2.5 让真实失败进入回归

生产中的能力失败、安全偏差和高价值反馈必须完成分类；可复现的问题应转为 Eval Case 并标记为 `regression`，用于约束后续 Harness 变更。

## 3. 权威术语

| 术语 | 定义 |
|---|---|
| Agent / 智能体 | 面向已定义用户任务提供输出或执行动作的交付对象 |
| Harness | 驱动智能体行为的 Prompt、Context、Skill、Tool、Agent 编排及相关运行配置的总称 |
| User Input | 一次真实或高度接近真实的用户输入文本；不同 Input 必须具有实质性的表达、信息或任务差异 |
| Eval Case | 可独立执行和判定的测试单元，包含输入、上下文、期望、评价方式、门禁及标签 |
| Eval Set | 绑定版本的一组 Eval Case；`core`、`boundary`、`safety`、`regression` 是同一数据集中的标签，不另建重复数据集 |
| 针对性检查 | 开发期间对受影响用例子集的快速检查，只用于反馈，不能形成正式结论 |
| 候选基线 | 在正式自测前冻结的需求、Harness、模型、Runtime、Policy、工具、Eval Set、Rubric 和环境组合，以 `baseline_id` 唯一标识 |
| 正式自测 | 开发成员在候选基线上完整运行冻结 Eval Set，并形成机器结果和自测结论 |
| Run | 对一个候选基线执行一次评估的完整记录，以 `run_id` 唯一标识；重跑生成新 `run_id`，不得覆盖旧结果 |
| 交付评估 | 交付负责人根据六件套、机器证据和必要复跑，对整体交付完成度作出的独立结论 |
| 发布 | 将交付评估通过的同一候选基线部署到目标环境，并记录结果和回滚点 |
| 回归 | 用既有 Case 和真实失败沉淀的 `regression` Case，检查后续变更是否造成能力或安全退化 |

本规范中的“必须”“应当”“可以”“禁止”分别表示强制要求、默认要求、可选实践和不可接受行为。

## 4. 角色与责任

全套规范只使用以下三个责任角色。人员可以兼任，但每项责任和结论必须按角色分别记录。

| 角色 | 核心责任 | 不得替代的责任 |
|---|---|---|
| 开发成员 | 澄清需求，开发 Harness，维护用例与证据，组织并记录输入质量复核，冻结候选基线，执行正式自测，提交交付物并完成整改 | 不得用自测结论代替交付评估；R3 复核不得仅由本人完成 |
| 交付负责人 | 确认需求和成功标准，选择复核等级，核验整体完成度，作出交付评估结论 | 不得用材料齐全代替能力和安全复核，也不得批准未通过硬门禁的版本 |
| 发布执行人 | 核对发布授权，部署评估通过的同一基线，记录发布结果并保证可回滚 | 不得替换已通过基线中的任何冻结项，也不得自行把未通过版本投入发布 |

## 5. 状态与结论

不同阶段使用不同状态，禁止混用：

| 阶段 | 允许状态 | 含义 |
|---|---|---|
| 正式自测 | `通过` / `不通过` / `不完整` | 分别表示全部门禁满足、存在失败门禁、证据或执行不足以判断 |
| 交付评估 | `通过` / `退回整改` / `材料不完整` | 分别表示整体交付满足要求、实质要求未满足、提交物不足以复核 |
| 发布 | `未发布` / `已发布` / `发布失败` / `已回滚` | 只描述评估通过基线的实际部署状态，不代表能力结论 |

不存在“条件通过”或“条件发布”。灰度范围、人工兜底和已知限制可以作为已批准的发布约束，但不得绕过 `blocking` Case、强制质量指标、安全门禁、材料完整性或基线一致性要求。

## 6. 风险等级与复核等级

风险等级描述智能体可能造成的后果，复核等级描述交付负责人要做到什么。两者必须分别记录，不得把 R1/R2/R3 写成风险等级。

| 风险等级 | 特征 | 典型行为 | 最低复核等级 |
|---|---|---|---|
| 低 | 只读、生成或建议，不直接改变业务状态 | 问答、总结、研判建议 | R1 |
| 中 | 调用业务系统，动作可逆或必须经人工确认 | 创建工单、生成待审批策略 | R2 |
| 高 | 可直接造成重大业务、安全、数据或合规后果 | 封禁、隔离、删除、修改生产策略 | R3 |

| 复核等级 | 最低动作 |
|---|---|
| R1 | 核验六件套、哈希、数量、覆盖、追溯关系和原始结果；不强制复跑 |
| R2 | 完成 R1，并复跑全部 `blocking`、全部 `safety` 及本次变更影响 Case 的并集 |
| R3 | 由非开发成员完整重跑冻结 Eval Set，并与开发自测结果比对 |

同一智能体具有多种能力时，按最高风险动作确定风险等级。交付负责人不得选择低于风险等级所要求的复核等级；权限扩大、控制边界变化、框架迁移、关键工具变化或严重历史失败可以提高复核等级，但不改变风险事实。发现证据不一致、结果不可复现或真实性存疑时，无论原风险和复核等级如何，均升级为 R3。

逐条输入质量复核可以由开发成员本人或其组织的、具备相应业务知识的人员执行。这不是新增责任角色：开发成员对复核完成和记录负责，交付负责人在交付评估中核验复核人员、日期和依据。

## 7. 基线与证据约束

- 正式自测前必须冻结候选基线和通过门槛，运行后不得倒改标准解释结果。
- 需求、Harness、模型、Runtime、Policy、工具、Eval Set、Rubric 或环境任一冻结项发生变化，必须生成新的 `baseline_id`。
- 对同一候选基线重跑只生成新的 `run_id`，并保留所有历史结果。
- 六件套是人读交付文档；`cases.jsonl` 和 `results.csv` 是必需机器证据。模板本身不是交付物。
- 数据过大或敏感时可以受控外置，但必须记录 URI、版本、SHA-256 和访问方式，不得创建空文件伪装完整。
- 发布版本必须与交付评估通过的 `baseline_id` 一致；任何临时修改都应停止发布并重新进入基线冻结和自测流程。

交付物名称、目录和提交规则以[《06｜交付评估与发布规范》](./06_交付评估与发布规范.md)为唯一依据；六件文档字段以[《07｜智能体研发交付六件套模板》](./07_研发交付六件套模板.md)为准，机器数据契约以[《04｜评估、测试与回归规范》](./04_评估测试与回归规范.md)为准。

## 8. 红线

1. **没有明确需求、成功标准和任务样本，不进入复杂 Agent 设计。**
2. **没有冻结评价标准，不以主观感受、演示效果或运行后的标准调整作为完成依据。**
3. **禁止为了达到数量门槛降低输入和 Case 质量。** 证据不足时必须结论为“不完整”，不能充数。
4. **没有明确并验证安全边界，不授予高风险写权限。**
5. **针对性检查不得冒充正式自测，开发自测不得代替交付评估。**
6. **没有绑定候选基线、可追溯 Run 和机器证据，不得判定交付通过。**
7. **发布执行人不得发布与评估通过记录不一致的基线。**
8. **任何硬门禁失败都不得以“条件通过”“条件发布”、灰度或人工兜底绕过。**
