# 06｜交付评估与发布规范

## 1. 核心原则

> **交付评估判断的是一个明确候选版本是否整体完成，而不只是某次能力测试是否通过。**

整体完成至少包括：需求与非目标明确、交付材料完整、正式自测有效、能力与安全门槛达标、候选版本可复现、发布和回滚有据可查。

## 2. 角色、流程与结论

| 阶段 | 责任人 | 必须完成的动作 | 输出 |
|---|---|---|---|
| 开发与正式自测 | 开发成员 | 冻结候选版本，完整运行 Eval Set，核对结果并填写报告 A 区 | 自测结论和六件套交付物 |
| 交付评估 | 交付负责人 | 检查整体完成度，依据风险选择复核等级，核验证据或独立复跑，并填写报告 B 区 | 交付评估结论 |
| 发布与回滚 | 发布执行人 | 只发布评估通过的同一 Baseline，记录实际结果并保留回滚能力 | 现有发布记录中的发布结果 |

一人可以兼任多个角色，但必须按角色分别留痕。R3 的完整复跑必须由未参与该版本开发的人执行。

```text
冻结候选 Baseline
→ 开发成员完整正式自测
→ 提交六件套和机器证据
→ 交付负责人评估
→ 通过：发布同一 Baseline
→ 退回：整改并按变更情况生成新 Baseline，再走完整流程
```

结论只能使用以下状态：

- 开发自测：`通过`、`不通过`、`不完整`；
- 交付评估：`通过`、`退回整改`、`材料不完整`；
- 发布结果：`未发布`、`已发布`、`发布失败`、`已回滚`。

禁止使用“条件通过”或“条件发布”。已知限制只有在需求中已明确属于非目标或经确认不影响成功标准、强制门槛和安全边界时，才可以随“通过”结论如实记录；否则必须退回整改。

## 3. 智能体研发交付六件套

| 文档 | 主要内容 |
|---|---|
| 智能体需求定义 | 用户、目标任务、成功标准、非目标、风险等级和角色 |
| 用户场景与输入覆盖矩阵 | 需求场景、意图、输入变体及其 Case 映射 |
| 任务与评估数据集说明 | Eval Set、用例质量、判定方法、Rubric 和门槛 |
| 安全与控制边界清单 | 数据、工具、权限、审批、失败安全和安全 Case 映射 |
| 候选版本基线 | 本次自测和交付评估针对的完整运行组合 |
| 自测与交付评估报告 | 开发成员的正式自测结论和交付负责人的整体评估结论 |

`cases.jsonl` 和 `results.csv` 是六件套必须引用的机器证据，但不计入六份文档。模板文件本身不是交付物；复制并按具体项目填写完整的文档才是交付物。

## 4. 唯一交付目录

具体 Agent 项目统一使用以下结构。本节是交付目录的唯一权威定义，其他文档只引用，不另建一套目录。

```text
<agent-project>/
├── ...                                # Agent/Harness 源码与配置
└── delivery/
    ├── 01_智能体需求定义.md
    ├── 02_用户场景与输入覆盖矩阵.md
    ├── 03_任务与评估数据集说明.md
    ├── 04_安全与控制边界清单.md
    ├── 05_候选版本基线.md
    ├── 06_自测与交付评估报告.md
    └── eval/
        ├── cases.jsonl
        └── results.csv
```

使用规则：

- `delivery/` 不复制 Agent/Harness 源码、配置或构建产物，只通过版本、Commit、镜像摘要或制品哈希引用；
- 仓库内只维护一套当前交付结构，历史版本通过 Git Commit、Tag 或现有制品库版本追溯，不建立 `latest`、`final-final` 等重复目录；
- `cases.jsonl`、`results.csv` 可安全入库时放入 `delivery/eval/`；
- 敏感或大型数据可以外置，但文档必须记录受控 URI、版本、SHA-256 和实际访问方式；外置时不得为凑齐结构创建空文件或空目录；
- 不预建 `evidence/`、`logs/`、`reports/` 等空目录。确有额外证据时，由 `results.csv` 的 `evidence_ref` 指向现有受控位置；
- 实际发布结果写入团队现有发布记录，不为此新增第七份交付文档。

## 5. 候选版本基线

候选版本基线必须回答：“本次正式自测、交付复核和计划发布的，究竟是哪一套需求、Agent/Harness、模型、运行时、策略、数据集和环境？”

```yaml
candidate_version: agent-x-v1.3.0-rc2
baseline_id: agent-x-v1.3.0-rc2
requirements_version: req-v3
git_commit: immutable-commit
runtime: runtime-name
runtime_version: x.y.z
model: model-name-and-version
prompt_version: hash-or-tag
skill_version: hash-or-tag
tool_mcp_version: version-or-hash
policy_version: policy-v3
evaluation_set_version: eval-v7
evaluation_set_ref: delivery/eval/cases.jsonl
evaluation_set_hash: sha256:...
evaluation_set_access: local
rubric_version: rubric-v2
rubric_ref: delivery/03_任务与评估数据集说明.md
execution_environment: env-id
build_artifact: image-or-package-ref
build_artifact_hash: sha256:...
rollback_target: previous-stable-version
```

不存在的字段填写 `N/A` 并说明原因，不为填表而创建无意义版本。

基线与运行遵守以下规则：

1. 需求、Agent/Harness、模型、Runtime、Prompt、Skill、Tool/MCP、Policy、Eval Set、Rubric、执行环境或构建制品等任一冻结项变化，必须生成新的 `baseline_id`；
2. 每次执行必须生成新的 `run_id`。同一 Baseline 因复核、随机性检查或瞬时错误而重跑时，保留 `baseline_id`，但不得复用旧 `run_id`；
3. `results.csv` 可以保存多个 `run_id`，旧运行结果不得覆盖、删除或改写；
4. 开发自测必须写明采用的 `run_id`；交付复核只有实际复跑时才填写新的复核 `run_id`，R1 未复跑时填写 `N/A`；所有 Run 均须回溯到同一 Baseline；
5. 交付通过后，使用同一 Git Commit、制品摘要及交付快照锁定结果。任何发布前改动都会使原结论失效，必须建立新 Baseline 并重新评估。

## 6. 开发成员提交条件

开发成员只有在以下内容齐备后，才能将候选版本提交为“待交付评估”：

- 六份文档已经按当前候选版本填写，名称、版本和引用一致；
- Eval Set 符合《04｜评估、测试与回归规范》的数量、质量和覆盖要求；
- 完整 Eval Set 已在冻结 Baseline 上执行，所有要求的 Trial 均有结果；
- `cases.jsonl`、`results.csv` 或其受控引用可访问，版本和 SHA-256 可核对；
- 《自测与交付评估报告》A 区已填写并由开发成员确认；
- 自测结论为`通过`。

自测结论按《04｜评估、测试与回归规范》的优先级判定：存在已确认的 `blocking`、安全或强制质量失败时为`不通过`；没有已确认实质失败，但数量、覆盖、Trial、结果或追溯关系不足时为`不完整`。二者都不得以“待发布”状态提交。

## 7. 交付负责人评估

### 7.1 先检查材料完整性

交付负责人先核对六件套、机器证据、哈希和相互引用，并确认报告 A 区的开发成员及日期已经填写。材料缺失、证据不可访问、结果无法定位或 Baseline 对不上时，直接给出`材料不完整`，无需用主观判断补齐缺口。

### 7.2 确定并执行复核等级

先在报告中分别记录风险等级（低/中/高）与复核等级（R1/R2/R3）。复核等级不得低于《00｜智能体研发总纲》为该风险规定的最低等级；变更重大或证据存疑时应升级。

| 复核等级 | 最低复核要求 |
|---|---|
| R1 | 审查哈希、数量、覆盖、追溯关系和原始结果；可以按需抽测 |
| R2 | 在证据审查基础上，独立复跑全部 `blocking`、`safety` 以及本次变更涉及的 Case |
| R3 | 由未参与该版本开发的人完整重跑冻结的 Eval Set |

复核运行必须使用新的 `run_id`，结果写入同一 `results.csv` 或同一受控结果系统，不复制第二套 Eval Set 或结果事实源。

任一等级发现自测与原始结果不一致、哈希或追溯关系无法核对、相同 Baseline 无法复现、结果真实性或完整性存疑时，必须升级为完整重跑。

### 7.3 检查整体完成度

交付评估至少回答：

1. 目标任务和成功标准是否逐项满足，非目标是否守住；
2. 需求场景、输入变体和安全边界是否有充分且高质量的 Case 证据，逐条输入质量复核人员、日期和依据是否可信；
3. 自测和复核是否满足完整性、能力、回归和安全门槛；
4. Baseline、数据集、结果和制品是否可追溯、可复现；
5. 发布范围、运行控制和回滚目标是否明确可用。

除专项技术审查外，不要求交付负责人逐行评价 Prompt、Skill 目录或 Workflow 节点；但影响需求、安全、结果真实性或复现性的实现问题必须纳入评估。

### 7.4 作出结论

| 结论 | 使用条件 |
|---|---|
| `通过` | 材料完整，需求和安全边界满足，正式自测及规定复核通过，Baseline 可复现，具备发布与回滚条件 |
| `退回整改` | 材料可评估，但需求、能力、安全、复现性或发布准备任一项未达到要求，或独立复跑失败 |
| `材料不完整` | 缺少必需文档、证据、报告责任人或日期、哈希或追溯关系，无法完成可靠判断 |

结论、依据、交付负责人和评估日期填写在《自测与交付评估报告》B 区。若整改改变任何冻结项，必须建立新 Baseline、重新完整自测并再次提交；若只补齐材料且冻结项未变，可沿用 Baseline，但发生重跑时仍必须使用新 `run_id`。

## 8. 发布与回滚

交付评估`通过`只授权发布报告所绑定的同一 Baseline。发布执行人必须再次核对 `baseline_id`、Git Commit、构建制品摘要和计划发布对象，禁止在评估后临时修改 Prompt、模型、策略、工具或环境再直接发布。

不得发布的情形包括：

- 交付评估不是`通过`；
- 任一 `blocking` Case、强制质量指标或安全门槛失败；
- 存在未解决的 `error`、缺失 Trial 或无法解释的结果差异；
- 发布对象与评估通过的 Baseline 不一致；
- 没有可执行的最小回滚方式。

灰度、小范围开放、只读模式或高风险动作审批可以作为发布策略，但不能替代任何交付门槛，也不能产生“条件发布”。实际发布时间、环境、制品、结果和回滚情况写入现有发布记录，不回填为新的交付文档。

最小回滚准备包括：上一稳定版本或可恢复状态、明确触发条件、执行人、操作步骤和回滚后的验证方法。

## 9. 迁移与生产问题

框架、Runtime 或模型迁移都属于新 Baseline。迁移时使用同一需求和评估体系，补充迁移新增风险 Case，再选择不低于当前风险最低要求的复核等级执行完整自测与交付评估；不要求新旧框架配置逐项同形。

生产失败应加入统一 Eval Set 并标记为 `regression`。修复后先做针对性检查；形成下一候选版本时，仍须完整运行 Eval Set，不得用子集结果替代正式自测或交付评估。
