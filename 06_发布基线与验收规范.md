# 06｜发布基线与验收规范

## 1. 核心原则

> **验收的不是“Prompt 写得好不好”，而是某个明确运行基线是否满足需求、能力和安全要求。**

## 2. 发布前必须形成六件套

| 文档 | 作用 |
|---|---|
| 智能体需求定义 | 说明做什么、不做什么 |
| 用户场景与输入覆盖矩阵 | 说明主要怎么被使用 |
| 任务与评估数据集说明 | 说明如何验证 |
| 安全与控制边界清单 | 说明不能突破什么 |
| Release Harness Baseline | 说明测试的是哪一版 |
| 能力评估与验收报告 | 说明结果怎样 |

### 2.1 最小交付目录

六件套、实际评估数据和逐 Case 结果应放在具体 Agent 项目中，不放回本规范仓库：

```text
<agent-project>/
├── ...                              # Agent/Harness 源码与配置
└── delivery/
    ├── 01_智能体需求定义.md
    ├── 02_用户场景与输入覆盖矩阵.md
    ├── 03_任务与评估数据集说明.md
    ├── 04_安全与控制边界清单.md
    ├── 05_Release_Harness_Baseline.md
    ├── 06_能力评估与验收报告.md
    └── eval/
        ├── cases.jsonl                  # 默认：可入库的评估数据
        └── results.csv                  # 默认：可入库的逐 Trial 结果
```

使用规则：

- `delivery/` 只保存填写完成的交付文档、评估数据和结果，不复制 Agent/Harness 源码与配置；
- 仓库内只维护一份 `delivery/`，通过 Git Tag / Commit 锁定发布快照，不建立 `latest`、`final-final` 等重复目录；
- 对外导出时可命名为 `<agent>-<release>-delivery.zip`；
- 敏感或过大的评估数据、结果不得为了目录完整而复制进 Git；对应本地文件可以省略，不创建空占位文件，并记录受控 URI、数据集版本或结果 `run_id`、SHA-256 及获取/访问方式；
- 不默认创建 `evidence/`、`logs/`、`reports/` 等空目录，只在验收结论确实需要额外证据时增加。

## 3. Release Harness Baseline 最小字段

```yaml
release: agent-x-v1.3.0
baseline_id: agent-x-v1.3.0
requirements_version: req-v3
agent_version: git-commit-or-tag
runtime: AgentScope / DSH / Claude Code / other
runtime_version: x.y.z
model: model-name-and-version
prompt_version: hash-or-tag
skill_version: hash-or-tag
mcp_toolset_version: version-or-hash
policy_version: policy-v3
evaluation_set: eval-v7
evaluation_set_path: delivery/eval/cases.jsonl
evaluation_set_hash: sha256:...
evaluation_set_access: local
rubric_version: rubric-v2
rubric_location: delivery/03_任务与评估数据集说明.md
evaluation_results: delivery/eval/results.csv
evaluation_results_hash: sha256:...
evaluation_results_access: local
execution_environment: env-id
```

如果某项不存在，不为了填表而人为创建。

为减少无意义编号，`baseline_id` 默认直接复用唯一的 `release`；如项目已有不可变 Baseline ID，可使用既有编号。路径和访问字段在文件入库时分别填写仓库路径和 `local`，数据外置时改填受控 URI 和实际获取/权限说明。`results.csv` 可以保存多个 `run_id`，验收报告必须明确本次采用的 `run_id`。

运行前先冻结需求、Agent/Harness、模型、Runtime、Policy、Eval Set、Rubric 和环境字段；运行后再填写结果路径与哈希。验收完成后用同一个 Git Tag / Commit 锁定整份交付快照，禁止用新结果覆盖已验收快照。

## 4. 为什么要锁基线

测试结果必须能够回答：

> “到底是哪一套 Agent + Harness + 模型 + Policy 通过了这次测试？”

否则一个月后 Prompt、模型或工具变了，原验收报告将无法复现。

## 5. 验收内容

正式验收聚焦四类结果：

正式验收前必须先确认：

- 已按《评估、测试与回归规范》运行完整、版本化的 Eval Set；
- 有效 Eval Case 和经质量审查的不同 User Input 均不少于 50；
- 所有已定义需求场景和适用的输入覆盖矩阵格均关联到具体 Case；
- 安全边界 Case 已纳入同一 Eval Set，数据集、Rubric、阈值和 Baseline 已在运行前锁定；
- 每个有效 Case 均已按运行前冻结的次数完成全部要求的 Trial，且绑定同一 Baseline，不存在缺失结果或未解决的 `error`；
- 验收报告已记录本次 `baseline_id` 和采用的 `run_id`，可以在结果文件中唯一定位。

### A. 需求符合性

- 目标任务是否完成；
- 非目标是否越界；
- 关键交付是否符合约定。

### B. 场景和输入覆盖

- 所有已定义需求场景是否覆盖；
- 正常、口语、模糊/省略、缺失、异常和高风险等适用输入变体是否覆盖；
- 矩阵中的 Case ID、`N/A` 及其理由是否完整且可追溯。

### C. 能力评估

- 核心任务结果；
- 关键错误；
- 历史回归；
- 必要的性能指标。

### D. 安全控制

- 权限；
- 审批；
- 高风险动作；
- 失败安全；
- 审计与回滚。

## 6. 不建议作为验收重点的内容

除非是专项技术审查，否则不要求验收方逐项评价：

- System Prompt 文案；
- Skill 目录结构；
- Agent 内部分工；
- Workflow 具体节点；
- 框架特有配置格式。

这些内容应由开发团队负责，通过评估结果证明其有效性。

## 7. 发布判定

### 可以发布

- 完整 Eval Set 已绑定本次 Baseline 运行完成；
- 所有 `blocking` Case 100% 通过；
- 开放性质量指标达到运行前确定的强制阈值；
- 安全违规数为 0；
- 历史严重回归未复现；
- 基线完整可复现。

### 条件发布

仅适用于运行前已定义为非阻断的已知限制，且有明确兜底、灰度范围和回退方式。不得通过“条件发布”跳过 `blocking` Case 失败或强制质量阈值。

### 不允许发布

- 有效 Eval Case 或不同 User Input 少于 50；
- 已定义需求场景或适用矩阵格未覆盖；
- 存在缺失的 Case 或 Trial 结果；
- 存在未解决的 `error` Trial；
- 高风险越权；
- 任一 `blocking` Case 失败；
- 强制开放性质量指标低于预设阈值；
- 核心任务明显退化；
- 严重历史问题复现；
- 测试结果无法绑定明确版本；
- R3 动作缺少必要控制。

## 8. 灰度与回滚

不强制建设复杂灰度平台。

最小可行方式可以是：

- 只对少量用户开放；
- 只开放只读能力；
- 高风险动作全部审批；
- 新旧 Agent 并行对比；
- 保留上一稳定版本一键切回。

## 9. 框架迁移验收

框架迁移后，验收重点顺序：

1. 统一 Eval Set 中的核心、边界、安全和回归 Case 是否通过；
2. 迁移引入的新风险是否已补充为 Case；
3. 所有 `blocking` Case 是否 100% 通过；
4. 性能与成本是否可接受；
5. 新框架带来的剩余风险是否已记录。

不要求 Harness 配置逐项一一映射。
