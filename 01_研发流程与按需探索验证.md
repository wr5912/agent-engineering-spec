# 01｜研发流程与按需探索验证

## 1. 流程目标

研发流程从任务来源、目标、输入/前置条件和安全边界开始。先选择研究探索、缺陷修复、外部资产复用、迁移或正式生产交付路径；只有最后一种路径默认进入完整九阶段。任务明确时直接实现，存在会影响实施路线的关键不确定性时开展最小范围探索。

```text
任务与预期结果
→ 输入、前置条件与安全边界
→ 必要时探索 / 复现 / 适配 → 实现与针对性检查
→ 记录观察、限制和决策
```

正式生产交付在上述路径后增加“需求与验收确认 → 冻结候选基线 → 正式评估 → 分级复核 → 同一基线发布 → 线上观察与回归”。正式路径不得跳过正式自测、交付评估或基线一致性门禁；需求中的上线后业务结果目标必须在发布后另行验证。

本文使用《00｜智能体研发总纲》定义的智能体运行支撑配置、候选基线、评估数据集和正式评估运行等术语。正式生产路径的 `baseline_id` 和 `run_id` 使用 UUIDv4（随机生成的通用唯一标识符第 4 版）；填写人直接执行[模板中的生成命令](./07_研发交付六件套模板.md#唯一标识符id生成复用与填写位置)，无须手工编造。其他路径不为记录强制生成这两个标识。其他字段以[《00｜智能体研发总纲》](./00_总纲.md)和[《04｜评估、测试与回归规范》](./04_评估测试与回归规范.md)为准。

## 2. 先选路径

| 路径 | 适用场景 | 最小交接 | 完成判定 |
|---|---|---|---|
| 研究探索 | 方案、能力或控制效果未知 | 问题、条件、观察、限制、决策 | `采用`、`继续验证`、`不采用`或`未知` |
| 已知缺陷修复 | 现象和预期已知 | 复现、根因、最小修复、受影响检查 | 原失败不再复现，边界未退化 |
| 外部资产复用 | 引入 Skill、工具、MCP 或数据资产 | 来源/版本/范围、适配差异、目标行为验证 | 采用、部分采用或不采用的理由明确 |
| 迁移 | 替换模型、运行时、框架或配置 | 前置条件、差异、结果、回退条件 | 目标行为和安全边界满足 |
| 正式生产交付 | 要发布并承担运行责任 | 下表完整九阶段 | 交付评估通过并发布同一基线 |

前四条路径不发布时可以在决策形成后结束；真实写操作、敏感数据和外部副作用仍按[《05｜安全与控制边界规范》](./05_安全控制边界规范.md)执行。若结果要发布，再转入正式生产路径，不复制已有记录。

## 3. 正式生产路径的阶段与交接

| 阶段 | 责任人 | 必要输入 | 必须输出 | 退出条件 |
|---|---|---|---|---|
| 1. 任务与验收确认 | 开发成员记录；交付负责人确认 | 正式需求、工单、回归记录、迁移指令、法规或合规要求、确认记录及业务约束 | 填有需求来源的 `REQ-xxx` 任务与非目标；风险等级、能力适用性、场景与输入初稿；正式路径的 `AC-xxx`、安全红线及适用的上线后业务结果目标 | 每项需求来源可核验，任务与非目标足以确定当前工作范围，正式路径的验收项已确认 |
| 2. 关键不确定性验证（按需） | 开发成员 | 待验证问题、预期结果、停止条件和足以区分结论的样本 | 最小智能体运行支撑配置、逐样本结果、差异以及继续或停止结论 | 满足预先规定的继续条件；需要调整任务或验收时返回阶段 1；不存在关键不确定性时跳过 |
| 3. 智能体运行支撑配置开发与针对性检查 | 开发成员 | 已确认任务、实施范围、受影响评估用例和控制边界；开展过探索验证时还包括其结论 | 可运行实现、变更记录、针对性检查结果、更新后的场景和评估用例；缺陷修复包含复现结果，迁移包含前置条件和预期状态核对结果 | 受影响用例达到已确认的检查要求；失败和证据缺口均已按第 8 节处理，具备冻结候选基线的条件 |
| 4. 冻结候选基线 | 开发成员 | 候选实现、已确认的需求与验收标准、评估数据集、本次正式评估范围、评分规则、评估实现、安全策略、安全边界、运行时和执行环境 | 开发成员在首次正式自测前生成 `bl-<UUIDv4>` 格式的唯一 `baseline_id`；各冻结项的不可变定位信息 | 所有冻结项可取得、可追溯；评估实现及执行试次规则已经确定，且未改变已确认的验收标准 |
| 5. 正式自测 | 开发成员 | 冻结的候选基线和正式评估范围 | 执行开始前生成的 `run-<UUIDv4>` 格式新 `run_id`、逐试次和逐用例结果、失败证据、自测结论 | 结论为`通过`才能提交；`不通过`回到开发，`不完整`补齐数据或执行条件 |
| 6. 提交交付物 | 开发成员 | 自测通过的候选基线、完整交付记录和机器结果 | 六项交付内容、`cases.jsonl`、`results.csv`，或合规的外置证据引用 | 交付内容完整、不可变定位信息一致，报告可定位 `baseline_id` 和采用的 `run_id` |
| 7. 分级复核与发布准入 | 交付负责人 | 完整交付包、风险等级、本次交付范围、历史问题 | R1/R2/R3 复核等级及理由；实际复跑时的新 `run_id` 和相应范围结果或正式评估运行结论；交付评估结论 | `通过`进入发布；`退回整改`回到对应阶段；`材料不完整`退回补充 |
| 8. 发布同一候选基线 | 发布执行人 | 交付评估通过记录、候选基线、发布及回滚方案 | 发布前已明确且可执行的完整任务链验证步骤和执行条件；发布后记录实际版本、环境、时间、执行记录、验证结果、回滚点和发布状态 | 线上 `baseline_id` 与通过记录一致，真实任务链验证正常；失败则停止或回滚 |
| 9. 发布观察与回归 | 交付负责人确认适用的结果结论；发布执行人写入现有发布或运营记录；开发成员处理回归 | 运行日志、用户反馈、能力失败和安全事件；需求包含上线后业务结果目标时，还包括预定指标、数据源和观察窗 | 运行观察结果、问题分类、可复现评估用例、`regression` 标签及下一轮需求或整改项；适用时记录上线后业务结果 | 运行问题进入明确处理路径，高风险问题已先止损；适用的上线后业务结果已有证据结论 |

正式生产路径的需求来源、智能体任务、交付验收标准和上线后业务结果目标的填写方法，以[《02｜需求定义与场景输入规范》](./02_需求定义与场景输入规范.md)为准；其他路径只记录与当前判断有关的目标、边界、输入和期望观察。

模板、交付目录和复核细则见[《06｜交付评估与发布规范》](./06_交付评估与发布规范.md)；六项交付内容按[《07｜研发交付六件套模板》](./07_研发交付六件套模板.md)填写，可以合并在一份记录中。

## 4. 直接开发与探索验证

### 4.1 直接进入开发

同时满足以下条件时，直接进入实现与针对性检查：

- 任务、预期结果、实施路线和影响范围已经明确；正式生产路径还需确认交付验收标准；
- 所需能力、权限、安全边界和架构范围已经确认；
- 不存在会改变实现选择的关键技术或控制假设。

常规功能开发、已知缺陷修复、回归整改和按令迁移均可适用。新增功能应记录目标行为和影响范围；缺陷修复应记录复现步骤、当前结果和预期结果；迁移应记录前置条件、预期状态和迁移范围。实现完成后运行原失败用例或受影响用例的针对性检查。

### 4.2 探索验证

只有存在会影响实施路线的关键可行性或方案不确定性时，才开展探索验证。每次原则上验证一个具体问题，例如：

> 加入明确上下文和一个只读工具后，智能体是否能稳定完成目标任务？

执行要求：

1. 从主要场景、关键边界和真实失败中选择足以区分结论的探索样本；数量由待验证问题决定，不设固定配额。
2. 在实验前固定当前方案和评价方式，实验过程中不得根据结果调整标准。
3. 优先采用能够验证待验证问题的最小智能体运行支撑配置。
4. 为识别效果来源，原则上每轮只改变一个主要变量；多个变量确实不可分割时，应记录组合变化及无法单独归因的限制。
5. 逐样本记录结果和失败原因，不得仅报告汇总结论。
6. 根据证据决定继续、调整假设或停止，不因已经投入时间而扩大方案。

实验记录至少包含待验证问题、预期结果、停止条件、样本、逐样本结果、主要失败和继续或停止结论。禁止将完整后台、资产中心、自动评估平台或复杂多智能体管理能力作为探索验证的前置条件。探索中发现任务边界或安全边界需要实质调整时，先更新任务记录；正式生产路径还需在进入评估前确认完整验收标准，不能用结果倒推标准。

## 5. 开发检查与正式自测的边界

开发期间应先运行受影响评估用例或与当前问题相称的针对性检查，以快速发现智能体运行支撑配置修改造成的直接问题。针对性检查可以重复，也可以仅运行子集，但不得形成“正式自测通过”“交付通过”或“可以发布”的结论。

进入正式自测前（仅正式生产路径），开发成员必须：

- 完成本次正式评估范围内需求场景和适用输入覆盖单元到评估用例的映射；
- 保证有效评估用例和不同用户输入达到当前交付适用的正式规模与覆盖门槛，且低质量样本未计入有效数量；
- 将 `core`、`boundary`、`safety`、`regression` 评估用例保存在同一版本化评估数据集中；
- 模板一已使用 `AC-xxx` 唯一定义项目特有的验收要求、判定作用和阈值；模板三已为每个验收项映射评估用例、评价方法、数据来源、结果字段、单位、执行试次和聚合规则，且未复制或修改验收阈值；
- 冻结候选基线后，完整执行本次正式评估范围。

若修改任何冻结项，当前自测结果不再代表新的候选版本：必须生成新 `baseline_id` 并重新正式自测。仅因执行错误或复核需要而重跑同一基线，且冻结项未变化时，保留 `baseline_id` 并生成新 `run_id`。

详细数据和判定契约见[《04｜评估、测试与回归规范》](./04_评估测试与回归规范.md)。

## 6. 修改前评估的对照规则（正式生产路径；其他路径按需）

本节适用于正式交付或需要声明相对改进的变更。研究、已知问题修复、外部资产复用和迁移任务只在当前问题需要对照时建立最小参照，不默认制作完整基线。

正式生产路径要求证明本轮相较旧版有所改进时，开发成员必须在需求或报告中填写修改前版本原有的 `baseline_id + run_id`。其他路径记录可定位的参考版本、被测版本、本次输入与执行条件即可，不为比较强制创建正式编号。仅填写“旧版结果”不满足追溯要求。

- 既有运行是否可直接作为版本对照，必须逐项满足《04｜评估、测试与回归规范》第 13 节规定的可比性条件；
- 任一可比性条件不满足时，应按当前口径重跑修改前实现，或明确标记为仅供参考的历史结果，不得直接计算提升；
- 只有修改前版本确实代表当前业务实际使用方式，且业务样本与观察窗可比时，该运行才能同时支持当前业务表现的判断；
- 全新项目或无可比旧运行时，对照编号记录 `N/A（无可比对照）`，只验证绝对门槛。需要形成当前业务表现或相对改进结论时，按《02｜需求定义与场景输入规范》记录必要的证据状态和测量计划；没有可靠对照时不得形成相对提升结论。

版本对照仅用于比较可比运行，不得将测试版本表现直接表述为线上业务结果。

## 7. 分级复核执行要求（正式生产路径）

本节的 R1/R2/R3 适用于正式生产路径。其他路径按风险和决策需要复核，不因使用本规范而强制配置完整角色链。

交付负责人根据[《00｜智能体研发总纲》](./00_总纲.md)记录低、中或高风险，再选择不低于其最低要求的 R1、R2 或 R3 复核等级，并在报告中说明本次交付范围和选择理由：

- **R1**：核验交付内容、机器数据、数量、覆盖、不可变定位信息、追溯关系和原始结果。
- **R2**：完成 R1，并在本次冻结的正式评估范围内，复跑全部 `blocking`、全部 `safety` 及本次交付影响评估用例的并集。
- **R3**：由未参与该版本开发的人完整重跑本次冻结的正式评估范围，并将复核结果与开发自测结果比对。

复跑必须使用同一评估数据集、评分规则和候选基线，产生新的 `run_id`，不得复制或维护第二套事实数据。R2 仅按《04｜评估、测试与回归规范》形成`范围内通过`、`范围内不通过`或`范围内不完整`的范围结果；R3 形成`通过`、`不通过`或`不完整`的正式评估运行结论。发现证据不一致、不可复现或真实性存疑时，若当前复核等级为 R1 或 R2，必须升级为 R3；若已经执行 R3，必须停止评估并调查，不得循环升级或无依据重复运行。

## 8. 失败原因路由

失败必须按根因回流。非生产任务只回到受影响的最小环节；正式生产任务不得仅修改报告表述，也不得在未定位根因时反复执行正式评估运行。

下表中的 `REQ`、`AC`、正式自测和交付评估适用于正式生产路径；其他路径沿用同一根因方向，记录对应的最小修复、检查和决策。

| 发现的问题 | 当前结论或动作 | 返回阶段 | 恢复推进条件 |
|---|---|---|---|
| `REQ-xxx` 的需求来源无法核验，或任务与非目标不足以界定当前工作范围 | 不进入探索验证或开发；已进入交付评估时为`材料不完整` | [需求与场景输入](./02_需求定义与场景输入规范.md) | 填写可核验的需求来源，由交付负责人重新确认任务与边界 |
| 探索判定标准或安全红线无法判定；或者进入直接开发前仍有项目特有验收项未确认 | 前者不开展探索验证，后者不进入开发 | [需求与场景输入](./02_需求定义与场景输入规范.md) | 交付负责人确认探索判定标准与安全红线；探索结束后确认全部 `AC-xxx` 及其判定作用和阈值 |
| 需求包含上线后业务结果目标，但验证计划无法判定；或尚无发布后证据却填写了`已达成` | 候选基线冻结前补充或纠正；已进入交付评估时为`材料不完整` | [需求与场景输入](./02_需求定义与场景输入规范.md) | 明确指标、目标值、数据源、观察窗、未达目标后的处理方式和负责人；业务结果需要触发停止、限制范围或回滚时再补相应阈值；需求不包含此类目标时删除相关目标表 |
| 实际使用工具、写操作、长期记忆、检索增强生成或多智能体等能力，但未记录适用性、边界或所需评估用例 | 暂停冻结候选基线 | [需求与场景输入](./02_需求定义与场景输入规范.md)、[智能体运行支撑配置开发](./03_Harness开发与变更规范.md) | 补齐适用性、控制边界及对应评估用例；不适用项记录理由 |
| 外部 Skill 已安装但未加载，或已加载但目标任务未选择 | 不把安装、加载或工具可见状态当作任务能力；暂停适配结论 | [智能体运行支撑配置开发](./03_Harness开发与变更规范.md)、[外部资产复用与集成](./10_外部资产复用与集成.md) | 分别核对加载入口、依赖、描述、任务匹配和同名覆盖，并在目标任务中验证实际选择 |
| 外部资产可调用但未适配当前任务，或调用成功却产生错误任务结果 | 不判定为有效；回到任务适配或编排 | [外部资产复用与集成](./10_外部资产复用与集成.md)、[智能体运行支撑配置开发](./03_Harness开发与变更规范.md) | 修正任务规则、参数构造或结果消费逻辑，并用目标行为验证 |
| 外部资产返回 `accepted`、分页、单位、空值或部分结果，但调用方将其当作已完成、单页、同单位或完整结果 | 保留原始状态并暂停正式结论 | [评估与回归](./04_评估测试与回归规范.md)、[外部资产复用与集成](./10_外部资产复用与集成.md) | 在已有调用或适配边界增加确定性状态/数据转换，并验证未知和部分状态不被吞掉 |
| 接口与取得的事实正确，但最终摘要遗漏、误归类或编造引用 | 执行可以完成，任务结果仍不满足 | [外部资产复用与集成](./10_外部资产复用与集成.md)、[智能体运行支撑配置开发](./03_Harness开发与变更规范.md) | 局部修正 Skill 的目标、组织方式、输出示例或结果消费，并复查相关输入；不改写正确接口 |
| 上游没有任务所需的信息、动作或控制能力 | 记录能力缺口，不以字段改名或包装结果判定成功 | [需求与场景输入](./02_需求定义与场景输入规范.md)、[外部资产复用与集成](./10_外部资产复用与集成.md) | 缩小任务范围、改用已有合适能力或不采用，并保留未支持范围 |
| 合成、Mock 或上游单测通过，但目标环境装载、组合或真实依赖路径失败 | 只承认已通过的局部检查，不形成目标集成结论 | [智能体运行支撑配置开发](./03_Harness开发与变更规范.md)、[外部资产复用与集成](./10_外部资产复用与集成.md) | 核对实际配置和依赖，只替换必要外部边界，在目标环境完成一次相关行为观察 |
| 场景、输入或评估数据集未完整覆盖；未达到当前交付适用的评估规模，且不符合“影响范围明确的有限交付”条件 | 正式自测为`不完整` | [需求与场景输入](./02_需求定义与场景输入规范.md)、[评估与回归](./04_评估测试与回归规范.md) | 补足覆盖和有效输入；低质量输入不得计入数量；按明确范围评估时，依照评估规范记录适用前提、范围限定依据、实际范围和覆盖结果 |
| `blocking` 评估用例或适用的硬门禁指标失败 | 正式自测为`不通过`，或交付评估`退回整改` | [智能体运行支撑配置开发](./03_Harness开发与变更规范.md) | 定位根因、完成必要修改、生成新候选基线并重新正式自测 |
| 执行试次为 `error`、瞬时执行故障或证据无法判定，且冻结项未变 | 正式自测为`不完整` | [评估与回归](./04_评估测试与回归规范.md) | 修复瞬时执行或证据链，对同一基线生成新 `run_id` |
| 执行环境配置、镜像、依赖、资源边界或其他冻结项发生变化 | 原候选基线结论失效 | [智能体运行支撑配置开发](./03_Harness开发与变更规范.md) | 生成新 `baseline_id`，并重新完整正式自测 |
| 越权、绕过审批、泄露或其他安全硬门禁失败 | 立即停止相关动作；自测`不通过`或交付`退回整改` | [安全与控制边界](./05_安全控制边界规范.md) | 修复控制层、形成新基线，并重新执行安全用例和本次正式评估范围 |
| 交付内容缺失、不可变定位信息不一致或结果无法关联 | 交付评估为`材料不完整` | [交付评估与发布](./06_交付评估与发布规范.md) | 补齐真实证据并重新提交；存疑时升级 R3 |
| 复核与开发自测的差异改变 `blocking` 评估用例、适用硬门禁或正式评估运行结论，违反冻结的评价或聚合规则，或使真实性、完整性、可复现性无法确认 | 不得通过；R1 或 R2 升级 R3，已执行 R3 时停止评估并调查 | [评估与回归](./04_评估测试与回归规范.md) | 查明差异；冻结项未变时以新 `run_id` 重跑，冻结项变化时形成新候选基线并重新正式自测 |
| 发布对象与评估通过的 `baseline_id` 不一致 | 停止发布；已部署则回滚 | [交付评估与发布](./06_交付评估与发布规范.md) | 恢复通过基线，或将新对象重新走冻结、自测和评估 |
| 生产能力失败 | 先分类并调查复现条件；能够复现时建立 `regression` 评估用例 | 需求、智能体运行支撑配置或评估规范中对应环节 | 完成原因定位；需要变更时形成新候选基线并完成自测和交付评估 |
| 生产安全事件 | 立即隔离、禁用或回滚，并补充安全评估用例 | [安全与控制边界](./05_安全控制边界规范.md) | 完成止损、根因修复和新基线复核 |
| 需求包含上线后业务结果目标，离线交付通过，但发布后未达目标或证据不足 | 不得追溯修改原自测和交付结论；执行预先规定的未达目标处理方式 | [交付评估与发布](./06_交付评估与发布规范.md)、[需求与场景输入](./02_需求定义与场景输入规范.md) | 记录结果结论和原因，按实际需要继续观察、调查原因、调整需求或进入下一轮整改 |

## 9. 架构扩展停止条件

满足以下条件时，正式生产路径应停止扩展架构并进入候选基线准备和正式自测；研究、修复、复用或迁移任务在形成足够决策证据后即可结束，不为进入正式路径扩展架构：

- 当前主要场景已达到任务要求；
- 关键失败已有明确的处理机制、安全控制或人工接管路径；
- 新增复杂度无法对应已确认任务、已观察失败或实际安全风险；
- 进一步设计无法比现有验证更快取得可信证据。

需要真实用户或真实业务流量参与的灰度（限定用户或流量范围的真实发布）、小范围开放和只读试用均属于发布策略，仅可在候选基线完成正式自测且交付评估通过后进行。发布前的额外验证仅可在隔离沙箱、离线回放（使用已记录请求重新执行）或影子环境（复制真实请求进行验证，但验证结果不驱动真实业务动作）中开展，不得改变真实业务状态。
