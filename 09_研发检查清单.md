# 09｜智能体研发交付检查清单

本清单用于三个实际交接点，不替代六项交付内容和机器证据。不适用项必须记录业务理由，不得仅为满足清单形式而创建空组件或空评估用例。

## A. 开发成员提交前

### A1. 需求、任务与验收

- [ ] 每项 `REQ-xxx` 已填写可核验的需求来源，包括正式需求、工单、事故、回归、迁移、法规或合规要求，或者确认记录引用；技术实现方案在任务和交付验收标准确认后确定
- [ ] 每项 `REQ-xxx` 已明确用户、使用场景、目标任务、输入、输出或动作及非目标；每项需求至少关联一个定义最低可接受任务结果的硬门禁 `AC-xxx`，更高期望另设质量目标
- [ ] 模板一是项目特有交付验收标准的唯一事实源；全部硬门禁已在直接开发前，或探索结束后、进入开发前明确阈值或允许结果，并由交付负责人确认
- [ ] 低/中/高风险等级已按数据敏感度、后果、自治程度、作用规模和可逆性填写依据，并列明决定等级的需求和能力；每项 `AC-xxx` 已填写阈值依据；使用量化信息时已标为`实测`、`估算`或`暂无可靠数据`并填写相应来源或处理计划
- [ ] 本项仅在需求包含上线后业务结果目标时适用：已在候选基线冻结前完整确认指标、目标值、数据源与计算口径、观察窗、未达目标后的处理方式和负责人；业务结果需要触发停止、限制范围或回滚时，已另行明确相应阈值和执行人。需求不包含该目标时，交付材料删除对应字段
- [ ] 工具调用、写操作、多轮状态、长期记忆、检索增强生成、多智能体、路由或降级、外部网络和在线服务已逐项判断适用性；不适用项有理由
- [ ] 仅在存在会改变实施路线的关键不确定性时开展探索验证，并在实验前明确待验证问题、预期结果和停止条件，使用 5～20 条真实或高度接近真实的输入；任务、验收、实施路线、影响范围及所需控制均已明确时直接开发
- [ ] 中、高风险写操作的合法动作和审批条件已写入需求“输出或动作”，禁止动作已写入非目标，审批绑定、越权阻断和最终状态已进入交付验收标准
- [ ] 提示词、工具、技能和工作流等实现均能对应已确认任务、已观察失败或实际风险，没有为体现架构而增加复杂度

### A2. 评估数据与冻结

- [ ] 六项交付内容已按当前候选版本填写；合并版或拆分版只有一套，模板文件未被直接当作交付物
- [ ] `delivery/` 未复制源码或创建空证据目录；外置数据记录了受控 URI、版本、SHA-256 和访问方式
- [ ] 评估用例数量和经逐条输入质量复核的不同用户输入数量，符合《04｜评估、测试与回归规范》的适用规模和范围规则
- [ ] 按影响范围明确的有限交付规则评估时，模板三已记录适用前提、范围限定依据、前一一般门槛通过基线（仅局部维护变更）、新增或受影响项目、实际评估范围和覆盖结果，并由交付负责人在候选基线冻结前确认；未删除已有评估用例
- [ ] 一般门槛覆盖的已定义需求场景，或明确范围内新增及受影响的需求场景与适用输入矩阵格，覆盖率均为 100%；`N/A` 有明确理由
- [ ] 每个评估用例仅归属一个 `scenario_id + intent_id` 和一个 `acceptance_id`，通过 `requirement_ids` 可追溯到该验收项关联的需求，并有可执行的预期行为、检查项和 `required_trials`；同一输入验证多个验收项时已按检查项拆分用例
- [ ] 机械改写、仅变更标点或对象名、重复执行试次和未复核的批量生成样本未计入数量
- [ ] `core`、`boundary`、`safety`、`regression` 使用同一评估数据集，仅以标签区分
- [ ] 模板一仅记录实际适用的任务质量、安全、稳定性、可靠性、时延和成本验收项；成本只在影响验收、预算、风险或方案比较时纳入
- [ ] 模板三已为每个 `AC-xxx` 建立且仅建立一行评估实现，每行至少命中一个有效评估用例，且每个命中用例的 `acceptance_id` 等于该行验收编号；各行命中用例的并集与本次冻结正式评估范围完全一致，无遗漏用例；评估用例编号或可执行筛选条件、评价方法、数据来源、结果字段、单位、执行试次和聚合规则齐全，且未复制或修改模板一中的验收阈值
- [ ] 评估数据集、正式评估范围、执行试次次数、聚合规则、评分规则和评估实现已在候选基线冻结前确定；交付负责人的确认身份与时间可核验，且未事后降低标准
- [ ] 候选源码、配置、评估数据集、评分规则、执行环境和所有实际适用组件均已填写不可变定位信息；仓库内使用路径和 Git 提交，外置对象使用受控 URI、对象版本和 SHA-256；无独立安全策略或镜像时已填 `N/A（原因及实际承载位置）`
- [ ] 开发成员在上述冻结项齐备后、首次正式自测前生成了 `bl-<UUIDv4>`，并填入模板五和模板六

### A3. 正式评估运行与结论

- [ ] 每次准备将正式自测、复核或回归结果写入事实源时，该次执行人在开始前生成了新 `run-<UUIDv4>`；不进入正式结果的临时检查未被强制留 ID
- [ ] 已在冻结候选基线上完整执行本次正式评估范围，未用探索实验或针对性检查替代
- [ ] `results.csv` 符合《04｜评估、测试与回归规范》第 9 节的唯一接口，每行一个执行试次；`status=error` 时 `score` 必须留空，不得填写 0 分代替缺失结果
- [ ] 每个评估用例的结果行数等于 `required_trials`，不存在缺失、重复 `trial_id`、静默删除或未解决 `error`
- [ ] 适用时已按冻结范围报告目标分位时延与超时率、工具调用与重试、模型输入与输出文本用量、总直接成本及每个成功任务成本；单位、币种和聚合口径一致
- [ ] `blocking` 用例 100% 通过，模板一全部硬门禁达到阈值，安全违规和已映射为 `blocking` 的历史失败用例复现数均为 0
- [ ] 开发自测正式评估运行结论已直接按照《04｜评估、测试与回归规范》第 11 节形成
- [ ] 报告 A 区已记录自测 `run_id`、结果、失败/限制、结论、执行人和日期
- [ ] 同一候选基线重跑仅生成新 `run_id`；任一冻结项变化均生成新 `baseline_id` 和新 `run_id`；旧结果未被覆盖
- [ ] 需要证明“相较旧版有所改进”时已填写可比的对照 `baseline_id + run_id`；无可比对照时仅报告绝对结果
- [ ] 正式评估运行通过时，质量目标结论按预设目标和不可退化护栏填写`达成`或`未达成`；正式评估运行不通过或不完整时填写`未判定`；未设定质量目标时填写 `N/A`

### A4. 安全与回滚

- [ ] 仅对实际适用的工具、写操作、不可信内容、长期记忆、检索增强生成、多智能体、多租户或审批建立安全用例，未构造缺乏实际攻击面的用例
- [ ] 适用安全用例同时核对了回复、工具与关键参数、权限/审批/策略、执行预算和最终业务状态
- [ ] 中、高风险数据、动作、凭据、失败停止、审计和回滚边界已由可强制机制实现
- [ ] 上一稳定版本或可恢复状态、触发条件、执行人、操作步骤和回滚后验证方法已明确

## B. 交付负责人评估

- [ ] 六项交付内容齐全，合并或拆分形式唯一，名称、版本、候选基线和引用一致；报告 A 区的开发成员和日期完整
- [ ] `cases.jsonl` 可访问且不可变定位信息一致；不可变结果快照和本次结论采用的运行编号列表均已锁定
- [ ] 自测 `run_id` 能在结果中唯一定位，评估用例、执行试次和候选基线可追溯
- [ ] 尚无已确认实质失败，但材料缺失、证据不可访问或无法追溯时，结论为`材料不完整`，未依据经验作补充判定
- [ ] 已分别记录风险等级和复核等级，复核等级不低于风险规定的最低要求，并写明本次交付范围与选择理由
- [ ] 已确定一个最终复核等级并完成对应要求：R1 核验原始证据；R2 在此基础上，在本次冻结的正式评估范围内复跑全部 `blocking`、`safety` 和本次交付影响评估用例的并集；R3 由未参与该版本开发的人完整复跑本次冻结的正式评估范围
- [ ] R2、R3 每次复跑均生成新 `run_id` 并写入同一结果事实源；报告 B2 逐次列出复跑，R2 填写范围结果，R3 填写正式评估运行结论，并标明纳入本次结论的运行；R1 未复跑时填写 `N/A（R1 未复跑）`
- [ ] 自测与复核差异已按运行前冻结的规则处理：文本按同一预期行为和检查项判断，数值仅在预设容差存在时判断一致性；改变 `blocking` 用例、适用硬门禁或正式评估运行结论的差异已按实质不一致处理
- [ ] 已核对每项任务的需求来源和验收表，并核对智能体整体风险依据是否列明决定等级的需求和能力；已逐项核对智能体任务、交付验收标准、输入覆盖、能力、历史回归、安全、复现性、完整业务链验证步骤及执行条件和回滚；未仅依据总分作出结论
- [ ] 报告 B 区已记录复核方式、范围、发现、结论、执行人/交付负责人和日期
- [ ] 已确认实质失败时，即使同时存在材料缺口，结论仍为`退回整改`，并在整改项中列明待补材料
- [ ] 结论限定为`通过`、`退回整改`、`材料不完整`，未使用“条件通过”
- [ ] 整改改变冻结项时，已要求新建候选基线、生成新运行编号、重新完整自测并再次提交；仅补充材料且未发生重跑时，未生成无对应运行事实的编号

## C. 发布、回滚与上线后运行结果

- [ ] 仅交付评估`通过`的候选版本进入发布，待发布 `baseline_id`、实际适用的不可变定位信息和发布对象与报告完全一致
- [ ] 评估通过后未临时修改提示词、模型、工具、安全策略、执行环境或其他冻结项
- [ ] 灰度、只读或小范围开放仅作为发布策略，未被用来绕过门槛或形成“条件发布”
- [ ] 所有发布均已记录运行观察范围、异常触发条件、停止/回滚动作和负责人；仅在需求包含上线后业务结果目标时，发布记录另填写指标、目标、数据源、当前证据状态和观察窗；需求不包含该目标时，交付材料删除对应字段
- [ ] 发布验证使用真实入口和对外协议，完整经过实际智能体、工具、审批或数据依赖，并检查用户可见结果、最终业务状态和审计记录；未用超文本传输协议（HTTP）状态码 200 或 `/health` 代替
- [ ] 实际发布时间、目标环境、制品、任务链结果和回滚情况已写入现有发布记录
- [ ] 本项仅在需求包含上线后业务结果目标时适用：观察窗结束后已按每个指标记录发布版本/`baseline_id`、实际观察窗和样本、数据源与不可变证据、实际值、口径一致性、确认人/日期和后续动作；需求不包含该目标时，交付材料删除对应字段
- [ ] 本项仅在需求包含上线后业务结果目标时适用：结果结论限定为`已达成`、`未达目标`、`证据不足`，且未以离线运行或健康检查替代该结论；需求不包含该目标时，交付材料删除对应字段
- [ ] 模型、工具、模型上下文协议、检索增强生成、策略或路由、任务质量、安全、时延和成本等实际适用的漂移信号有阈值和处理动作
- [ ] 对已发布候选基线单独执行周期或漂移回归时，已在现有发布或运营记录中登记 `baseline_id`、`run_id`、执行范围、执行人、日期、正式评估运行结论和不可变结果快照定位信息；仓库内快照使用路径和 Git 提交，外置快照使用 URI、对象版本和 SHA-256
- [ ] 真实失败已先分类、止损和调查；能够复现并可转化为评估用例时，才纳入同一评估数据集并标记 `regression`，安全失败同时标记 `safety`；暂不能复现时保留调查记录
- [ ] 修复后先运行受影响子集；形成新候选版本时，仍完整自测并执行不低于风险最低要求的复核等级
- [ ] 下线或替换时已按范围停止流量导入或任务调度、撤销凭据与工具权限、处理记忆或业务数据、记录替代路径，并验证已生效
