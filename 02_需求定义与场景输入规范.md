# 02｜需求定义与场景输入规范

## 1. 目标

把“想做一个智能体”转换成**可测试、可验收、可持续迭代的任务定义**。

## 2. 需求定义最小结构

每个 Agent 只要求先写清以下内容：

| 字段 | 必填内容 |
|---|---|
| 需求 ID | 使用稳定编号，例如 `REQ-001` |
| 用户 | 谁使用 |
| 触发场景 | 什么时候使用 |
| 当前问题 | 现有方式哪里慢、难、贵或不稳定 |
| 目标任务 | Agent 实际完成什么 |
| 输入 | 最少需要哪些信息 |
| 输出/动作 | 用户最终得到什么或系统执行什么 |
| 成功标准 | 怎样判断有效 |
| 非目标 | 明确不解决什么 |
| 风险等级 | R1 / R2 / R3 |

需求 ID 一旦进入评估数据集，不应因文字润色而变化；需求拆分、合并或删除时，应同步更新关联场景和评估用例。

## 3. 好需求与坏需求

### 不推荐

> 建设一个网络安全运营专家智能体，提升安全运营效率。

问题：无法据此设计测试，也无法验收。

### 推荐

> 面向 SOC 分析员，在收到终端或网络告警后，自动汇总关联资产、用户、进程和网络证据，给出风险判断、关键证据、攻击阶段判断和处置建议；普通告警不直接执行封禁，高风险处置必须进入审批。

这类定义可以直接转成任务和评估。

## 4. 场景与意图

场景不是功能菜单，而是：

> **某类用户 + 某种业务上下文 + 某种业务目标。**

每个场景使用稳定的 `Sxx` 编号，并至少定义一个意图编号 `I01`；同一场景存在多个不同目标时，继续定义 `I02`、`I03`。这样可以避免把“不同问法”和“不同任务”混为一谈，并保证每个 Eval Case 都能追溯到明确意图。

探索阶段先列出 3～5 个高价值业务场景即可，例如“单告警研判”“关联调查”“处置审批”。口语、模糊、信息不足等属于输入变体；Tool 超时、权限和审批状态等属于 Case Context。除非用户目标发生变化，否则不要把这些条件重复定义成新场景。

## 5. 用户输入覆盖

### 5.1 为什么单独管理输入

Agent 的失败经常不是“不会任务”，而是用户说法变化后没有正确理解。

因此应把真实输入作为独立资产保存，并能追溯到需求、场景、意图和评估用例。

### 5.2 输入变体类型

每个主要场景按实际适用性考虑以下类型；枚举值与《04｜评估、测试与回归规范》保持一致：

| 枚举值 | 含义 | 示例 |
|---|---|---|
| `standard` | 标准表达 | “分析这条告警” |
| `colloquial` | 口语表达 | “这个报警咋回事？” |
| `elliptical` | 简写或省略表达 | “看看昨天财务那台机器” |
| `multi_intent` | 同时包含多个意图 | “分析一下，顺便看看要不要封” |
| `incorrect_or_noisy` | 含错误或噪声信息 | 时间、IP、名称不准确 |
| `missing_info` | 缺少完成任务所需信息 | 只给一个告警 ID |
| `high_risk` | 高风险或越权要求 | “直接把机器隔离掉” |

不是每个场景都必须机械覆盖全部类型；不适用项应明确说明原因。

## 6. 输入覆盖矩阵

使用一张可追溯矩阵管理覆盖关系：

```text
需求 ID × 场景 ID × 意图 ID × 输入变体类型 → Case ID 或 N/A 原因
```

最小记录示例：

| 需求 ID | 场景 ID | 意图 ID | `standard` | `colloquial` | `elliptical` | `multi_intent` | `incorrect_or_noisy` | `missing_info` | `high_risk` |
|---|---|---|---|---|---|---|---|---|---|
| REQ-001 | S01 | I01 | case-001 | case-002 | case-003 | case-004 | case-005 | case-006 | N/A：该场景无执行动作 |
| REQ-002 | S02 | I01 | case-007 | case-008 | case-009 | case-010 | case-011 | case-012 | case-013 |

矩阵遵循以下原则：

- 所有已定义需求场景都必须进入矩阵；
- 正式评估时，所有适用格都必须关联至少一个有效 Case；
- 单元格可关联多个 Case ID，用于覆盖同一输入变体下不同且必要的 Context 或风险条件；
- `N/A` 只用于确实不适用的格子，并写明原因，不能用来规避测试；
- 不按场景平均分配固定配额，额外样本优先覆盖高频、高风险和历史失败位置；
- 同一 Case 可覆盖多个标签，但不得因多次引用而重复计数。

正式评估的数量、质量、计数和通过规则以《04｜评估、测试与回归规范》为唯一依据。

## 7. 需求变更规则

以下变化属于**需求变化**，不能仅当 Harness 优化处理：

- 新增或删除业务目标；
- Agent 从“建议”变成“自动执行”；
- 新增高风险工具权限；
- 用户群体发生变化；
- 输出责任边界变化；
- 验收标准显著变化。

需求变化后，应同步修改场景、输入覆盖矩阵、评估用例和验收依据。
