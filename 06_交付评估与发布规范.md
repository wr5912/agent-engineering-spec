# 06｜交付评估与发布规范

> 本规范只适用于正式生产交付。研究、已知缺陷修复、外部资产复用/集成或迁移且不发布的工作，使用《00｜总纲》《01｜研发流程与按需探索验证》和《10｜外部资产复用与集成》定义的最小记录，不套用本规范的六件套、候选基线和发布准入流程。需要保存可复用研究版本时，用现有版本控制或研究记录标明来源、版本、适用范围和限制即可。

## 1. 核心原则

> **交付评估用于判断明确候选版本是否达到发布准入条件，不用于判定上线后的业务结果。**

整体完成至少包括：需求与非目标明确、交付材料完整、正式自测有效、能力与安全要求达到预设标准、候选版本可复现，以及发布和回滚具有可核验记录。涉及外部资产时，先按《10｜外部资产复用与集成》确认来源、版本、适配范围和目标任务中的实际行为，再将适用内容纳入候选基线。

以下三个事项必须保持概念和结论边界清晰：

- **发布准入是否通过**：交付材料、适用的硬门禁指标、证据完整性条件和复现要求是否满足；
- **质量目标是否达成**：依据候选基线冻结前设定的质量目标和不可退化护栏判定；目标要求相较旧版改进时，必须提供符合《04｜评估、测试与回归规范》第 13 节全部可比性条件的对照 `baseline_id + run_id`；
- **上线后业务结果目标是否达成**：仅在需求包含此类目标时适用，发布后在预先约定的观察窗内依据真实业务数据独立判定。

不得用一个总分让成本、时延或表达质量抵消任务失败、越权、绕审批或其他安全违规。

## 2. 角色、流程与结论

| 阶段 | 责任人 | 必须完成的动作 | 输出 |
|---|---|---|---|
| 开发与正式自测 | 开发成员 | 冻结候选基线，完整执行本次正式评估范围，核对结果并填写报告 A 区 | 开发自测正式评估运行结论和交付记录 |
| 交付评估 | 交付负责人 | 检查整体完成度，依据风险选择复核等级，核验证据或独立复跑，并填写报告 B 区 | 交付评估结论 |
| 发布、回滚与运行结果记录 | 发布执行人记录；交付负责人确认适用的结果结论 | 发布通过的同一候选基线，按已明确且可执行的完整业务链验证步骤完成发布验证，并记录实际发布、回滚和适用的上线后业务结果 | 现有发布/运营记录 |

一人可以兼任多个角色，但必须按角色分别留痕。最高等级的独立复核（R3，具体动作见第 7.2 节）必须由未参与该版本开发的人执行。

```text
冻结候选基线和适用的上线后业务结果验证计划
→ 开发成员完整正式自测
→ 提交六项交付内容和机器证据
→ 交付负责人评估
→ 通过：将已确认的发布观察、停止/回滚条件和适用的结果验证计划写入现有发布记录
→ 验证实际业务协议与完整任务链
→ 完成发布观察与回归；适用时记录上线后业务结果
→ 退回：完成整改，按变更情况生成新候选基线，并重新执行完整流程
```

结论只能使用以下状态：

- 开发自测正式评估运行结论：`通过`、`不通过`、`不完整`；
- 交付评估：`通过`、`退回整改`、`材料不完整`；
- 发布结果：`未发布`、`已发布`、`发布失败`、`已回滚`；
- 上线后业务结果（需求包含相应目标时）：`已达成`、`未达目标`、`证据不足`。

禁止使用“条件通过”或“条件发布”。已知限制只有在需求中已明确属于非目标或经确认不影响智能体任务、交付验收标准、硬门禁和安全边界时，才可以随“通过”结论如实记录；否则必须退回整改。

## 3. 智能体研发的六项交付内容

| 内容单元 | 主要内容 |
|---|---|
| 智能体需求定义 | 每个 `REQ-xxx` 的需求来源、智能体任务与非目标；`AC-xxx` 项目特有交付验收标准；能力适用性；风险等级及其依据；适用的上线后业务结果目标 |
| 用户场景与输入覆盖矩阵 | 需求场景、意图、输入变体及其评估用例映射 |
| 任务与评估数据集说明 | 评估数据集、用例质量、评价方法、评分规则，以及每个 `AC-xxx` 的评估实现 |
| 安全与控制边界清单 | 数据、工具、权限、审批、失败安全和安全评估用例映射 |
| 候选基线 | 本次自测和交付评估针对的完整运行组合 |
| 自测与交付评估报告 | 开发成员的正式评估运行结论和交付负责人的整体评估结论 |

以上六项内容合称“六件套”，其含义是六类必需信息，不是必须建立六个独立文件。默认可将六项内容按模板顺序合并到一份 `交付记录.md`；已有团队文档或多人并行维护确有需要时，可以拆分为六份文件。无论采用哪种形式，字段、责任人、确认时间和相互引用均不得缺失。

`cases.jsonl` 和 `results.csv` 是交付记录必须引用的机器证据，不计入六项内容。模板文件本身不是交付物；只有按具体项目填写的内容和实际运行证据才构成交付物。

## 4. 唯一交付目录

具体智能体项目采用以下最小结构。本节是交付目录的唯一权威定义；其他文档应引用本节，不得另行增加固定目录。

```text
<agent-project>/
├── ...                                # 智能体及其运行支撑配置的源码与配置
└── delivery/
    ├── 交付记录.md                    # 按模板顺序包含六项交付内容；可以按需拆分
    └── eval/
        ├── cases.jsonl
        └── results.csv
```

使用规则：

- `delivery/` 不复制智能体及其运行支撑配置的源码、配置或构建产物，仅记录其不可变定位信息；
- 六项内容拆分时，文件名依次为 `01_智能体需求定义.md`、`02_用户场景与输入覆盖矩阵.md`、`03_任务与评估数据集说明.md`、`04_安全与控制边界清单.md`、`05_候选基线.md`、`06_自测与交付评估报告.md`，并继续放在同一 `delivery/` 目录；不得同时维护内容重复的合并版和拆分版；
- 仓库内仅维护一套当前交付结构，历史版本通过 Git 提交、版本标签（Tag，用于标记可识别版本）或现有制品库版本追溯，不建立 `latest`、`final-final` 等重复目录；
- `cases.jsonl`、`results.csv` 可安全入库时放入 `delivery/eval/`；
- 敏感或大型数据可以外置，但文档必须记录受控的统一资源标识符（URI）、版本、SHA-256 内容摘要（用于核对内容是否变化的摘要值）和实际访问方式；外置时不得仅为保持目录形式而创建空文件或空目录；
- 不预建 `evidence/`、`logs/`、`reports/` 等空目录。确有额外证据时，由 `results.csv` 的 `evidence_ref` 指向现有受控位置；
- 实际发布结果写入团队现有发布记录。

## 5. 候选基线

候选基线必须绑定本次正式自测、交付复核和计划发布实际使用的完整组合。唯一填写结构为《07｜研发交付六件套模板》中的模板五；本节不再定义第二套基线字段。

模板五至少必须绑定：

- 需求版本，以及模板一至模板四中的任务、验收项、评估实现和安全边界；
- 智能体及其运行支撑配置、模型、运行时、安全策略、工具和实际启用的条件能力；
- 评估数据集、评分规则、执行试次与聚合规则、执行环境和构建制品。

仓库内文件使用路径和不可变 Git 提交定位，不要求对同一文件另行填写 SHA-256。外置数据、外置配置和构建制品使用受控统一资源标识符、版本或对象标识和 SHA-256 内容摘要定位。同一 Git 提交覆盖多个字段时，其他字段可交叉引用，不重复生成摘要。

不存在或不适用的组件填写 `N/A（原因）`，不得为满足模板形式创建空制品、空配置或空系统。交付结论要求与旧版作定量比较时，在模板五填写可比的对照 `baseline_id + run_id`；没有可比运行时只报告绝对验收结果。

`baseline_id` 和 `run_id` 的含义、生成命令、填写位置和重跑规则以《07｜智能体研发交付六件套模板》“唯一标识符生成、复用与填写位置”为准。任一冻结项变化时建立新候选基线；同一候选基线重跑时保留 `baseline_id`、生成新 `run_id`。结果追加到同一事实源，不覆盖历史运行；报告通过 Git 提交或外置对象版本锁定所采用的不可变结果快照。

## 6. 开发成员提交条件

开发成员只有在以下内容齐备后，才能将候选版本提交为“待交付评估”：

- 六项交付内容已经按当前候选版本填写；合并或拆分形式唯一，名称、版本和引用一致；
- 评估数据集符合《04｜评估、测试与回归规范》的适用范围、数量、质量和覆盖要求；
- 模板一已唯一记录项目特有的验收要求、判定作用、阈值及依据，并在直接开发前，或探索结束后、进入开发前由交付负责人确认；模板三已为每个 `AC-xxx` 记录并冻结评估用例范围、评价方法、数据来源、结果字段、单位、执行试次和聚合规则，且未另行修改验收标准；
- 需求包含上线后业务结果目标时，指标、目标值、数据源与计算口径、观察窗、未达目标后的处理方式和负责人已在候选基线冻结前确认；业务结果需要触发停止、限制范围或回滚时，相应阈值已明确；
- 本次冻结的正式评估范围已在候选基线上完整执行，所有要求的执行试次均有结果；
- `cases.jsonl`、`results.csv` 或其受控引用可访问，并能按第 5 节规定的方式核对不可变定位信息；
- 《自测与交付评估报告》A 区已填写并由开发成员确认；
- 开发自测正式评估运行结论为`通过`。

开发自测正式评估运行结论直接采用《04｜评估、测试与回归规范》第 11 节的判定结果。结论不是`通过`时，不得提交为“待交付评估”。

## 7. 交付负责人评估

### 7.1 材料完整性检查

交付负责人应首先核对六项交付内容、机器证据、不可变定位信息和相互引用，并确认报告 A 区已填写开发成员及日期。材料缺失、证据不可访问、结果无法定位或候选基线不一致，且尚无已确认的实质失败时，应判定为`材料不完整`，不得依据主观判断补齐证据缺口。已有证据能够确认实质失败时，应按第 7.4 节的优先级判定。

### 7.2 确定并执行复核等级

先在报告中分别记录风险等级（低/中/高）与复核等级（R1/R2/R3）。复核等级不得低于《00｜智能体研发总纲》为该风险规定的最低等级；变更重大或证据存疑时应升级。

| 复核等级 | 最低复核要求 |
|---|---|
| R1 | 审查不可变定位信息、数量、覆盖、追溯关系和原始结果；不执行复跑，需要独立复跑时选择 R2 或 R3 |
| R2 | 在证据审查基础上，在本次冻结的正式评估范围内独立复跑全部 `blocking`、`safety` 以及本次交付影响评估用例的并集，并记录范围内复核结果 |
| R3 | 由未参与该版本开发的人完整复跑本次冻结的正式评估范围，并记录正式评估运行结论 |

R2、R3 每次复跑都必须使用新的 `run_id`，结果写入同一 `results.csv` 或同一受控结果系统；不得复制第二套评估数据集或结果事实源。R2 按《04｜评估、测试与回归规范》第 3 节填写`范围内通过`、`范围内不通过`或`范围内不完整`，不得将`范围内通过`表述为正式评估运行通过。R3 按该规范第 11 节填写`通过`、`不通过`或`不完整`。同一基线发生多次复跑时，报告 B2 逐次记录；R1 未复跑时记录 `N/A（R1 未复跑）`。

复核差异按《04｜评估、测试与回归规范》中运行前冻结的规则判断。文本结果均满足同一预期行为和检查项时，记录差异并分别评价质量目标和护栏；数值结果仅在运行前已设跨运行容差时判断是否一致，未设容差时分别报告实测值。出现以下任一情形时，R1 或 R2 必须升级为 R3：不可变定位信息或追溯关系无法核对；原始证据不一致；差异改变阻断用例、适用硬门禁或正式评估运行结论；差异违反冻结的评价或聚合规则；结果真实性、完整性或可复现性无法确认。已经执行 R3 时必须停止评估并调查；只有在执行条件或证据链修复后，才可使用新 `run_id` 重跑同一候选基线；冻结项发生变化时，必须建立新候选基线并重新正式自测。

### 7.3 检查整体完成度

交付评估至少回答：

1. 每个 `REQ-xxx` 的需求来源是否可核验，任务是否与该来源一致；智能体整体风险依据是否列明决定等级的需求和能力，每个 `AC-xxx` 的阈值依据是否已填写；
2. 每个 `REQ-xxx` 是否完成、非目标边界是否得到遵守，每个 `AC-xxx` 是否具有至少命中一个本次冻结范围内有效评估用例的评估实现；硬门禁是否达标，质量目标与不可退化护栏是否已按其判定作用记录结果；需求包含上线后业务结果目标时，验证计划是否已在候选基线冻结前完整确认，且未在发布前填写“已达成”；
3. 需求场景、输入变体和安全边界是否有充分且高质量的评估用例证据，逐条输入质量复核人员、日期和依据是否可信；
4. 自测和复核是否满足完整性、能力、回归和安全要求；
5. 候选基线、评估数据集、结果和制品是否可追溯、可复现；
6. 发布范围、运行控制和回滚目标是否明确可用。

除专项技术审查外，不要求交付负责人逐行评价提示词、技能目录或工作流节点；但影响需求、安全、结果真实性或复现性的实现问题必须纳入评估。

### 7.4 作出结论

| 结论 | 使用条件 |
|---|---|
| `通过` | 材料完整，需求和安全边界满足，正式自测及规定复核通过，候选基线可复现，具备发布与回滚条件；该结论仅表示达到发布准入要求 |
| `退回整改` | 已有充分证据确认需求、能力、安全、复现性或发布准备任一项未达到要求，或独立复跑失败；同时存在材料缺口时一并列明 |
| `材料不完整` | 尚无已确认实质失败，但缺少必需内容、证据、报告责任人或日期、不可变定位信息或追溯关系，无法完成可靠判断 |

交付评估必须按以下优先级形成唯一结论：存在已确认的需求、能力、安全、复现性或发布准备失败时，判定为`退回整改`，即使同时存在材料缺口，也不得以`材料不完整`掩盖实质失败；此时应在整改项中同时列明待补材料。仅在尚无已确认实质失败、但材料不足以完成可靠判断时，判定为`材料不完整`。只有不存在实质失败且材料完整时，才可以判定为`通过`。

结论、依据、交付负责人和评估日期填写在《自测与交付评估报告》B 区。若整改改变任何冻结项，必须建立新候选基线、重新完整自测并再次提交；若只补齐材料且冻结项未变，可沿用候选基线，但发生重跑时仍必须使用新 `run_id`。

## 8. 发布、运行结果验证与回滚

交付评估`通过`仅授权发布报告所绑定的同一候选基线。发布执行人必须再次核对 `baseline_id`、实际适用的不可变定位信息和计划发布对象；禁止在评估后临时修改提示词、模型、策略、工具、运行时或执行环境并直接发布。

不得发布的情形包括：

- 交付评估不是`通过`；
- 任一 `blocking` 评估用例或模板一中的项目特有硬门禁失败，或者安全违规数不为 0；
- 存在未解决的 `error`、缺失执行试次，或复核差异符合第 7.2 节规定的停止评估情形；
- 发布对象与评估通过的候选基线不一致；
- 没有可执行的最小回滚方式。

灰度、小范围开放、只读模式或高风险动作审批可以作为发布策略，但不能替代任何交付门槛，也不能产生“条件发布”。

### 8.1 发布前观察与回滚计划

每次发布均须在团队已有的发布单、变更单或运营记录中写明运行观察范围、异常触发条件、停止或回滚动作和具体责任人。观察项按实际能力和风险选择，不得为不适用项构造指标。

需求包含上线后业务结果目标时，发布执行人必须在发布前将需求阶段已确认的下列内容写入同一记录；此时只得复制和核对，不得新设目标或改变口径：

| 上线后业务结果内容 | 填写要求 |
|---|---|
| 指标与目标值 | 直接引用需求定义，并按适用情况明确分子、分母、单位和统计方法 |
| 数据源与当前值 | 记录可查询位置、样本范围和`实测 / 估算 / 暂无可靠数据`状态；目标要求相对现状改进时必须填写当前值，采用绝对目标且当前值不影响判定时可以省略 |
| 观察窗 | 明确开始条件和结束条件，不得仅记录“发布后观察” |
| 未达目标后的处理方式 | 明确继续观察、分析原因、调整需求或进入下一轮整改等动作和负责人；业务结果需要触发停止、限制范围或回滚时，同时记录相应阈值和执行人 |
| 记录负责人 | 记录现有角色中的具体责任人，不得仅记录团队名称 |

当前值为`估算`时必须写明估算依据。缺失数据会影响目标判定时，填写`暂无可靠数据`、采集方法、负责人和首个回顾时间；不影响绝对目标判定时省略该数据。没有可靠当前值时不得作出“提升 X%”等相对改进结论。需求不包含上线后业务结果目标时，省略本表；运行观察和回滚准备仍按实际风险执行。

### 8.2 验证实际业务链

发布验证必须使用对外提供的实际协议和至少一条完整任务链：真实入口发起请求，经过实际智能体、工具、审批或数据依赖，检查用户可见结果、最终业务状态和必要的审计记录。进程存在、端口监听、超文本传输协议（HTTP）状态码 200 或健康检查接口 `/health` 通过仅表明局部存活，不得替代该项验收。

实际协议验证失败时，发布结果记录为`发布失败`或按预案回滚；不得修改原交付评估报告来隐藏差异。

### 8.3 生产观察、漂移与上线后业务结果

不强制建设新监控平台。使用现有日志、业务报表、抽样和用户反馈即可，但必须能定位发布版本、时间窗、样本和计算口径。至少按实际适用性观察：

| 漂移类型 | 可直接执行的触发示例 |
|---|---|
| 模型 | 实际 `resolved_model` 或供应商版本与基线不一致 |
| 工具/MCP | 参数结构、权限、返回语义或错误率改变 |
| 知识源/RAG | 语料或索引版本未按计划更新，引用过期或无法追溯 |
| 策略与路由 | 安全策略、路由或降级规则，或审批规则与基线不一致 |
| 质量与可靠性 | 任务成功率低于预设阈值、阻断级任务错误出现，或未解决执行错误超过预设阈值 |
| 安全 | 出现越权、绕审批、跨租户、数据外泄或禁止动作 |
| 时延 | 端到端第 95 百分位时延（P95，即 95% 样本不高于该值）或超时率超过预设阈值 |
| 成本 | 每个成功任务成本或资源用量超过预设预算 |

触发后按预案采取一项可执行动作：停止扩大范围、限制为只读、转人工、紧急停用、回滚、撤销凭据或清理受影响的记忆或业务数据。撤销凭据和数据清理必须限定对象与范围，应先保留必要的审计证据，并验证权限已失效或污染状态已清除。

需求包含上线后业务结果目标时，观察窗结束后在同一现有发布/运营记录中填写：

| 结果结论 | 使用条件 |
|---|---|
| `已达成` | 预定观察窗和样本要求满足，且指标达到目标 |
| `未达目标` | 证据足够，但指标未达到目标 |
| `证据不足` | 数据缺失、样本不足、口径变化或观察窗未完成，无法可靠判断 |

观察结束后至少填写一行；存在多个指标时，每个指标分别填写一行：

| 发布版本 / `baseline_id` | 指标与目标 | 实际观察窗与样本 | 数据源、计算口径与证据快照 | 实际值 | 口径与预定义一致 | 结果结论 | 确认人 / 日期 | 后续动作 |
|---|---|---|---|---:|---|---|---|---|
| | | | | | 是 / 否 | 已达成 / 未达目标 / 证据不足 | | |

口径不一致时，不得将实际值与原目标直接比较，结果结论填写`证据不足`，并明确重新取数或重新确认口径的动作。交付负责人确认结果结论，发布执行人将结果写入现有记录。

`未达目标`不得导致原交付评估`通过`结论被追溯修改；应执行发布前已记录的未达目标处理方式。只有该业务结果目标本身已设定停止、限制范围或回滚阈值时，才按相应阈值执行。`证据不足`不得记录为`已达成`。

最小回滚准备包括：上一稳定版本或可恢复状态、明确触发条件、执行人、操作步骤和回滚后的验证方法。

下线或替换智能体时，应在现有发布/运营记录中写明停止流量导入或任务调度、凭据撤销、定时任务与工具权限移除、记忆或数据保留或删除范围、替代路径和验证结果。

## 9. 迁移与生产问题

框架、运行时或模型迁移都属于新候选基线。迁移时使用同一需求和评估体系，补充迁移新增风险评估用例，再选择不低于当前风险最低要求的复核等级执行完整自测与交付评估；不要求新旧框架配置逐项同形。

生产失败应先分类、止损和调查。能够复现并可转化为评估用例的失败，才纳入统一评估数据集并标记为 `regression`；安全失败同时标记为 `safety`。暂不能复现的失败保留调查记录，不作为回归评估用例。修复后先做针对性检查；形成下一候选版本时，仍须完整执行本次冻结的正式评估范围，不得用针对性检查结果替代正式自测或交付评估。
