# 01｜研发流程与最小实验

## 1. 流程目标

研发流程的目标是尽快取得可信证据，并把每次交接变成可检查的输入和输出，而不是一次性写出完整设计。

```text
需求确认 → 最小实验 → Harness 开发与针对性检查 → 冻结候选基线
→ 开发成员全量正式自测 → 提交六件套及机器证据 → 交付负责人分级复核
→ 通过或退回 → 发布同一基线 → 反馈进入回归
```

可以因失败回到前一阶段，但不得跳过正式自测、交付评估或基线一致性门禁。

## 2. 阶段与交接

| 阶段 | 责任人 | 必要输入 | 必须输出 | 退出条件 |
|---|---|---|---|---|
| 1. 需求确认 | 交付负责人；开发成员参与 | 真实问题、用户任务、现状材料、约束 | 确认的需求、成功标准、非目标、风险等级（低/中/高）；场景与输入覆盖矩阵初稿 | 目标可观察、标准可判定、边界可说明；否则继续澄清 |
| 2. 最小实验 | 开发成员 | 已确认需求；5～20 个真实或高度接近真实的探索样本；待验证假设 | 最小 Harness、逐样本结果、主要失败、继续或停止建议 | 关键假设获得足够事实支持；若方向不成立则停止或回到需求确认 |
| 3. Harness 开发与针对性检查 | 开发成员 | 实验结论、失败模式、受影响 Case、控制边界 | 可运行实现、变更记录、针对性检查结果、更新后的场景和 Case | 已知主要问题得到处理，受影响用例无未解释退化，具备冻结候选版本的条件 |
| 4. 冻结候选基线 | 开发成员 | 候选实现、需求版本、完整 Eval Set、Rubric、Policy、安全边界、运行环境 | 唯一 `baseline_id`；冻结项、版本、路径或 URI 及哈希 | 所有冻结项可获取、可追溯；运行前门槛已确定 |
| 5. 全量正式自测 | 开发成员 | 冻结候选基线和完整 Eval Set | 新 `run_id`、逐 Trial/Case 结果、失败证据、自测结论 | 结论为`通过`才能提交；`不通过`回到开发，`不完整`补齐数据或执行条件 |
| 6. 提交交付物 | 开发成员 | 自测通过的候选基线、完整文档和机器结果 | 六件套、`cases.jsonl`、`results.csv`，或合规的外置证据引用 | 交付包完整、哈希一致、报告可定位 `baseline_id` 和采用的 `run_id` |
| 7. 分级复核 | 交付负责人 | 完整交付包、风险等级、变更范围、历史问题 | R1/R2/R3 复核等级及理由；必要的新 `run_id`；交付评估结论 | `通过`进入发布；`退回整改`回到对应阶段；`材料不完整`退回补充 |
| 8. 发布同一基线 | 发布执行人 | 交付评估通过记录、候选基线、发布及回滚方案 | 实际版本、环境、时间、执行记录、健康检查、回滚点和发布状态 | 线上 `baseline_id` 与通过记录一致且验证正常；失败则停止或回滚 |
| 9. 反馈与回归 | 交付负责人决定；开发成员执行 | 运行日志、用户反馈、能力失败、安全事件 | 问题分类、可复现 Case、`regression` 标签、下一轮需求或整改项；必要时形成独立正式回归 Run | 问题已进入明确处理路径；高风险问题已先完成止损 |

模板、交付目录和复核细则见[《06｜交付评估与发布规范》](./06_交付评估与发布规范.md)；六件文档从[《07｜研发交付六件套模板》](./07_研发交付六件套模板.md)复制填写。

## 3. 最小实验怎么做

最小实验每次只验证一个关键假设，例如：

> 加入明确上下文和一个只读工具后，智能体是否能稳定完成目标任务？

执行要求：

1. 从主要场景、关键边界和真实失败中选择 5～20 个探索样本。
2. 固定当前方案和评价方式，避免边看结果边改标准。
3. 优先采用能够运行的最简单 Harness。
4. 一轮只改变一个主要变量，以便判断效果来源。
5. 逐样本记录结果和失败原因，不只汇报平均印象。
6. 根据证据决定继续、调整假设或停止，不因已经投入时间而扩大方案。

实验输出只需回答：验证了什么、用什么样本、结果怎样、主要失败是什么、下一步是否值得做。禁止为了实验先建设完整后台、资产中心、自动评估平台或复杂多 Agent 管理能力。

## 4. 开发检查与正式自测的边界

开发期间应先运行受影响 Case 进行针对性检查，以快速发现 Harness 修改造成的直接问题。针对性检查可以重复、可以只跑子集，但结论不能写成“正式自测通过”“交付通过”或“可以发布”。

进入正式自测前，开发成员必须：

- 完成全部已定义场景和适用输入覆盖单元到 Eval Case 的映射；
- 保证有效 Case 和不同 User Input 达到正式门槛，且未使用低质量样本充数；
- 将 `core`、`boundary`、`safety`、`regression` Case 保存在同一版本化 Eval Set；
- 在运行前冻结 `blocking`/`scored` 门禁、Rubric、聚合规则和强制阈值；
- 冻结候选基线后再完整执行 Eval Set。

若修改任何冻结项，当前自测结果不再代表新的候选版本：应生成新 `baseline_id` 并重新正式自测。只因执行错误或复核需要而重跑同一基线时，保留 `baseline_id`，生成新 `run_id`。

详细数据和判定契约见[《04｜评估、测试与回归规范》](./04_评估测试与回归规范.md)。

## 5. 分级复核怎么执行

交付负责人根据[《00｜智能体研发总纲》](./00_总纲.md)记录低、中或高风险，再选择不低于其最低要求的 R1、R2 或 R3 复核等级，并在报告中说明变更范围和选择理由：

- **R1**：核验文档、数据、数量、覆盖、哈希、追溯关系和原始结果。
- **R2**：完成 R1，并复跑全部 `blocking`、全部 `safety` 及本次变更影响 Case 的并集。
- **R3**：由非开发成员完整重跑冻结 Eval Set，并将复核结果与开发自测结果比对。

复跑必须使用同一 Eval Set、Rubric 和候选基线，产生新的 `run_id`，不得复制或维护第二套事实数据。证据不一致、不可复现或真实性存疑时直接升级为 R3。

## 6. 失败原因路由

失败应按根因回流，不得只修改报告文字或反复重跑碰运气。

| 发现的问题 | 当前结论或动作 | 回到哪里 | 再次前进的条件 |
|---|---|---|---|
| 需求、成功标准或非目标无法判定 | 暂停开发或`材料不完整` | [需求与场景输入](./02_需求定义与场景输入规范.md) | 交付负责人重新确认需求版本和标准 |
| 场景、输入或 Eval Set 未完整覆盖；无法在不降质的前提下达到数量门槛 | 正式自测为`不完整` | [需求与场景输入](./02_需求定义与场景输入规范.md)、[评估与回归](./04_评估测试与回归规范.md) | 补充并完成质量审查，不得用低质量输入充数 |
| `blocking` Case 或强制质量指标失败 | 正式自测为`不通过`，或交付评估`退回整改` | [Harness 开发](./03_Harness开发与变更规范.md) | 定位根因、最小修改、生成新候选基线并全量自测 |
| Trial 为 `error`、瞬时执行故障或证据无法判定，且冻结项未变 | 正式自测为`不完整` | [评估与回归](./04_评估测试与回归规范.md) | 修复瞬时执行或证据链，对同一基线生成新 `run_id` |
| 环境配置、镜像、依赖、资源边界或其他冻结项发生变化 | 原 Baseline 结论失效 | [Harness 开发](./03_Harness开发与变更规范.md) | 生成新 `baseline_id`，并重新完整正式自测 |
| 越权、绕过审批、泄露或其他安全门禁失败 | 立即停止相关动作；自测`不通过`或交付`退回整改` | [安全与控制边界](./05_安全控制边界规范.md) | 修复控制层、形成新基线、重跑安全和完整 Eval Set |
| 交付文件缺失、哈希不一致、结果无法关联 | 交付评估为`材料不完整` | [交付评估与发布](./06_交付评估与发布规范.md) | 补齐真实证据并重新提交；存疑时升级 R3 |
| 复核结果与开发自测不一致 | 不得通过，升级 R3 | [评估与回归](./04_评估测试与回归规范.md) | 查明差异并形成一致、可复现的证据链 |
| 发布对象与评估通过的 `baseline_id` 不一致 | 停止发布；已部署则回滚 | [交付评估与发布](./06_交付评估与发布规范.md) | 恢复通过基线，或将新对象重新走冻结、自测和评估 |
| 生产能力失败 | 建立 `regression` Case | 需求、Harness 或评估规范中对应环节 | 完成新一轮候选基线、自测和交付评估 |
| 生产安全事件 | 先隔离、禁用或回滚，再补安全 Case | [安全与控制边界](./05_安全控制边界规范.md) | 完成止损、根因修复和新基线复核 |

## 7. 何时停止继续设计

满足以下条件时，应停止扩展架构，进入候选基线准备和正式自测，而不是继续增加复杂度：

- 当前主要场景已能完成；
- 关键失败已有明确的处理或安全兜底；
- 新增复杂度无法对应一个已观察到的问题；
- 继续设计不能比一次小实验更快地产生可信证据。

需要真实用户或真实业务流量参与的灰度、小范围开放和只读试用均属于发布策略，只能在候选基线完成全量正式自测且交付评估通过后进行。发布前的额外验证只能在隔离沙箱、回放或影子环境中开展，不得改变真实业务状态。
