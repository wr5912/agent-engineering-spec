# 02｜需求定义与场景输入规范

## 1. 目标

将经确认的任务转化为与任务规模匹配的可执行契约。正式生产交付还要满足开发成员能够实现、正式自测能够检查、交付负责人能够判断的完整契约；需求明确规定上线后的业务结果时，同时定义指标和验证方法。

```text
任务来源、目标与边界 → 输入和前置条件 → 预期观察与安全边界
正式生产交付再扩展为：场景、输入与状态覆盖 → 交付验收标准 → 评估用例与运行证据
```

正式生产路径中，开发成员将正式需求、缺陷单、事故整改、回归问题、迁移事项、法规或合规要求拆分为 `REQ-xxx`，并在同一行填写对应的需求来源；没有现成书面材料时，由交付负责人形成包含任务内容和确认日期的记录。研究探索、已知缺陷修复、外部资产复用或迁移且不发布时，记录任务来源、目标与边界、输入和前置条件、预期观察、安全边界即可，不强制创建 `REQ-xxx`、`AC-xxx`、角色分工或正式评估数据集。任何路径在任务边界或安全边界尚不足以界定时，都不得开始会产生真实副作用的执行；正式生产路径再按第 10 节完成完整确认。

## 2. 责任与填写顺序

| 责任人 | 必须完成的事项 |
|---|---|
| 开发成员（正式生产） | 按需求来源拆分 `REQ-xxx`，填写场景、意图和状态；填写能力适用性；构建输入覆盖矩阵、交付验收标准和评估用例；填写验收阈值和风险依据；需求包含上线后业务结果目标时制定验证计划，并标出估算和缺失数据 |
| 交付负责人（正式生产） | 确认智能体任务、非目标、交付验收标准、风险等级及其依据；适用时确认上线后业务结果验证计划；确认已具备开始开发和后续交付评估的条件 |
| 发布执行人（正式生产） | 不参与修改需求或门槛；仅发布交付评估通过的版本，并按既定发布记录留存真实结果 |
| 研究/修复/适配任务 | 明确实际执行人和决策人；按问题需要记录场景、能力或输入，不为形式完整性创建空字段 |

正式生产路径由开发成员根据已有授权材料完成初始记录、由交付负责人确认任务与门槛；其他路径由执行人和当前决策人确认最小记录。任何影响当前判断的字段缺失时，应在执行前补齐，而不是在测试阶段猜测。正式生产路径确需改变任务边界、硬门禁或适用的上线后业务结果目标时，按需求变更处理并建立新的候选基线。

### 2.1 五项信息不要混用

同一记录中保持以下语义独立，字段不必拆成五份文件：

| 信息 | 回答的问题 | 只读状态摘要示例 |
|---|---|---|
| 需求来源 | 为什么做 | 研究已有只读查询能否支撑资产状态摘要 |
| 智能体任务 | 要完成什么 | 根据指定资产及查询返回，区分管理面在线与服务运行状态，指出缺失信息，不执行修复 |
| 测试数据与前置条件 | 用什么输入、在什么条件下做 | 合成输入：资产 A 的管理面在线，服务状态和报告链接缺失；未连接真实服务 |
| 评价方法 | 如何检查 | 检查摘要是否保留“服务状态未知”、未编造报告链接且没有写调用 |
| 判断 | 观察意味着什么 | 上述检查满足时，只说明本地任务语义符合预期；真实接口和目标运行时仍为未知 |

这是说明性示例，不是已执行证据；局部可运行检查及其真实接入边界见[《08｜最小实践示例》第 9 节](./08_最小实践示例_告警研判与处置.md#9-外部只读资产适配的最小示例说明性)。

## 3. 正式生产交付契约

本节及第 6–9 节是正式生产路径的完整模板。非生产任务只保留能够回答当前问题的任务目标、输入/前置条件、安全边界、观察和决策；可按需借用场景、状态或输入变体，不强制 `REQ`/`AC`、覆盖矩阵、正式数据集、基线或 `run_id`。

### 3.1 交付契约

| 内容 | 适用性 | 形成的记录 |
|---|---|---|
| 智能体任务与非目标 | 正式生产路径必填；其他路径记录目标与边界 | 一个或多个稳定的 `REQ-xxx`；其他路径使用最小任务记录 |
| 交付验收标准 | 正式生产路径必填 | 一个或多个稳定的 `AC-xxx`，记录项目特有的验收要求、判定作用、阈值及依据 |
| 评估用例与正式评估运行 | 正式自测前必备 | `cases.jsonl`、`results.csv` 和必要的 `evidence_ref` |
| 上线后业务结果目标 | 需求包含此类目标时填写 | 指标、目标值、数据源、观察窗和负责人 |

上线后业务结果目标不要求全部转化为离线评估用例。例如，“告警平均研判时间下降”需要发布后的真实数据；离线运行只能证明智能体在受控任务中达到预先定义的交付门槛。安全边界、最低可接受任务结果和可复现性必须在发布前验证，不得留待发布后观察。

### 3.2 `REQ-xxx` 最小结构（正式生产路径）

每项智能体任务使用稳定的 `REQ-xxx` 标识，并与对应验收项和适用的上线后业务结果目标关联。每个 `REQ-xxx` 至少关联一个用于定义最低可接受任务结果的硬门禁验收项；期望达到的更高水平另设质量目标，不得用质量目标代替最低门禁。

| 字段 | 必填内容 | 确认责任人及确认时点 | 来源或缺失处理 |
|---|---|---|---|
| 需求编号 | 稳定编号，例如 `REQ-001` | 开发成员创建，交付负责人在探索验证或开发前确认 | 一旦被评估用例引用，不因文字润色改变 |
| 需求来源 | 对应的需求条款、缺陷、事故、回归问题、迁移指令、法规或合规要求，或者确认记录引用 | 开发成员填写，交付负责人确认 | 填写可访问的具体引用；没有书面材料时，填写交付负责人的确认记录和日期 |
| 用户 | 目标使用者 | 交付负责人在探索验证或开发前确认 | 来自需求来源；来源未写明时，由交付负责人确认并记录日期 |
| 触发场景 | 使用条件与业务上下文 | 开发成员填写，交付负责人确认 | 来自实际流程或已确认的全新场景 |
| 目标任务 | 智能体必须完成的任务结果 | 交付负责人确认 | 描述用户任务结果，不得表述为提示词、工具或框架清单 |
| 输入 | 完成任务所需的最小信息 | 开发成员填写 | 缺失信息的处理应进入边界评估用例 |
| 输出或动作 | 面向用户的交付结果或系统状态变化 | 开发成员填写，交付负责人确认 | 动作和最终业务状态必须可观察 |
| 非目标 | 当前阶段不解决的范围或禁止行为 | 交付负责人确认 | 不得将硬失败事后改写为非目标 |

风险等级针对本次交付的完整智能体确定，不在每个 `REQ-xxx` 中分别填写。开发成员综合全部需求和适用能力提出等级，交付负责人按可能造成的最高后果确认；模板一“风险依据”应列明决定该等级的需求编号、能力以及数据敏感度、后果、自治程度、作用规模和可逆性。依据不足时，应先按可能的较高后果采取控制并补充调查。

目标任务必须能够转成场景、`expected_behavior`、确定性检查或候选基线冻结前确定的评分规则。仅使用“效果好”“更智能”“提升效率”等定性表述，不构成智能体任务或交付验收标准。

### 3.3 量化字段的填写规则（按需）

只在模板字段或验收计算需要量化信息时填写：

| 情形 | 填写方式 | 推进规则 |
|---|---|---|
| 已有实际记录或能够按明确方法完成测量 | `实测`，并填写数据源、样本范围和观察窗 | 可以作为任务、风险、验收或结果判断的依据 |
| 当前只能使用有限样本、专家判断或近似计算，且相应字段允许估算 | `估算`，并填写方法、依据、人员和日期 | 不得将估算写成实测；需要实测才能判定的硬门禁仍不得据此通过 |
| 正式生产路径中任务边界、风险等级或 `AC-xxx` 阈值所需数据尚未取得 | `暂无可靠数据`，并填写取得方法、负责人和检查时间 | 正式生产路径不得进入开发，先补齐相应定义和确认；其他路径仅在该数据是安全或当前决策的前置条件时补齐 |
| 上线后业务结果的实际值尚未产生 | 不填写发布后实际值；在候选基线冻结前填写数据源、计算口径、观察窗和负责人 | 不阻止开发；实际结果在发布后按观察窗填写 |
| 模板未要求，或当前任务不使用该量化信息 | 省略，不建立测量计划 | 不新增字段或证据工作 |

`N/A` 只表示某项在当前业务中概念上不适用，不得用于表示应当取得但暂时缺失的数据。涉及个人、客户或敏感业务数据时，证据可以脱敏或受控外置，只记录可追溯引用，不得将凭据和原始敏感内容复制进交付文档。生产授权明确要求的量化证据必须在发布前取得。

### 3.4 交付验收标准填写要求（正式生产路径）

在项目交付记录中，《智能体需求定义》（模板一）是智能体任务、非目标和项目特有交付验收标准的唯一事实源；本节规定其填写方法。开发成员将每项 `REQ-xxx` 转化为一个或多个使用稳定 `AC-xxx` 编号的验收项，其中至少一个硬门禁验收项定义该任务的最低可接受结果。直接开发时，交付负责人在开发前确认；开展探索验证时，先确认探索判定标准和不可降低的安全红线，再根据探索结果更新验收项，并在进入开发前完成确认。

每个验收项至少记录：

| 字段 | 填写要求 |
|---|---|
| 验收编号 | 稳定编号，例如 `AC-001`；被评估实现引用后不因文字润色改变 |
| 关联需求 | 填写一个或多个 `REQ-xxx` |
| 维度 | 任务质量、安全、稳定性、可靠性、时延、成本等实际适用项 |
| 可观察的验收要求 | 写明必须得到或禁止出现的输出、动作、控制结果或最终业务状态 |
| 判定作用 | `硬门禁`、`质量目标`或`不可退化护栏`；同一指标承担不同作用时分别建立验收项 |
| 阈值或允许结果 | 可判定的绝对数值、范围、必须发生或不得发生的结果 |
| 阈值依据或规范条款 | 不可接受的业务后果、安全红线、用户可接受水平、资源预算、可比对照或探索结果 |

只记录实际适用的项目特有验收项，不为不适用维度建立占位行。证据完整性条件、阻断用例通过率、安全违规数和未解决执行错误等统一规则，直接适用《04｜评估、测试与回归规范》，不在项目验收表中重复抄录。项目要求高于统一规则时，以独立 `AC-xxx` 记录；低于统一规则的项目要求无效。

模板三仅通过 `AC-xxx` 记录评估用例、评价方法、数据来源、结果字段、执行试次和聚合规则，不得另行设定或修改验收要求及阈值。正式评估运行结论直接执行《04｜评估、测试与回归规范》第 11 节。

阈值依次依据不可接受的业务或安全后果、当前实测或可比旧运行、用户可接受水平、资源预算以及适用的探索结果确定。本规范不统一规定项目的准确率、时延、成本或执行试次数。探索结果需要改变验收项时，应由交付负责人在开发前重新确认；针对性检查和正式评估运行结果不得用于事后降低标准。

“通过”“质量目标达成”和“上线后业务结果达成”的区别，以及正式评估运行结论（`通过`、`不通过`或`不完整`）的判定方法，见[《00｜智能体研发总纲》](./00_总纲.md)和[《04｜评估、测试与回归规范》](./04_评估测试与回归规范.md)。

### 3.5 上线后业务结果目标（需求包含且进入正式生产路径时填写）

需求将上线后的处理时长、任务完成率、人工返工率、单位成本、重大风险事件或其他业务结果纳入目标时，由开发成员填写，交付负责人确认。需求只描述离线能力或版本质量时，相应要求应写入发布前交付验收标准或质量目标，不建立上线后业务结果目标。

进入探索验证或开发前至少明确指标或可观察行为及目标方向；候选基线冻结前补齐目标值、数据源与计算口径、观察窗、未达目标后的处理方式和负责人。目标是否达成在发布后判定，不作为发布前通过条件。需求不包含此类目标时，从项目文档中删除本节及其表格。

| 字段 | 填写要求 | 无数据或不适用时的处理规则 |
|---|---|---|
| 业务指标或可观察行为 | 直接反映需求规定的上线后结果 | 适用时至少有一个可验证目标，不得以模型分数代替 |
| 当前值与证据状态 | 目标要求相对当前值变化时必填 | 无可靠当前值时不得作出相对变化结论；采用绝对目标时可以省略 |
| 目标值或达成阈值 | 写明绝对目标、相对变化幅度或不得发生的事件 | 无法形成可判定阈值时，不得记为已达成 |
| 数据源与计算口径 | 记录查询、报表、抽样或反馈的来源和计算方法 | 尚不可用时记录负责人、接入步骤和最晚检查时点 |
| 观察窗与负责人 | 记录开始条件、观察周期和结果确认人 | 观察周期按实际业务周期确定 |
| 未达目标后的处理方式 | 记录继续观察、分析原因、调整需求或进入下一轮整改等动作 | 只有目标本身要求停止、限制范围或回滚时，才同时记录相应阈值和执行人 |

业务指标只保留能够直接反映需求且实际可取得的最小集合。缺陷修复、回归整改、按令迁移或单纯能力交付通常只需发布前验收；需求没有规定上线后的业务结果变化时，不增加此类目标。

## 4. 能力适用性（按需；正式生产路径完整记录）

正式生产路径在直接开发前将下表每项确认为`适用`或`不适用`并记录理由。其他路径只检查与当前问题有关的能力；开展探索验证时，受待验证问题直接影响的能力可暂记为`待验证（问题）`。涉及外部 Skill、工具、MCP 或数据资产时，按[《10｜外部资产复用与集成》](./10_外部资产复用与集成.md)区分已安装、已加载、可调用、已适配和有效，不能以连接成功代替目标行为验证。

下表提到的正式评估用例、冻结和发布后监控只适用于正式生产路径；其他路径使用能回答当前问题的最小检查和观察。

| 能力 | 判断问题 | 适用时的最小后续动作 |
|---|---|---|
| 工具或外部调用 | 智能体是否查询系统、调用模型上下文协议（MCP，用于连接工具和数据源的标准协议）、命令行环境（Shell）或外部应用程序接口（API） | 记录允许的工具、数据与参数边界，并建立成功、失败和误调用评估用例 |
| 写操作或真实动作 | 智能体是否改变工单、账户、主机、策略或其他业务状态 | 定义审批、幂等（同一业务请求重复执行时至多产生一次预期影响）、规模、回滚和最终状态检查 |
| 多轮状态 | 后续轮次是否依赖前序消息、任务进度或业务状态 | 定义会话（Session）作用域、初始状态、恢复，并检查不同用户或会话的状态是否相互污染 |
| 长期记忆 | 是否跨轮次或跨会话（Session）读取、写入长期信息 | 定义作用域、来源、过期、纠正、删除、污染和隔离要求 |
| 检索增强生成（RAG） | 是否从检索语料或索引生成结论 | 记录语料/索引版本和来源时效，并检查任务所需资料是否被检索到、回答是否得到检索证据支持 |
| 多智能体 | 是否存在委派、子智能体或共享状态 | 定义委派输入输出、权限、失败传播、取消和执行预算 |
| 路由或降级 | 是否按条件切换模型、智能体、工具或降级路径 | 冻结实际规则并为每条启用分支建立评估用例；未启用降级路径时无须增加相关资产 |
| 外部网络 | 候选系统是否访问公网、第三方服务或不可信内容 | 定义目标范围、出站控制、凭据与间接注入评估用例 |
| 在线服务 | 是否面向真实请求持续提供服务 | 定义端到端协议、并发/超时等适用门槛，以及发布后监控和停用条件 |

适用项的详细实现、测试和安全要求分别写入需求、智能体运行支撑配置、评估数据和安全清单。不适用项记录理由即可；不得因形式完整性要求引入长期记忆、检索增强生成、多智能体或降级路径。

## 5. 场景、意图与状态（正式生产路径完整建模）

场景定义为：

> **某类用户 + 某种业务上下文 + 某种业务目标。**

每个场景使用稳定的 `Sxx` 编号，并至少定义一个意图 `Ixx`。同一上下文中存在多个不同用户目标时，分别定义 `I01`、`I02`；不得将不同表达方式误判为不同任务。

定义场景时，优先纳入需求明确的主要场景、真实高频场景、高后果场景和历史失败场景；窄域任务只有一个真实场景时仅定义一个。口语、简写、信息缺失等属于输入变体；权限、审批、工具状态、多轮进度、长期记忆初始内容和外部依赖状态属于评估用例上下文。只有用户、业务上下文或业务目标发生实质变化时才新增场景。

每个场景至少说明：

- 关联需求 ID；
- 用户与业务目标；
- 进入场景所需的最小信息和适用的初始状态；
- 预期输出、动作或最终业务状态；
- 主要失败和风险；
- 是否适用各类输入变体及条件能力。

同一评估用例只能归属一个 `scenario_id + intent_id`，但可以关联多个 `requirement_ids` 和多个 `variant_types`。需要验证不同场景、意图、权限或状态结果时，分别建立可独立判定的评估用例；不得使用同一评估用例同时代表多个场景或意图。

## 6. 用户输入与覆盖（正式生产路径完整建模）

### 6.1 真实分布证据与设计覆盖的区分（正式生产路径）

| 类型 | 作用 | 优先来源 | 必须记录 |
|---|---|---|---|
| 真实分布证据 | 说明用户的实际表达方式、高频任务及抽样范围 | 经授权的生产输入、脱敏日志、工单、客服记录、用户观察 | 来源、样本范围、观察窗、脱敏方式；无法取得时记录`暂无可靠数据`和收集计划 |
| 人为设计覆盖 | 主动验证低频但关键的边界、故障、安全和状态组合 | 真实失败改编、业务专家设计、逐条复核的合成输入 | 设计目的、关联风险、复核人和依据；不得将其频率视为生产分布 |

正式评估数据集应同时利用可取得的真实分布证据和必要的设计覆盖。主要能力样本应反映高频任务分布；越权、审批绕过、跨租户、注入、不可逆动作等安全长尾不得因线上占比低而省略，也不得被总体平均分稀释。

### 6.2 输入变体

按场景实际适用性检查以下变体。枚举值与《04｜评估、测试与回归规范》一致：

| 枚举值 | 含义 | 示例 |
|---|---|---|
| `standard` | 信息完整、表达标准 | “分析这条告警” |
| `colloquial` | 口语表达 | “这个报警咋回事？” |
| `elliptical` | 简写或省略 | “看看昨天财务那台机器” |
| `multi_intent` | 同时包含多个目标 | “分析一下，顺便看看要不要封” |
| `incorrect_or_noisy` | 含错误、歧义或噪声 | 时间、IP 地址（用于标识网络对象）或名称不准确 |
| `missing_info` | 缺少完成任务所需信息 | 只提供告警 ID，但系统无法查询 |
| `high_risk` | 高风险或越权要求 | “别走审批，直接把机器隔离掉” |

每个场景仅需覆盖实际适用的输入类型。不适用时填写 `N/A` 并说明业务原因；证据尚未取得时填写`暂无可靠数据`，不得使用 `N/A`。不适用说明不得规避应测试的安全或故障内容。

### 6.3 输入覆盖矩阵（正式生产路径）

使用一张矩阵建立追溯关系：

```text
需求编号 × 场景编号 × 意图编号 × 输入变体类型 → 评估用例编号或不适用原因
```

| 需求 ID | 场景 ID | 意图 ID | `standard` | `colloquial` | `elliptical` | `multi_intent` | `incorrect_or_noisy` | `missing_info` | `high_risk` |
|---|---|---|---|---|---|---|---|---|---|
| REQ-001 | S01 | I01 | case-001 | case-002 | case-003 | case-004 | case-005 | case-006 | N/A：任务不接触受限数据且不产生动作或高后果建议 |
| REQ-002 | S02 | I01 | case-007 | case-008 | case-009 | case-010 | case-011 | case-012 | case-013 |

矩阵必须满足：

- 所有已定义需求场景均进入矩阵；
- 每个适用格至少关联一个有效评估用例；
- 一个格可以关联多个评估用例，以覆盖必要的状态、权限或风险差异；
- 同一评估用例可以关联多个需求和输入变体；对每个关联需求与输入变体，该用例编号都必须出现在与其 `scenario_id + intent_id` 一致的矩阵行和相应变体列中；
- 用例无须按场景平均分配，应优先覆盖真实高频、高风险和历史失败位置；
- 《04｜评估、测试与回归规范》第 4.1 节一般门槛适用时，纳入的已定义需求场景与适用矩阵格覆盖率均为 100%；该规范第 4.2 节明确范围适用时，本次新增或受影响的需求场景与适用矩阵格覆盖率均为 100%。

## 7. 输入质量要求（正式生产路径的规模与覆盖）

不同用户输入必须代表真实使用方式或有意义的理解挑战。以下内容不得计入新增输入数量：

- 仅修改标点、空格、称呼或语气词；
- 仅替换编号、IP 地址、日期、对象名等值；
- 机械替换同义词而未改变理解难点；
- 模型批量生成后未经具备相应业务知识的人员逐条复核；
- 不符合实际使用方式的虚构输入。

开发成员应优先收集真实输入及脱敏输入，再补充基于真实模式改编或逐条完成输入质量复核的合成输入。输入质量复核可由开发成员本人或其组织的、具备相应业务知识的人员逐条执行；开发成员负责记录复核人、日期、依据和剔除原因，交付负责人在交付评估中核验。输入来源统一填写《04｜评估、测试与回归规范》定义的 `source_type` 字段。

正式生产路径的评估规模和有限范围适用条件直接执行[《04｜评估、测试与回归规范》](./04_评估测试与回归规范.md)第 4 节，并在模板三记录实际采用的规则及依据。缩小范围必须具有该节规定的适用前提和范围限定依据；重复改写或低质量输入不得计入数量。其他路径只选择足以回答当前问题的样本，不设置统一数量门槛。

## 8. 修改前评估与版本对照（正式生产路径）

迭代项目引用修改前评估时，开发成员必须填写既有 `baseline_id + run_id` 并确认原始结果可访问；单一汇总分数不构成可追溯的对照证据。

1. 既有运行只有逐项满足《04｜评估、测试与回归规范》第 13 节规定的可比性条件时，才能直接作为版本对照；
2. 任一可比性条件不满足时，按当前口径重跑修改前实现，或将旧结果明确标记为不可直接比较的历史参考；
3. 只有修改前版本确实代表当前业务实际使用方式，且样本范围和观察窗一致时，该运行结果才能同时作为当前线上表现的事实依据；
4. 修改前评估继续引用其原有候选基线编号和运行编号；
5. 无可比旧运行时，对照编号填 `N/A（无可比对照）`，仍可验证绝对门槛；需要形成当前业务表现或相对改进结论时，按第 3.3 节记录相应量化字段。没有可比证据时不得形成相对提升结论。

上述条件用于避免将实验环境中的旧版本分数表述为当前人工流程或线上业务表现。

## 9. 需求冻结与变更（正式生产路径）

以下情况属于需求变化：

- 新增、删除或改变智能体任务，或适用的上线后业务结果目标；
- 用户群体或业务上下文变化；
- 输出责任边界变化；
- 智能体由“提供建议”变更为“自动执行”；
- 新增工具、长期记忆、检索增强生成、多智能体、路由或降级路径、外部网络、在线服务或高风险权限，导致适用性变化；
- 交付验收标准、强制阈值、风险依据或非目标变化。

需求变化后必须：

1. 由交付负责人重新确认智能体任务、非目标、交付验收标准和风险等级；需求包含上线后业务结果目标时，同时重新确认该目标；
2. 更新需求版本、能力适用性、场景、输入与状态覆盖、安全边界和关联评估用例；
3. 按《03｜智能体运行支撑配置开发与变更规范》生成新的候选基线；
4. 按《04｜评估、测试与回归规范》重新完成正式自测。

仅修正错别字且不改变含义、关联关系和判定标准时，可以保留需求版本，但应留下变更记录。发布后取得的新现状或实际业务结果不得追溯修改已经冻结的旧交付记录；应当写入现有发布记录，确需调整任务或门槛时再进入下一轮需求。

## 10. 路径选择与进入条件

### 10.1 共同条件

任何路径开展探索、实现或适配前，必须具备任务来源、目标与边界、输入/前置条件和安全边界。正式生产路径还必须具备以下完整条件：

- 每个 `REQ-xxx` 的需求来源可核验；
- 已定义足以界定当前工作范围的 `REQ-xxx`，包括用户、使用场景、目标任务、输入、输出或动作及非目标；
- 已确认初步风险等级，中、高风险能力的审批、阻断或禁止原则明确；
- 需求包含上线后业务结果目标时，已记录指标或可观察行为及目标方向；
- 使用估算时，已记录方法和依据；缺失信息不影响当前工作范围和安全控制。

正式生产路径此时不要求评估数据集达到正式评估规模。开发成员可在实现和针对性检查中完善评估用例，但必须在候选基线冻结前满足适用的规模、质量、覆盖和冻结要求；其他路径在形成足够观察和决策证据后即可结束。

### 10.2 直接开发

正式生产路径同时满足以下条件时，直接进入开发；其他路径只需确认当前问题的实施路线和必要控制：

- 交付负责人已确认任务、非目标、风险等级和全部项目特有 `AC-xxx`，所有硬门禁均有可判定的阈值或允许结果；
- 能力适用性已全部收敛为`适用`或`不适用`；
- 实施路线、影响范围和所需控制已经明确，不存在会改变实施路线的关键不确定性。

新增权限、工具或架构变化不自动要求探索验证；其控制、测试和复核要求按实际风险确定。

### 10.3 探索验证

只有存在会改变实施路线的关键不确定性时，才开展探索验证。正式生产路径实验前由交付负责人确认待验证问题、预期结果、停止条件和不可降低的安全红线；其他路径由执行人和决策人确认同样的最小边界。受该问题直接影响的能力适用性可标记为`待验证`，相关 `AC-xxx` 可在探索结束后完成。

探索结束后，正式生产路径必须在进入开发前完成第 10.2 节的全部确认。其他路径根据探索结论记录采用、继续验证、不采用或未知；若随后要发布，再转入正式生产路径。模板三中的评估实现可以在开发过程中完善，但不得改变模板一已确认的验收要求、判定作用和阈值。
