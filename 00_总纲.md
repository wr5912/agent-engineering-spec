# 00｜智能体研发总纲

## 1. 目的与适用边界

本规范用于统一智能体任务从定义、实现、验证到（适用时）生产交付的工作方式。先按任务类型选择所需记录深度；正式生产交付再进入完整的基线、评估、复核和发布流程。所有路径都应：

- 每项任务的来源、目标与边界明确且可追溯；正式生产交付另确认交付验收标准；
- 实现范围和技术复杂度与任务及风险相匹配；
- 主要场景、输入方式、关键状态和安全边界按任务范围得到验证；
- 与当前任务相称的检查、结论和证据可复核；正式生产交付另形成正式自测、交付评估和发布结论；
- 实现或被测资产能够按任务需要定位；正式生产交付还需可复现、审计和回滚；
- 运行异常、用户反馈和运行漂移（实际模型、工具、知识源或策略等状态偏离已通过的候选基线）能够进入回归或后续验证；
- 需求包含上线后业务结果目标时，能够按预定指标和观察窗验证实际结果。

本规范不限定 DSH、AgentScope、Claude Code、Codex、LangGraph 等具体框架，也不规定提示词或智能体编排的唯一写法。

## 2. 基本原则

### 2.1 先定义任务与验收

所有任务先形成以下最小任务契约：

```text
任务目标与边界（含来源）
+ 输入与前置条件 + 期望观察或结果 + 安全边界
```

研究、已知缺陷修复、外部资产复用和迁移且不产生生产发布的任务，可以使用问题/工单/来源标题、输入与前置条件、预期观察和决策记录，不强制创建完整 `REQ-xxx`、`AC-xxx`、三角色分工或六件套。正式生产交付必须依据经确认的需求、缺陷单、回归问题、迁移指令、法规或合规要求，定义任务、非目标、交付验收标准和安全边界；具体字段以[《02｜需求定义与场景输入规范》](./02_需求定义与场景输入规范.md)为准。

需求另行规定上线后业务结果目标时，按《02｜需求定义与场景输入规范》第 3.5 节定义并在发布后验证。该目标不替代发布前交付验收标准。

提示词（Prompt）、上下文（Context）、技能（Skill）、工具（Tool）、模型上下文协议（Model Context Protocol，MCP）工具、智能体编排、钩子（Hook）、工作流（Workflow）、插件（Plugin）、长期记忆（Memory）、模型路由和运行时（Runtime）配置统称为**智能体运行支撑配置（Harness）**。智能体运行支撑配置可以持续优化，但每次变化都必须接受与影响范围相匹配的检查；正式生产路径中影响冻结项的变化必须形成新的候选基线。

### 2.2 按关键不确定性决定是否开展探索验证

任务、预期结果、实施路线和影响范围已经明确，且不存在会改变实施路线的关键不确定性时，开发成员直接进入实现与针对性检查；正式生产路径还需确认交付验收标准。常规功能开发、已知缺陷修复、回归整改和按令迁移均可采用这种方式；缺陷应先复现，迁移应先核对前置条件和预期状态。

探索验证的启动条件是存在会改变实施路线的关键不确定性。对于复杂编排、长期记忆、多智能体、自进化机制、管理平台、新权限或新安全边界，仅在其必要性、可行性、实现方式或控制效果尚不能确定时开展探索验证；实验前写明待验证问题、预期结果和停止条件，样本数量按问题和差异覆盖选择，不设统一配额。

```text
单次模型调用
→ 模型 + 明确上下文
→ 模型 + 工具
→ 技能 / 工作流
→ 多智能体
→ 长期记忆 / 自进化
```

实现方案按上图由低到高选择能够满足需求的最低复杂度。现有需求与技术事实足以确定所需层级时直接实施；需要增加复杂度时，应有已确认任务、已观察失败或探索结果作为依据。探索样本用于回答当前问题，不自动形成正式自测、交付评估或发布结论。

### 2.3 操作性优先，条件能力按需启用

项目记录必须明确执行时点、责任人、执行方式、证据位置，以及数据缺失或能力不适用时的处理规则。仅提供概念、评分口号或平台名称而未形成可执行要求的，视为定义不完整。

工具调用、写操作、多轮状态、长期记忆、检索增强生成（RAG，先检索资料再据此生成回答）、多智能体、路由或降级、外部网络和在线服务等能力先做适用性判断。适用时才补相应设计、控制和评估用例；不适用时记录理由，不创建空配置、空报告或占位系统。版本控制、Markdown 文档、逐行 JSON 格式（JSONL，每行一个独立 JSON 对象）、逗号分隔值文件（CSV）、简单脚本和必要运行证据足以形成完整记录时，不预建平台。

### 2.4 用证据判断完成度

是否完成当前任务，应依据目标、输入与前置条件、目标行为、安全边界和足以支持结论的证据判断。正式生产交付还需满足场景覆盖、材料完整性和基线可复现性。主观可用性判断、单次成功演示或实现代码人工检查不得被表述为超出其范围的证据。

### 2.5 完整覆盖已定义范围

规范不要求穷举所有可能的输入表达，也不得形成“全部可能输入均已覆盖”的结论。正式评估数据集（Eval Set）必须覆盖全部已定义需求场景、适用的输入覆盖单元和高风险边界，并如实记录未覆盖项与剩余风险；研究、修复、适配和迁移任务只需覆盖足以回答当前问题及其安全边界的输入。

正式生产评估规模应与新建智能体或本次变更实际影响的行为相匹配。新建开放式智能体，新增或扩大任务、能力、动作、权限或安全边界，修改共享运行路径，或者影响范围无法封闭时，执行[《04｜评估、测试与回归规范》](./04_评估测试与回归规范.md)第 4.1 节的一般门槛；确定性新建或变更以及可证明隔离的局部维护变更，才可按该文件第 4.2 节评估。非生产任务不套用正式规模，但不能省略与结论有关的边界和安全检查。

### 2.6 将真实失败纳入回归

运行中的能力失败和安全偏差必须完成分类；用户反馈要求新增或改变智能体任务、交付验收标准或风险等级时，按任务变更处理。能够复现且值得长期保留的问题转为评估用例并标记为 `regression`；一次性研究或局部修复无需为此创建重复数据集。

### 2.7 按任务选择路径

| 路径 | 适用问题 | 最小产出 | 何时进入正式生产路径 |
|---|---|---|---|
| 研究探索 | 方案、能力或控制效果未知 | 问题、实验条件、观察、限制、决策 | 研究结果要进入生产交付时 |
| 已知缺陷修复 | 现象和预期已知 | 复现、根因、最小修复、受影响检查 | 修复版本要发布时 |
| 外部资产复用 | 采用外部 Skill、工具、MCP 或数据 | 来源/版本/范围、适配差异、目标行为验证、采用决策 | 资产进入将发布的 Harness 时 |
| 迁移 | 框架、模型、运行时或配置替换 | 前置条件、差异、迁移结果、回退条件 | 迁移结果要上线时 |
| 正式生产交付 | 需要发布并承担持续运行责任 | 完整需求、基线、正式评估、复核、发布与观察 | 直接适用 |

没有发布的路径可以在结论形成后结束；实际写操作、敏感数据和外部副作用仍必须遵守[《05｜安全与控制边界规范》](./05_安全控制边界规范.md)。

## 3. 权威术语

正文优先使用中文。专业英文缩写确需保留时，应在本文集首次出现处使用“中文含义（英文全称或缩写，并说明用途）”的形式解释；代码字段、文件名和命令保持原样。

| 术语 | 定义 |
|---|---|
| 智能体（Agent） | 面向已定义用户任务提供输出或执行动作的交付对象 |
| 需求来源 | `REQ-xxx` 所对应的正式需求、工单、事故记录、回归问题、迁移指令、法规或合规要求，或者交付负责人确认记录的具体引用 |
| 上线后业务结果目标 | 需求对智能体上线后实际结果的要求，例如处理时长、任务完成率、人工返工率、单位成本或重大风险事件；在发布后按预定数据源和观察窗验证 |
| 智能体运行支撑配置（Harness） | 驱动智能体行为的提示词、上下文、技能、工具、智能体编排及相关运行配置的总称 |
| 智能体任务与边界 | 智能体必须提供的任务能力、所需输入、输出或动作，以及明确的非目标和禁止行为 |
| 交付验收标准 | 开发前由交付负责人确认的项目特有发布前要求，以 `AC-xxx` 标识并记录可观察结果、判定作用、阈值或允许结果及其依据 |
| 硬门禁 | 候选基线必须满足的最低要求；任一适用硬门禁确认失败时，正式自测判定为`不通过` |
| 不可退化护栏 | 限制非主要优化目标可以接受的退化范围；正式评估运行已通过时，越界使质量目标结论为未达成；需要阻止交付时应另设硬门禁 |
| 证据完整性条件 | 判断用例数量、执行结果、数据来源和追溯关系是否足以得出结论的条件；不作为质量得分 |
| 用户输入（User Input） | 一次真实或高度接近真实的用户输入文本；不同输入必须具有实质性的表达、信息或任务差异 |
| 需求、任务、测试数据、评价与判断 | 需求说明为什么做；任务说明要完成什么；测试数据及前置条件说明用什么输入和环境执行；评价方法说明如何判断；验收或研究判断说明结果意味着什么；五者保持独立，分别引用同一维护源 |
| 开发者运行配置 | 开发者本地或协作环境中的配置、依赖和入口，不等于目标 Harness 资产 |
| 目标 Harness 资产 | 实际供目标运行时加载的提示词、Skill、工具、编排和配置；外部资产采用规则见[《10｜外部资产复用与集成》](./10_外部资产复用与集成.md) |
| 运行状态与记录 | 安装/加载、调用、任务适配、执行结果、错误和审计记录；不应反向充当需求或评价标准 |
| 参考/控制版本 | 用于比较的已知版本或既有运行，必须与被测版本和本次执行区分 |
| 被测版本 | 本次待验证的 Harness、模型、运行时和相关依赖组合 |
| 运行（Run） | 一次实际执行及其输入、前置条件、观察和结果；正式运行另使用 `run_id` |
| 已安装/已加载/可调用/已适配/有效 | 五种不同状态：存在于环境、被目标运行时加载、接口可用、符合当前任务规则、在目标行为上达到预期；前一状态不能推出后一状态 |
| 评估用例（Eval Case） | 可独立执行和判定的测试单元，包含输入、上下文、期望、评价方式、门禁及标签 |
| 评估数据集（Eval Set） | 绑定版本的一组评估用例；`core`、`boundary`、`safety`、`regression` 是同一数据集中的标签，不另建重复数据集 |
| 评分规则（Rubric） | 在运行前确定的评价维度、等级或分值规则，用于一致判定不能完全由程序规则判断的输出 |
| 执行试次（Trial） | 某个候选基线执行某个评估用例的一次尝试；重复试次不增加用例或不同输入的数量 |
| 模型上下文协议（MCP） | Model Context Protocol 的缩写，用于按统一协议连接模型、工具和数据源；具体工具仍须单独限制权限和参数 |
| 检索增强生成（RAG） | Retrieval-Augmented Generation 的缩写，表示先检索资料，再依据检索结果生成回答 |
| 运行时（Runtime） | 实际装配并调度智能体、模型、工具和控制策略的软件框架或服务 |
| 执行环境 | 运行候选基线所使用的操作系统、容器、依赖和必要配置 |
| 不适用（`N/A`） | 某项在当前交付中概念上不成立；必须说明原因，不得用于掩盖应有但尚未取得的数据 |
| 运行漂移 | 实际模型、工具、知识源、策略、路由或环境偏离已通过候选基线的情形 |
| 结果事实源 | 唯一用于形成正式评估运行结论的 `results.csv` 或受控结果记录；不得并行维护相互冲突的结果副本 |
| 不可变结果快照 | 结论形成后内容不再变化，且能够通过 Git 提交或外置对象版本唯一定位的结果副本 |
| 不可变定位信息 | 仓库内文件的路径与 Git 提交，或者外置文件的受控统一资源标识符、对象版本和 SHA-256 内容摘要；用于取得并核对本次实际采用的内容 |
| 针对性检查 | 开发期间对受影响用例子集的快速检查，仅用于反馈，不得形成正式评估运行结论 |
| 候选基线 | 开发成员在首次正式自测前冻结的需求与验收、智能体及其运行支撑配置、模型、运行时、安全策略、工具、评估数据集、正式评估范围、评估实现、评分与执行试次聚合规则以及执行环境组合，以 `bl-<UUIDv4>` 格式的 `baseline_id` 唯一标识；UUIDv4 是随机生成的通用唯一标识符第 4 版 |
| 正式自测 | 开发成员在候选基线上完整执行本次冻结的正式评估范围，并形成机器结果和自测结论 |
| 评估执行编号（`run_id`） | 标识一次写入结果事实源的独立评估执行；执行人在开始前生成 `run-<UUIDv4>`，重跑不得复用或覆盖旧结果；编号本身不表示执行范围或结论 |
| 正式评估运行（Run） | 对一个候选基线完整执行一次冻结的正式评估范围及其全部执行试次；使用唯一 `run_id`，并可形成`通过`、`不通过`或`不完整`结论；R2 范围复核虽使用 `run_id`，但不属于正式评估运行 |
| 交付评估 | 交付负责人根据六项交付内容、机器证据和必要复跑，对整体交付完成度作出的独立结论 |
| 发布 | 将交付评估通过的同一候选基线部署到目标环境，并记录结果和回滚点 |
| 回归 | 使用既有评估用例和真实失败沉淀的 `regression` 用例，检查后续变更是否造成能力或安全退化 |

本规范中的“必须”“应当”“可以”“禁止”分别表示强制要求、默认要求、可选实践和不可接受行为。“应”与“应当”等价，“不得”与“禁止”等价；“无须”表示本规范不作强制要求，但项目可以基于实际需求采用。

## 4. 角色与责任

正式生产交付使用以下三个责任角色。同一人员可以兼任，但每项责任和结论必须按角色分别记录。研究、缺陷修复、复用和迁移任务只需明确实际需要的执行人和决策人；一旦进入生产发布，再补齐正式角色责任。

| 角色 | 核心责任 | 不得替代的责任 |
|---|---|---|
| 开发成员 | 按需求来源拆分 `REQ-xxx`，定义交付验收项和风险判定依据；对包含上线后业务结果目标的需求制定验证计划；开发智能体运行支撑配置，维护用例与证据，组织输入质量复核，冻结候选基线，执行正式自测，提交交付物并完成整改 | 不得将估算表述为实测，不得以自测结论代替交付评估；最高等级的独立复核（R3，具体动作见第 7 节）必须由未参与该版本开发的人执行 |
| 交付负责人 | 确认智能体任务、交付验收标准、非目标、风险等级及其依据，选择复核等级，核验整体完成度并作出交付评估结论；适用时确认上线后业务结果 | 不得用材料齐全代替能力和安全复核，也不得批准未通过硬门禁的版本 |
| 发布执行人 | 核对发布授权，部署评估通过的同一基线，记录发布、回滚和实际运行结果 | 不得替换已通过基线中的任何冻结项，也不得自行将未通过版本投入发布 |

## 5. 通过、质量目标与上线后业务结果

正式生产路径中的以下三个判断回答不同问题，必须保持概念和结论边界清晰。非生产任务使用第 2.7 节的任务决策，不把局部检查表述为正式通过或上线后结果。需求没有预先设定质量目标时，不作出“质量目标达成”结论；需求不包含上线后业务结果目标时，不作出相应结果结论。

| 判断 | 回答的问题 | 判定原则 |
|---|---|---|
| `通过` | 当前候选基线是否达到发布前最低要求 | 每个 `REQ-xxx` 的最低可接受任务结果均由硬门禁验收项约束；证据完整、全部 `blocking` 用例通过、安全违规为 0、所有适用硬门禁达标、无未解决 `error`，且候选基线和结果可复现 |
| 质量目标达成 | 已通过的方案是否达到运行前设定的质量目标；如要求相对改进，是否优于可比版本 | 使用运行前约定的评分规则、目标指标和护栏逐项判断；安全、硬门禁或最低质量失败不得被总分、成本或其他优点抵消 |
| 上线后业务结果达成 | 需求中的上线后业务结果目标是否达到 | 发布后使用预先确定的业务指标、数据源、观察窗和阈值验证；不得根据离线运行、健康检查或一次演示推断 |

表中的`通过`给出正式自测的最低条件；交付评估还必须满足六项交付内容完整、规定复核完成、基线可复现及发布回滚条件，具体以[《04｜评估、测试与回归规范》](./04_评估测试与回归规范.md)和[《06｜交付评估与发布规范》](./06_交付评估与发布规范.md)为准。

“质量目标达成”在正式评估运行通过后另行评价，不改变正式评估运行结论（`通过`、`不通过`或`不完整`）。项目不使用统一综合分，也不要求所有指标均高于旧版本；绝对门槛全部满足且非关键指标处于预先允许的护栏范围内时，可依据项目目标作出判断。要求相对改进时，必须提供可比对照运行。

## 6. 状态与结论

不同阶段使用不同状态，禁止混用：

| 阶段 | 允许状态 | 含义 |
|---|---|---|
| 正式自测 | `通过` / `不通过` / `不完整` | 分别表示全部硬门禁满足、存在已确认的硬门禁失败、证据或执行不足以判断 |
| 交付评估 | `通过` / `退回整改` / `材料不完整` | 分别表示整体交付满足要求、实质要求未满足、提交物不足以复核 |
| 发布 | `未发布` / `已发布` / `发布失败` / `已回滚` | 仅描述评估通过基线的实际部署状态，不代表能力结论 |
| 上线后业务结果 | `已达成` / `未达目标` / `证据不足` | 仅在需求包含相应目标时使用；分别表示按预定观察窗达标、证据足够但未达标、尚无法可靠判断 |
| 研究/适配决策 | `采用` / `继续验证` / `不采用` / `未知` | 仅描述当前问题的证据边界；`采用`不等于生产交付通过，`未知`不等于成功 |

正式生产路径不存在“条件通过”或“条件发布”。限定用户或流量范围的灰度发布、人工接管措施和已知限制可以作为已批准的发布约束，但不得绕过 `blocking` 评估用例、适用的硬门禁指标、安全硬门禁、材料完整性或基线一致性要求。

同一阶段同时存在已确认实质失败与证据或材料缺口时，实质失败优先：正式自测判定为`不通过`，交付评估判定为`退回整改`，并同时记录缺口。仅在尚无已确认实质失败但证据或材料不足时，才分别使用`不完整`或`材料不完整`。

## 7. 风险等级与复核等级（正式生产路径）

本节用于正式生产交付的风险分级和 R1/R2/R3 复核选择。其他路径只按实际后果确定安全控制与是否需要独立复核，不强制配置完整等级链。

风险等级描述智能体可能造成的后果，复核等级规定交付负责人应执行的复核动作。两者必须分别记录，不得将 R1/R2/R3 作为风险等级使用。

风险等级应根据以下事实综合判断，不采用简单加总或平均：数据敏感度、输出可能造成的后果、智能体自治程度、单次或累计动作规模、失败后的可逆性。任一方面可能造成重大后果时，不得以其他方面风险较低为由降低风险等级。

| 风险等级 | 特征 | 典型行为 | 最低复核等级 |
|---|---|---|---|
| 低 | 数据不敏感，输出影响有限，自治和作用范围小，出错易发现、易撤回 | 公开资料问答、低后果内容整理 | R1 |
| 中 | 接触受限数据、输出会影响业务判断，或调用系统执行可逆且受控的动作 | 内部研判建议、创建工单、生成待审批策略 | R2 |
| 高 | 涉及高度敏感数据、重大决策或高自治、大范围、难以恢复的动作 | 封禁、隔离、删除、修改生产策略或影响人身与合规的判断 | R3 |

| 复核等级 | 最低动作 |
|---|---|
| R1 | 核验六项交付内容、不可变定位信息、数量、覆盖、追溯关系和原始结果；不复跑，需要独立复跑时选择 R2 或 R3 |
| R2 | 完成 R1，并在本次冻结的正式评估范围内，复跑全部 `blocking`、全部 `safety` 及本次交付影响评估用例的并集 |
| R3 | 由未参与该版本开发的人完整重跑本次冻结的正式评估范围，并与开发自测结果比对 |

“只读”仅描述系统写入能力，不表示后果必然较低：读取敏感数据、泄露信息，或提供直接影响重大决策的建议，仍可判定为中风险或高风险。同一智能体具有多种能力时，应按最高风险能力确定风险等级。交付负责人不得选择低于风险等级所要求的复核等级；权限扩大、控制边界变化、框架迁移、关键工具变化或既有 `blocking` 历史失败可以提高复核等级，但不改变风险事实。发现不可变定位信息、追溯或原始证据不一致，复核改变 `blocking` 评估用例、适用硬门禁或正式评估运行结论，或结果真实性、完整性、可复现性存疑时，按《06｜交付评估与发布规范》升级复核或停止评估并调查。

逐条输入质量复核可以由开发成员本人执行，也可以由开发成员组织具备相应业务知识的人员执行。开发成员对复核完成和记录负责，交付负责人在交付评估中核验复核人员、日期和依据。

## 8. 基线与证据约束

本节的候选基线、正式运行编号和机器证据要求适用于正式生产交付。其他路径只需记录能定位本次实现或被测资产的来源、版本、实际输入、观察和限制。

- 正式生产路径的项目特有交付验收标准必须在直接开发前，或探索结束后、进入开发前确认；正式自测前冻结候选基线和评估实现。运行结果不得用于追溯降低标准。
- 《06｜交付评估与发布规范》第 5 节及模板五列明的任一冻结项发生变化，必须生成新的 `baseline_id`。
- 对同一候选基线重跑仅生成新的 `run_id`，并保留所有历史结果。
- 修改前评估通过其原有 `baseline_id + run_id` 引用；能否直接作为版本对照，按《04｜评估、测试与回归规范》第 13 节判断；能否作为当前业务表现的事实依据，按《02｜需求定义与场景输入规范》第 8 节判断。
- 六件套表示正式生产交付中供人工审阅的六项内容，可以合并在一份记录中；`cases.jsonl` 和 `results.csv` 是正式评估的机器证据。研究、修复和适配任务使用与问题规模相称的记录，不为套模板创建空文件。
- 数据过大或敏感时可以受控外置，但必须记录统一资源标识符（URI）、版本、SHA-256 内容摘要（用于核对内容是否变化的摘要值）和访问方式；空文件不得作为完整性证据。
- 发布版本必须与交付评估通过的 `baseline_id` 一致；发生任何临时修改时都必须停止发布，并重新进入基线冻结和自测流程。

交付物名称、目录和提交规则以[《06｜交付评估与发布规范》](./06_交付评估与发布规范.md)为唯一依据；六项内容的字段以[《07｜智能体研发交付六件套模板》](./07_研发交付六件套模板.md)为准，机器数据契约以[《04｜评估、测试与回归规范》](./04_评估测试与回归规范.md)为准。

## 9. 红线

1. **任何路径在任务、输入/前置条件或安全边界无法判定时，不得开始会产生真实副作用的执行。**
2. **正式生产路径中，`REQ-xxx`、`AC-xxx` 和正式评估范围必须在进入评估前确认；未确认时不得冻结或发布。**
3. **禁止为达到数量门槛降低输入和评估用例质量。** 证据不足时如实记录未知或不完整，无效样本不得计入正式结论。
4. **未明确并验证安全边界时，不得授予高风险写权限。**
5. **针对性检查、研究观察或资产可调用，不得表述为正式自测、交付通过或上线后结果。**
6. **正式生产交付未绑定候选基线、可追溯的正式评估运行和机器证据时，不得判定交付通过。**
7. **发布执行人不得发布与评估通过记录不一致的基线。**
8. **任何硬门禁失败都不得以“条件通过”“条件发布”、灰度或人工接管措施绕过。**
9. **交付评估通过不得表述为上线后业务结果目标已经达成。** 需求包含此类目标时，必须在发布后按预设数据源和观察窗验证；需求不包含时，不得虚构指标或结果结论。
