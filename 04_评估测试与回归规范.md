# 04｜评估、测试与回归规范

## 1. 核心目标

评估不是为了生成一个漂亮分数，而是回答三个问题：

1. 这个 Agent 现在能不能完成目标任务？
2. 本轮修改到底变好还是变坏？
3. 线上出现过的问题以后会不会再次发生？

## 2. 测试集结构

建议分四类，不再细分更多层级：

| 集合 | 用途 |
|---|---|
| 核心任务集 | 验证主要能力 |
| 边界任务集 | 验证模糊、缺失、异常输入 |
| 安全任务集 | 验证越权、高风险动作和策略 |
| 回归任务集 | 保存历史真实失败 |

同一条样本可以同时属于多个集合。

## 3. 一条合格测试样本至少包含

```yaml
id: case-001
scenario: 告警研判
input: 用户真实输入或等价输入
context: 必要上下文
expected_behavior:
  - 汇总关键证据
  - 给出风险判断
  - 不自动执行隔离
check:
  - 事实正确
  - 结论有证据
  - 输出结构正确
  - 未越权
```

不要求所有任务都有唯一“标准答案”。

## 4. 评价方式

优先级如下：

```text
确定性规则
> 结构化校验
> 可验证事实比对
> 人工 Rubric
> LLM-as-Judge
```

能用程序判断的，不交给另一个模型判断。

### 4.1 确定性检查适用

- JSON Schema；
- 必填字段；
- 工具调用是否正确；
- 是否调用禁止工具；
- 是否产生写操作；
- ID、IP、时间、状态等事实。

### 4.2 人工 Rubric 适用

- 分析质量；
- 证据链是否充分；
- 建议是否合理；
- 是否遗漏关键因素。

Rubric 不宜过长，一般 3～5 个评分维度即可。

## 5. 通过标准

不要只设一个“总准确率”。

建议至少拆成：

| 指标 | 说明 |
|---|---|
| 核心任务通过率 | 主要业务能力 |
| 关键错误数 | 严重事实错误、错误动作 |
| 安全违规数 | 越权、绕审批、禁止动作 |
| 历史失败复现数 | 回归是否重新出现 |

原则：

- R1 可允许少量非关键失败；
- R2/R3 的关键安全违规应为 0；
- 历史严重问题重新出现时，不应直接发布。

## 6. 回归集增长机制

出现以下情况应新增回归样本：

- 用户明确投诉；
- 生产误判；
- 工具误调用；
- 越权尝试；
- 模型升级后出现新失败；
- 框架迁移后行为变化。

回归集不是“越多越好”。重复、低价值样本可以合并或删除。

## 7. 对比实验

Harness 优化应尽量做 A/B 对比：

```text
同一模型
同一数据集
同一环境
只改变主要 Harness 变量
```

如果同时更换模型，则应明确记录“模型变化”和“Harness 变化”无法完全分离。

## 8. 最小评估实践

最开始完全可以使用：

- Markdown / JSONL 存测试样本；
- Python 脚本执行；
- CSV / Markdown 输出结果；
- 人工复核失败样本。

只有当任务量、团队协作和运行频率增加后，再引入专门评估平台。
