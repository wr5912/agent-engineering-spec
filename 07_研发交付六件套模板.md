# 07｜智能体研发交付六件套模板

本文件只提供填写结构，不是具体项目的交付物。项目应复制并填写六份独立文档；交付目录、机器证据和外置数据规则以《06｜交付评估与发布规范》为准。

---

# 模板一｜智能体需求定义

```markdown
# 智能体需求定义

## 1. 基本信息
- Agent 名称：
- 需求版本：
- 交付负责人：
- 开发成员：
- 发布执行人：
- 风险等级：低 / 中 / 高

## 2. 用户与问题
- 主要用户：
- 典型触发场景：
- 当前处理方式：
- 主要问题：

## 3. 目标任务与成功标准
| 需求 ID | 目标任务 | 可验证的成功标准 | 优先级 |
|---|---|---|---|
| REQ-001 | | | |

## 4. 输入与输出
- 输入：
- 必需上下文：
- 输出或动作：

## 5. 非目标
-

## 6. 依赖与约束
- 数据或系统依赖：
- 性能或成本约束：
- 运行范围：

## 7. 已知风险与限制
-
```

---

# 模板二｜用户场景与输入覆盖矩阵

```markdown
# 用户场景与输入覆盖矩阵

> 单元格填写一个或多个 Case ID；不适用时填写 `N/A`，并在表后说明理由。

| 需求 ID | 场景 ID | 意图 ID | 场景/用户目标 | `standard` | `colloquial` | `elliptical` | `multi_intent` | `incorrect_or_noisy` | `missing_info` | `high_risk` |
|---|---|---|---|---|---|---|---|---|---|---|
| REQ-001 | S01 | I01 | | case-001 | case-002 | case-003 | N/A | case-004 | case-005 | N/A |

## 覆盖摘要
- 已定义需求场景数：
- 已覆盖需求场景数：
- 需求场景覆盖率：100% / 未达到
- 适用矩阵格数：
- 已覆盖适用矩阵格数：
- 适用矩阵格覆盖率：100% / 未达到

## N/A 说明
| 矩阵格 | 原因 |
|---|---|
| | |

## 未覆盖项与最高风险场景
- 未覆盖项：
- 最高风险场景：
```

---

# 模板三｜任务与评估数据集说明

```markdown
# 任务与评估数据集说明

## 1. 数据集身份与数量
- Eval Set 版本：
- 路径或受控 URI：
- SHA-256：
- 访问方式：仓库内 / 受控系统及权限说明
- 候选 Case 总数：
- 质量审查剔除数：
- 唯一有效 Eval Case 数（必须 ≥ 50）：
- 不同 User Input 数（必须 ≥ 50）：

| 标签 | Case 数 |
|---|---:|
| `core` | |
| `boundary` | |
| `safety` | |
| `regression` | |

> 标签可以重叠，各类数量不得相加作为有效 Case 总数。

## 2. 用例来源
| `source_type` | 含义 | 数量 |
|---|---|---:|
| `real` | 经授权保留的真实输入 | |
| `real_redacted` | 已脱敏且业务含义未改变的真实输入 | |
| `near_real` | 按真实模式改编并经具备相应业务知识的人员确认 | |
| `synthetic_reviewed` | 生成后已经具备相应业务知识的人员逐条复核 | |

## 3. Eval Case 契约
- 字段：`id`、`requirement_ids`、`scenario_id`、`intent_id`、`tags`、`source_type`、`variant_types`、`input`、`context`、`expected_behavior`、`check`、`gate`
- 安全 Case 额外字段：`expected_control`
- 项目自定义 `context` 字段：

## 4. Trial 与聚合规则（运行前冻结）
- 每个 Case 的 Trial 次数：
- `blocking` Case：任一 Trial 失败即失败；全部要求的 Trial 通过才通过
- `scored` Case 聚合规则：

## 5. Rubric（没有 `scored` Case 时填 N/A）
- Rubric 版本：
| 维度 | 评分方法 | 强制阈值 |
|---|---|---:|
| | | |

## 6. 强制门槛
| 指标 | 预设阈值 |
|---|---:|
| `blocking` Case 通过率 | 100% |
| 强制开放性质量指标 | |
| 严重事实错误数 | 0 |
| 安全违规数 | 0 |
| 历史严重回归数 | 0 |
| 未解决 `error` Trial 数 | 0 |

## 7. 质量审查
- 开发成员：
- 逐条质量复核执行人：
- 复核执行人的业务知识或选取依据：
- 复核日期：
- 是否逐条确认来源、代表性、有意义差异、预期行为和可执行检查：是 / 否
- 主要剔除原因：
- 已知不足：
```

---

# 模板四｜安全与控制边界清单

```markdown
# 安全与控制边界清单

## 1. 数据边界
- 允许读取：
- 禁止读取：
- 租户或对象范围：

## 2. 工具和网络边界
- 允许 Tool/MCP：
- 禁止 Tool/MCP：
- 允许网络范围：
- 禁止网络范围：

## 3. 写操作与审批
| 操作 | 自动执行 | 需审批 | 禁止 | 回滚方式 |
|---|---|---|---|---|
| | | | | |

## 4. 执行环境与凭据
- Shell：允许 / 禁止 / 受限
- Sandbox：
- 凭据与最小权限：

## 5. 失败安全
- Tool 失败：
- 信息不确定：
- 高风险动作失败：
- 转人工条件：

## 6. 安全测试映射
| 控制边界 ID | 动作/控制 | 拦截 Case | 合法放行/审批 Case | 实际检查项 |
|---|---|---|---|---|
| CTRL-001 | | | | 回复 / Tool 调用与参数 / 权限与审批 / 最终状态 / 审计记录 |

> 若某动作永久禁止，合法放行/审批和回滚填写 `N/A（策略永久禁止）`，但必须保留强制阻断和审计证据。
```

---

# 模板五｜候选版本基线

```markdown
# 候选版本基线

## 1. 基线身份
- 候选版本：
- Baseline ID：
- 冻结时间：
- 开发成员：
- 需求版本：
- Git Commit：

## 2. Agent/Harness 与运行组合
- Runtime / Framework：
- Runtime Version：
- Model：
- Prompt Version：
- Skill Version：
- Tool / MCP Version：
- Policy Version：
- Environment：
- Build / Image：
- Build / Image SHA-256：

## 3. 评估输入
- Eval Set Version：
- Eval Set Path / URI：
- Eval Set SHA-256：
- Eval Set 访问方式：仓库内 / 受控系统及权限说明
- Rubric Version：
- Rubric Path / URI：

## 4. 版本变化
- 上一 Baseline ID：
- 本次主要变化：
- 控制边界变化：无 / 有，说明：

## 5. 发布与回滚准备
- 计划发布范围：
- 上一稳定版本或回滚目标：
- 回滚方法：

> 不存在的字段填写 `N/A` 并说明原因。任一冻结项变化都必须生成新的 Baseline ID。
```

---

# 模板六｜自测与交付评估报告

```markdown
# 自测与交付评估报告

## 1. 评估对象与最终证据
- 报告版本：
- 候选版本：
- Baseline ID：
- 需求版本：
- Eval Set 版本：
- Eval Set Path / URI：
- Eval Set SHA-256：
- Eval Set 访问方式：仓库内 / 受控系统及权限说明
- Results Path / URI：
- 交付评估完成后的 Results SHA-256：
- Results 访问方式：仓库内 / 受控系统及权限说明

## A. 开发自测

### A1. 执行信息
- 自测 Run ID：
- 执行人：
- 执行日期：
- 实际执行 Baseline ID：
- 与候选版本基线一致：是 / 否

### A2. 数据、执行与覆盖完整性
- 唯一有效 Eval Case 数（必须 ≥ 50）：
- 不同 User Input 数（必须 ≥ 50）：
- 应执行 Trial 数：
- 实际 Trial 数：
- 缺失 Trial 数（必须为 0）：
- 未解决 `error` 数（必须为 0）：

| 覆盖项 | 已覆盖 | 应覆盖 | 结论 |
|---|---:|---:|---|
| 已定义需求场景 | | | 100% / 未达到 |
| 适用输入矩阵格 | | | 100% / 未达到 |
| 适用安全控制边界 | | | 100% / 未达到 |

### A3. 结果与门槛
| 指标 | 预设阈值 | 实际结果 | 结论 |
|---|---:|---:|---|
| `blocking` Case 通过率 | 100% | | |
| 强制开放性质量指标 | | | |
| 严重事实错误数 | 0 | | |
| 安全违规数 | 0 | | |
| 历史严重回归数 | 0 | | |

| 分类 | Case 数 | 通过 | 失败 | `error` |
|---|---:|---:|---:|---:|
| `core` | | | | |
| `boundary` | | | | |
| `safety` | | | | |
| `regression` | | | | |

> 标签可以重叠，各类数量不得相加作为有效 Case 总数。

### A4. 失败与已知限制
| Case ID | 问题 | 严重度 | 是否阻断 | 处理计划 |
|---|---|---|---|---|
| | | | | |

- 已知限制及其需求依据：
- 剩余风险：

### A5. 开发自测结论
- 结论：通过 / 不通过 / 不完整
- 结论依据：
- 开发成员：
- 确认日期：

> 判定顺序：存在已确认的硬失败时为“不通过”；没有已确认硬失败但证据不足时为“不完整”；全部门槛满足时才为“通过”。

## B. 交付评估

### B1. 材料完整性
| 检查项 | 结果 | 说明 |
|---|---|---|
| 六件套齐全且相互一致 | 完整 / 缺失 | |
| `cases.jsonl` 或受控引用可访问且哈希一致 | 是 / 否 | |
| `results.csv` 或受控引用可访问且结果可定位 | 是 / 否 | |
| 开发自测 A 区的结论、开发成员和日期已确认 | 是 / 否 | |

### B2. 复核信息
- 风险等级：低 / 中 / 高
- 复核等级：R1 / R2 / R3
- 复核等级选择理由：
- 本次变更范围：
- 复核方式：证据审查 / 指定范围独立复跑 / 非开发者完整复跑
- 复核范围：
- 复核 Run ID（R1 未复跑时填 N/A）：
- 复核结果已写入同一结果事实源：是 / 否 / N/A
- 复核执行人：
- 是否未参与该版本开发（R3 必须为是）：是 / 否 / N/A
- 自测与复核结果对比：一致 / 不一致 / N/A
- 结果差异及处理：
- 复核日期：

### B3. 整体完成度
| 评估项 | 结论 | 依据或问题 |
|---|---|---|
| 目标任务和成功标准 | 满足 / 不满足 / 无法判断 | |
| 场景与输入覆盖 | 满足 / 不满足 / 无法判断 | |
| 能力与历史回归 | 满足 / 不满足 / 无法判断 | |
| 安全与控制边界 | 满足 / 不满足 / 无法判断 | |
| Baseline 与证据可追溯、可复现 | 满足 / 不满足 / 无法判断 | |
| 发布范围与回滚准备 | 满足 / 不满足 / 无法判断 | |

### B4. 复核发现
| Case/材料 | 发现 | 严重度 | 处理要求 |
|---|---|---|---|
| | | | |

### B5. 交付评估结论
- 结论：通过 / 退回整改 / 材料不完整
- 结论依据：
- 必须整改或补充项：
- 交付负责人：
- 评估日期：

> “通过”只适用于本报告绑定的 Baseline，不证明穷举现实世界所有问法。若声称不少于 50 种问法均能正确处理，相关不同 User Input 所关联的全部 Case 必须通过。实际发布结果进入现有发布记录，不在本报告中新增发布结论。
```
