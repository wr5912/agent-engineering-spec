# 09｜智能体研发交付检查清单

本清单用于三个实际交接点，不替代六件套正文和机器证据。

## A. 开发成员提交前

- [ ] 目标用户、任务、成功标准、非目标和低/中/高风险等级已经明确
- [ ] 已用 5～20 条真实或近真实输入做过最小探索，且未把探索结果当作正式结论
- [ ] Prompt、Tool、Skill、Workflow 等实现都有需求或失败证据依据
- [ ] 六件套已按当前候选版本填写，模板文件没有被直接当作交付物
- [ ] `delivery/` 未复制源码或创建空证据目录；外置数据记录了受控 URI、版本、SHA-256 和访问方式
- [ ] 唯一有效 Eval Case 不少于 50，且经质量审查的不同 User Input 不少于 50
- [ ] 已定义需求场景和适用输入矩阵格覆盖率均为 100%，`N/A` 有明确理由
- [ ] 每个 Case 可追溯到需求、场景和意图，并有可执行的预期行为与检查项
- [ ] 机械改写、只换标点或对象名、重复 Trial 和未复核批量生成样本未计入数量
- [ ] `core`、`boundary`、`safety`、`regression` 使用同一 Eval Set，以标签区分
- [ ] Trial 次数、聚合规则、Rubric 和强制阈值已在运行前冻结，未事后降低
- [ ] 已在冻结 Baseline 上完整运行 Eval Set，未用探索实验或针对性子集替代
- [ ] 所有 Trial 均有结果，不存在缺失、静默删除或未解决的 `error`
- [ ] `blocking` Case 100% 通过，强制质量指标达标，安全违规和严重历史回归均为 0
- [ ] 报告 A 区已记录自测 `run_id`、结果、限制、`通过`结论、责任人和日期
- [ ] 候选版本、需求、Git Commit、Harness、模型、Runtime、Policy、Eval Set、Rubric、环境和制品可追溯
- [ ] 每次执行都生成新 `run_id`；任一冻结项变化都生成新 `baseline_id`；旧结果未被覆盖
- [ ] 中、高风险的数据、工具、权限、审批、失败安全和回滚边界已经明确
- [ ] 安全 Case 核对了回复、Tool 调用与参数、权限与审批、运行结果和最终业务状态
- [ ] 上一稳定版本或回滚目标已经明确

## B. 交付负责人评估

- [ ] 六件套齐全，名称、版本、Baseline 和引用相互一致；报告 A 区的开发成员和日期完整
- [ ] `cases.jsonl`、`results.csv` 或其受控引用可访问，SHA-256 与报告一致
- [ ] 自测 `run_id` 能在结果中唯一定位，Case、Trial 和 Baseline 可追溯
- [ ] 材料缺失、证据不可访问或无法追溯时，结论填写`材料不完整`，没有凭经验补判
- [ ] 已分别记录风险等级和复核等级，复核等级不低于风险规定的最低要求，并写明变更范围与选择理由
- [ ] 已完成所选复核等级的对应动作（以下只选一项；升级时按更高等级执行）
  - [ ] R1：已审查哈希、数量、覆盖、追溯关系和原始结果
  - [ ] R2：已完成 R1，并独立复跑全部 `blocking`、`safety` 和本次变更涉及的 Case
  - [ ] R3：已由未参与开发的人完整重跑冻结的 Eval Set，并比对自测与复核结果
- [ ] 若实际复跑，已使用新 `run_id` 并进入同一结果事实源；R1 未复跑时本项标记 `N/A`
- [ ] 证据不一致、不可复现或真实性存疑时，已升级为完整重跑
- [ ] 已逐项核对成功标准、输入覆盖、能力、历史回归、安全、复现性和回滚，而不只查看总分
- [ ] 已知限制只落在明确非目标或不影响强制门槛的范围内
- [ ] 报告 B 区已记录复核方式、发现、结论、责任人和日期；实际复跑时记录新 `run_id`，R1 未复跑时填 `N/A`
- [ ] 结论只使用`通过`、`退回整改`、`材料不完整`，未使用“条件通过”
- [ ] 整改改变冻结项时，要求新建 Baseline、重新完整自测并再次提交

## C. 发布、回滚与生产反馈

- [ ] 只有交付评估`通过`的候选版本进入发布
- [ ] 待发布的 Baseline ID、Git Commit 和制品摘要与报告完全一致
- [ ] 评估通过后没有临时修改 Prompt、模型、工具、策略、环境或其他冻结项
- [ ] 灰度、只读或小范围开放仅作为发布策略，未被用来绕过门槛或形成“条件发布”
- [ ] 回滚目标、触发条件、执行人、步骤和回滚验证方法明确可用
- [ ] 实际发布时间、环境、制品、结果和回滚情况写入现有发布记录，未新增第七份文档
- [ ] 发布后真实失败和有效反馈能映射回需求、场景或 Case
- [ ] 严重失败已加入同一 Eval Set 并标记为 `regression`，安全问题同时标记为 `safety`
- [ ] 修复后先运行受影响子集；形成新候选版本时，仍完整自测并执行不低于风险最低要求的复核等级
- [ ] 清理重复或失效 Case 后，需求场景、输入矩阵和安全边界仍保持完整覆盖
