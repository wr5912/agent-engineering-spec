# 07｜智能体研发交付六件套模板

本文件只提供正式生产交付的填写结构，不构成具体项目的交付物。正式生产交付应填写六项内容，可以依次合并在一份 `交付记录.md` 中，也可以根据协作需要拆分；不得同时维护内容重复的合并版和拆分版。交付目录、机器证据和外置数据规则以《06｜交付评估与发布规范》为准。

> 本模板只用于正式生产交付。研究、已知缺陷修复、外部资产复用/集成或迁移且不发布的工作，不要为了套用模板创建 `REQ/AC`、候选基线、正式运行编号或空评估数据；按《00｜总纲》《01｜研发流程与按需探索验证》和《10｜外部资产复用与集成》记录目标、前置条件、观察、证据和决定即可，外部接入的轻量记录直接使用《10》第 6 节，不在本文件复制第二份模板。

## 填写责任、填写时点与数据缺失处理

模板中每个项目特有的内容由开发成员填写，交付负责人在 B 区复核；复核运行的实际执行人填写该次运行信息；发布执行人将发布与上线后运行结果写入团队现有发布/运营记录。上述责任归入本规范既有三个角色。

| 内容 | 填写责任人 | 填写时点 | 首选来源 | 暂无数据时的处理 |
|---|---|---|---|---|
| 需求来源、智能体任务与交付验收标准 | 开发成员，交付负责人确认 | 直接开发前确认；开展探索时，在实验前确认任务边界、判定标准和安全红线，在探索结束后、进入开发前确认全部项目特有验收项 | 正式需求、工单、事故或回归记录、迁移或整改指令、法规或合规要求、确认记录及安全约束 | 任一需求来源无法核验，或任务边界不足以界定当前工作时，不进入探索验证或开发；探索判定标准或安全红线不明确时不开展实验；硬门禁尚未确认时不进入直接开发 |
| 能力适用性 | 开发成员 | 探索前允许对待验证项标记`待验证`；进入开发前确认最终结论 | 候选版本的实际能力和运行路径 | 进入开发前只能填`适用`或`不适用（原因）`；不创建空资产 |
| 候选基线（Baseline） | 开发成员 | 冻结项登记完成后、首次正式自测前 | 版本控制提交、构建制品、配置、评估数据集和评分规则的实际不可变定位信息 | 不适用的条件组件填 `N/A（原因）`；必填项缺失则不得开始正式自测 |
| 评估执行编号与执行试次（Trial）结果 | 该次执行人 | `run_id` 在正式评估运行或复核执行开始前填写；每次执行试次结果在执行中或执行后填写 | 评估脚本、实际执行环境、工具和用量记录 | 适用字段缺失时按《04｜评估、测试与回归规范》判定；仅在确认不适用时留空 |
| 对照候选基线/正式评估运行 | 开发成员 | 形成相对改进结论之前 | 同口径旧版的不可变结果快照 | 填 `N/A（无可比对照）`，仅报告绝对验收结果 |
| 上线后业务结果目标（按需） | 需求包含此类目标时由开发成员填写，交付负责人确认 | 开发前明确目标，候选基线冻结前完成验证计划 | 需求、业务口径、用户可接受边界和可追溯业务数据 | 计划不完整时不得冻结候选基线；需求不包含此类目标时删除对应小节 |

量化字段的证据状态和推进条件直接执行《02｜需求定义与场景输入规范》第 3.3 节，本模板不重复定义。

本模板中，**结果事实源**是唯一用于形成正式评估运行结论的 `results.csv` 或受控结果记录；**不可变结果快照**是结论形成后内容不再变化、且能够通过 Git 提交或外置对象版本定位的结果副本；**不可变定位信息**是仓库内文件的路径与 Git 提交，或者外置文件的受控统一资源标识符、对象版本和 SHA-256 内容摘要。

## 唯一标识符（ID）生成、复用与填写位置

`baseline_id` 用于标识一套已冻结的候选基线，`run_id` 用于标识对该基线的一次独立评估执行。二者均采用 UUIDv4（随机生成的通用唯一标识符第 4 版）以避免重名；填写人不需要手工编造，直接执行本节命令并复制结果即可。`run_id` 本身不表示执行范围或结论；只有完整执行冻结的正式评估范围时，才形成正式评估运行结论。

- `baseline_id`：开发成员在全部冻结项登记完成后、首次正式自测前生成，格式为 `bl-<UUIDv4>`。
- `run_id`：每次准备将正式自测、复核或回归结果写入事实源时，由该次执行人在开始前生成，格式为 `run-<UUIDv4>`。
- 模板五、模板六针对同一候选基线的记录，以及属于该候选基线的全部结果行，必须填写同一个 `baseline_id`，不得在不同位置分别生成；同一结果事实源可以保留其他候选基线的历史结果行。
- 同一次评估执行的全部执行试次必须填写同一个 `run_id`，每个执行试次另使用在该次执行内唯一的 `trial_id`；下一次独立执行必须生成新的 `run_id`。
- 同一候选基线重跑：复用 `baseline_id`，生成新 `run_id`。任一冻结项变化：生成新 `baseline_id`，并为新运行生成新 `run_id`。
- 不写入正式结果的临时本地检查可以没有 `run_id`；一旦成为 `results.csv` 或受控结果事实，必须先生成。
- 两类编号仅用于保证唯一性，不编码版本、环境、执行人或结论；便于人工识别的信息应填写在独立字段中。

有 `uuidgen` 时：

```bash
uuidgen -r | tr '[:upper:]' '[:lower:]' | sed 's/^/bl-/'
uuidgen -r | tr '[:upper:]' '[:lower:]' | sed 's/^/run-/'
```

无 `uuidgen` 时：

```bash
python3 -c 'import uuid; print(f"bl-{uuid.uuid4()}")'
python3 -c 'import uuid; print(f"run-{uuid.uuid4()}")'
```

填写位置：`baseline_id` 填入模板五、模板六和每条结果；自测 `run_id` 填入模板六 A1，交付复跑的每个 `run_id` 各填入 B2 一行，每条结果同时填写所属运行编号。对已发布候选基线单独执行周期或漂移回归时，在现有发布或运营记录中填写 `baseline_id`、`run_id`、执行范围、执行人、日期、正式评估运行结论及不可变结果快照定位信息。仓库内快照填写路径和 Git 提交；外置快照填写受控统一资源标识符、对象版本和 SHA-256 内容摘要。需要形成相对改进结论时，将对照编号填入模板五“版本变化与对照”和模板六 A4；无可比对照时填 `N/A（无可比对照）`。

完整操作示例：开发成员冻结第 2 个发布候选版本（rc2）后生成 `bl-3f6f9ab6-4c41-4c93-a62e-f775677e3971`；首次正式自测的执行人生成 `run-64d40e51-aaf9-48de-91d7-247b7339d2ce`。最高等级的独立复核（R3）对同一基线复跑时继续使用前一个 `baseline_id`，新建 `run-96fa0cb5-5c83-48d4-bf72-d2ea7330469e`。若随后修改安全策略（Policy），新建 `bl-4d4cfbe4-febe-48c7-8e4b-2d1a87d97cb8`，下次正式评估运行再新建 `run_id`。

## `results.csv` 接口

`results.csv` 的精确表头、字段来源、单位、空值和聚合规则仅以《04｜评估、测试与回归规范》第 9 节为准，本模板不重复定义。执行人应在评估开始前生成 `run_id`，按 `cases.jsonl.required_trials` 为每个纳入范围的评估用例写入规定数量的结果行，并在执行结束后核对缺失行、重复 `trial_id` 和未解决 `error`。模板三只记录结果事实源的位置及其是否符合该契约。

## 不可变引用的填写顺序

为避免记录尚未产生的 Git 提交（Commit，用于在版本控制中唯一定位一组变更），按以下顺序形成交付记录：

1. 冻结并提交实际被测的智能体及其运行支撑配置，或者形成具有对象版本和内容摘要的不可变构建制品。
2. 冻结模板一至模板四、`cases.jsonl`、适用的评分规则、运行时和执行环境。仓库内内容记录路径和 Git 提交；外置内容记录受控统一资源标识符、对象版本和 SHA-256 内容摘要。全部冻结项齐备后生成 `baseline_id`，并填写模板五。
3. 每次正式评估运行或复核执行前生成 `run_id`，执行全部要求的执行试次，并将结果追加到唯一结果事实源。
4. 复核结束后，将本次结论采用的结果形成不可变快照。仓库内快照先提交，再在模板六中填写路径、该提交和采用的运行编号列表；外置快照填写受控统一资源标识符、对象版本和 SHA-256 内容摘要。模板六可以在后续提交中完成，不得填写尚未产生的提交编号。

---

# 模板一｜智能体需求定义

```markdown
# 智能体需求定义

## 1. 基本信息
- 智能体（Agent）名称：
- 需求版本：
- 交付负责人：
- 开发成员：
- 发布执行人：
- 风险等级：低 / 中 / 高
- 风险依据（列明决定该等级的需求编号、能力，以及数据敏感度、后果、自治程度、作用规模和可逆性）：
- 任务与验收确认（交付负责人 / 时间；直接开发前，或探索结束后、进入开发前）：
- 探索验证判定标准及安全红线确认（仅开展探索时填写，交付负责人 / 时间）：

## 2. 智能体任务与非目标
| 需求编号（ID） | 需求来源 | 用户与使用场景 | 智能体必须完成的任务 | 必需输入 | 输出或动作 | 禁止或非目标 |
|---|---|---|---|---|---|---|
| REQ-001 | 需求条款、问题记录、法规或合规要求，或者确认记录引用 | | | | | |

## 3. 发布前交付验收标准
| 验收编号 | 关联需求 | 维度 | 可观察的验收要求 | 判定作用 | 阈值或允许结果 | 阈值依据或规范条款 |
|---|---|---|---|---|---|---|
| AC-001 | REQ-001 | 任务质量 / 安全 / 稳定性 / 可靠性 / 时延 / 成本 | | 硬门禁 / 质量目标 / 不可退化护栏 | | |

> 《智能体需求定义》是项目特有交付验收标准的唯一事实源。每项需求至少关联一个定义最低可接受任务结果的硬门禁验收项；更高的期望水平另设质量目标。只记录实际适用项；验收要求必须是可观察的输出、动作、控制结果或最终业务状态，不得使用“能用”“效果好”等主观表述。全部硬门禁在直接开发前，或探索结束后、进入开发前明确阈值或允许结果。模板三通过验收编号记录评估实现，不得另行设定或修改验收标准。证据完整性及《04｜评估、测试与回归规范》的统一最低规则直接适用，不在本表重复填写。

## 4. 上线后业务结果目标（需求包含此类目标时保留）

> 需求要求改变上线后的处理时长、任务完成率、人工返工率、单位成本、重大风险事件或其他业务结果时，填写本表；需求不包含此类目标时，从项目文档中删除本小节。验证计划必须在候选基线冻结前完整确认；实际结果只能在发布后按观察窗判定。

| 业务结果指标 | 当前值及证据状态（相对目标时必填） | 目标值 | 数据源与计算口径（按适用项填写分子、分母、单位、统计方法） | 观察窗 | 未达目标后的处理方式 | 记录负责人 |
|---|---|---|---|---|---|---|
| | | | | | | |

> 发布前由发布执行人将已确认的适用内容写入现有发布/运营记录。没有可靠当前值时可以采用可判定的绝对目标，但不得形成定量或相对提升结论。

## 5. 能力适用性
| 能力 | 适用性 | 实际用途/风险 | 需求或证据位置 |
|---|---|---|---|
| 工具/模型上下文协议（Tool/MCP）调用 | 适用 / 不适用（原因） / 待验证（仅探索前） | | |
| 写操作或外部副作用 | 适用 / 不适用（原因） / 待验证（仅探索前） | | |
| 多轮状态 | 适用 / 不适用（原因） / 待验证（仅探索前） | | |
| 长期记忆（Memory） | 适用 / 不适用（原因） / 待验证（仅探索前） | | |
| 检索增强生成（RAG）/知识源 | 适用 / 不适用（原因） / 待验证（仅探索前） | | |
| 多智能体/委派 | 适用 / 不适用（原因） / 待验证（仅探索前） | | |
| 路由/降级（Routing/Fallback） | 适用 / 不适用（原因） / 待验证（仅探索前） | | |
| 外部网络 | 适用 / 不适用（原因） / 待验证（仅探索前） | | |
| 在线服务 | 适用 / 不适用（原因） / 待验证（仅探索前） | | |

> 开发成员根据本版本真实运行路径填写。`待验证`只能在探索验证前使用，进入开发前必须改为`适用`或`不适用`；`不适用`必须记录明确的业务原因。不得仅为满足表格形式而引入能力或创建空文档。

## 6. 依赖与约束
- 数据或系统依赖：
- 性能或成本约束：
- 运行范围：

## 7. 已知风险与限制
-

> 中、高风险写操作应当在本模板中明确初步控制原则：合法动作和审批条件写入第 2 节“输出或动作”，禁止动作写入“禁止或非目标”，审批与请求绑定、越权阻断和最终业务状态写入第 3 节验收标准。实现级详细控制在候选基线冻结前填入模板四，不在本节重复建表。
```

---

# 模板二｜用户场景与输入覆盖矩阵

```markdown
# 用户场景与输入覆盖矩阵

> 单元格填写一个或多个评估用例编号（Case ID）；不适用时填写 `N/A`，并在表后说明理由。
> 一个评估用例仅归属一个 `scenario_id + intent_id`，但可以通过 `requirement_ids` 关联多个需求；不得为满足覆盖率要求而复制相同输入。

| 需求 ID | 场景 ID | 意图 ID | 场景/用户目标 | 标准表达 `standard` | 口语表达 `colloquial` | 省略表达 `elliptical` | 多重意图 `multi_intent` | 错误或噪声输入 `incorrect_or_noisy` | 信息缺失 `missing_info` | 高风险输入 `high_risk` |
|---|---|---|---|---|---|---|---|---|---|---|
| REQ-001 | S01 | I01 | | case-001 | case-002 | case-003 | N/A | case-004 | case-005 | N/A |

> 上表输入变体键的定义、适用条件和填写规则以《02｜需求定义与场景输入规范》第 6.2 节为准。各场景按适用性填写；某类输入变体对全部场景均不适用时可以删除该列，但必须在“N/A 说明”中记录该类型及业务理由。

## 覆盖摘要
- 已定义需求场景数：
- 已覆盖需求场景数：
- 需求场景覆盖率：100% / 未达到
- 适用矩阵格数：
- 已覆盖适用矩阵格数：
- 适用矩阵格覆盖率：100% / 未达到

## N/A 说明
| 矩阵格 | 原因 |
|---|---|
| | |

## 未覆盖项与最高风险场景
- 未覆盖项：
- 最高风险场景：
```

---

# 模板三｜任务与评估数据集说明

```markdown
# 任务与评估数据集说明

## 1. 数据集身份与冻结范围
- 评估数据集（Eval Set）版本：
- 路径或受控统一资源标识符（URI）：
- 不可变定位信息（仓库内填路径和 Git 提交；外置填 URI、对象版本和 SHA-256）：
- 访问方式：仓库内 / 受控系统及权限说明
- 评估数据集、评分规则（Rubric）、执行试次（Trial）聚合规则和评估实现冻结确认（交付负责人 / 时间）：
- 候选评估用例总数：
- 逐条输入质量复核剔除数：
- 本次冻结范围内的唯一有效评估用例数：
- 本次冻结范围内的不同用户输入数：
- 适用规模规则：一般门槛 / 影响范围明确的有限交付
- 影响范围明确的有限交付记录（仅选择后一项时填写）：
  - 适用前提：确定性新建或变更 / 既有智能体的局部维护变更
  - 确定性新建或变更：预期结果可由确定性检查完整判定，且未新增或改变开放式或非确定性模型行为：是 / 否，检查方式与依据：
  - 局部维护变更：前一按一般门槛通过的 `baseline_id + run_id`：；本次未新增或扩大需求、验收含义、工具能力、真实动作、权限或安全边界：是 / 否
  - 范围限定依据（实际配置、路由、知识集合、工具暴露范围或代码路径）：
  - 本次新增或受影响的需求、验收项、场景、意图、输入矩阵格、控制边界和历史问题清单：
  - 已纳入上述项目关联的全部评估用例、能够经过被修改运行路径的全部评估用例，以及同一执行路径或控制边界中的全部 `blocking`、`safety` 和 `regression` 评估用例：是 / 否，评估用例编号或可执行筛选条件：
  - 本次范围内的需求场景和适用输入矩阵格均 100% 覆盖，且不存在未查明的影响范围：是 / 否，覆盖结果：
  - 交付负责人于候选基线冻结前确认适用前提、范围限定依据、评估范围和覆盖结果（姓名 / 时间）：
- 已有覆盖更广的评估数据集：无 / 有，版本或引用：；本次未纳入范围的部分及理由：

> 只有《04｜评估、测试与回归规范》第 4.2 节的适用前提和全部共同条件均满足，才可按明确范围评估；任一条件不满足或无法核实时，执行一般门槛。按明确范围评估不会删除已有评估用例，后续变更应重新判定影响范围。

| 标签 | 评估用例数 |
|---|---:|
| `core` | |
| `boundary` | |
| `safety` | |
| `regression` | |

> 标签可以重叠，各类数量不得相加作为有效评估用例总数。

## 2. 用例来源
| `source_type` | 含义 | 数量 |
|---|---|---:|
| `real` | 经授权保留的真实输入 | |
| `real_redacted` | 已脱敏且业务含义未改变的真实输入 | |
| `near_real` | 按真实模式改编并经具备相应业务知识的人员确认 | |
| `synthetic_reviewed` | 生成后已由具备相应业务知识的人员逐条完成输入质量复核 | |

## 3. 评估用例契约
- 字段：`id`、`requirement_ids`、`acceptance_id`、`scenario_id`、`intent_id`、`tags`、`source_type`、`variant_types`、`input`、`context`、`expected_behavior`、`check`、`required_trials`、`gate`
- 安全评估用例额外字段：`expected_control`
- 一个评估用例仅归属一个 `scenario_id + intent_id`，可以包含多个 `requirement_ids` 和 `variant_types`，但只关联一个 `acceptance_id`
- 同一用户输入需要分别验证多个验收项时，按不同检查项拆分为不同评估用例；该输入在不同用户输入数量中只计一次
- `blocking` 用例的 `acceptance_id` 必须关联硬门禁验收项
- 项目自定义 `context` 字段及其版本引用（多轮状态、初始快照或模拟器按需填写）：

## 4. 执行试次与聚合规则（候选基线冻结前确定）
- `required_trials` 默认值：1
- 需要多次执行的评估用例及原因（针对性检查已观察到结果差异 / 需求要求重复一致性 / 高风险动作依赖模型判定）：
- 自测与复核需要判断数值一致性时，填写适用指标及跨运行容差；不判断数值一致性时填 N/A：
- 文本差异的判定依据：同一 `expected_behavior` 和 `check`
- `blocking`（阻断）用例：任一执行试次失败即失败；全部要求的执行试次通过才通过
- `scored`（计分）用例聚合规则：
- 采用数值评分规则时，分数到执行试次 `pass` / `fail` 的映射：

## 5. 评分规则（没有采用评分规则的评估用例时填 N/A）
- 评分规则版本：
| 维度 | 评分方法 | 适用评估用例/检查 | 分数到执行试次状态的映射 |
|---|---|---|---|
| | | | |

### 5.1 大语言模型评判器校准（仅使用模型评判器时填写）
- 评判器模型、供应方和版本：
- 评判提示词版本及不可变定位信息：
- 校准用例编号：
- 人工参考判定及复核人：
- 一致性计算方法和通过阈值：
- `blocking` 校准用例与人工参考判定全部一致：是 / 否
- 其他校准用例实际一致性结果：
- 校准结论：通过 / 不通过
- 校准证据引用：

## 6. 交付验收标准的评估实现（候选基线冻结前完成）
| 验收编号 | 评估用例编号或可执行筛选条件 | 评价方法或评分规则引用 | 数据源、结果字段与单位 | 执行试次与聚合规则引用 |
|---|---|---|---|---|
| AC-001 | | 确定性检查 / 人工评分 / 已校准评判器 | | 模板三第 4 节通用规则，或本项特殊规则 |

> 每个模板一中的 `AC-xxx` 必须且只能在本表建立一行评估实现，并至少命中本次冻结正式评估范围内一个有效评估用例；本行列出或筛选出的每个评估用例，其 `acceptance_id` 必须等于本行验收编号。各行命中的用例按 `case_id` 去重后，其并集必须与本次冻结正式评估范围完全一致，确保每个范围内用例均被其 `acceptance_id` 对应的验收项命中。评估用例使用明确的 `case_id` 列表，或者可由执行器不含歧义地复现的筛选条件；不得仅填写“相关用例”。验收项缺少或重复评估实现、列表或筛选结果为 0、编号不匹配，或任一范围内用例未被命中时，追溯关系不完整。验收要求、判定作用、阈值和阈值依据仅以模板一为准，本表不得复制或修改；实际结果及证据写入 `results.csv` 和模板六。任务质量、安全、稳定性、可靠性、时延和成本的适用条件及统一最低检查要求直接执行《04｜评估、测试与回归规范》第 6.1 节。

## 7. 正式评估运行结论依据
- 本次采用的模板一需求版本及不可变定位信息：
- 模板一 `AC-xxx` 总数 / 已建立评估实现数：

> 正式评估运行结论直接采用《04｜评估、测试与回归规范》第 11 节的判定结果，本模板不另行定义结论规则。

## 8. 机器结果接口
- 结果事实源路径 / URI：
- 表头、字段、空值和聚合规则符合《04｜评估、测试与回归规范》第 9 节：是 / 否
- 一行一个执行试次：是 / 否
- 适用指标的数据来源或缺口：

## 9. 输入质量复核
- 开发成员：
- 逐条输入质量复核执行人：
- 复核执行人的业务知识或选取依据：
- 复核日期：
- 是否逐条确认来源、代表性、有意义差异、预期行为和可执行检查：是 / 否
- 主要剔除原因：
- 已知不足：
```

---

# 模板四｜安全与控制边界清单

```markdown
# 安全与控制边界清单

## 1. 数据边界
- 允许读取：
- 禁止读取：
- 租户或对象范围：
- 不可信输入来源（网页/文档/工具/检索内容/子智能体等）：
- 敏感数据在输入、输出、日志和证据中的处理：

## 2. 工具、模型上下文协议与网络边界（模板一判定工具调用适用时保留）
| 工具/模型上下文协议（Tool/MCP） | 允许调用条件 | 禁止调用条件 | 关键参数与业务值检查 | 超时/部分成功 | 重试、幂等与最终状态验证 |
|---|---|---|---|---|---|
| | | | | | |

- 允许网络范围：
- 禁止网络范围：

> 不得仅检查智能体回复中的“已完成”；对有副作用的工具必须检查实际参数、审批、工具结果和最终业务状态。

## 3. 写操作与审批（模板一判定写操作适用时保留）
| 操作 | 自动执行条件 | 需审批条件 | 审批绑定与有效性 | 禁止条件 | 服务端强制控制 | 重放/幂等 | 回滚、补偿或止损方式 |
|---|---|---|---|---|---|---|---|
| | | | 申请人身份、审批人身份、审批结果、动作、对象/租户、原因与证据、关键参数、范围、预计影响、时间/有效期、本次请求 | | | | |

> 每项写操作必须明确归入自动执行、需审批或禁止，并填写相应条件；不适用的单元格填 `N/A（原因）`。需审批时，审批上下文不得仅填写“是”，必须能够核对批准内容与实际请求一致，且参数、对象、租户、范围或有效期变化后必须重新审批。

## 4. 状态与条件能力（仅填写需求中标记适用的项）
| 能力 | 作用域/来源与可信度 | 必须的控制和失败处理 | 测试用例/证据 |
|---|---|---|---|
| 多轮状态 | 用户/租户/会话（Session）；状态来源 | 隔离、过期、更正和删除 | |
| 长期记忆（Memory） | 作用域；来源与写入依据 | 污染、跨用户泄漏、过期、更正、删除 | |
| 检索增强生成（RAG）/知识源 | 语料/索引版本；来源与时效 | 所需内容能否检出、回答是否忠实于证据、引用追溯、过期与回滚 | |
| 多智能体/委派 | 委派输入输出；共享状态 | 权限不扩大、失败传播、取消、最大调用层数和并发子任务数限制 | |
| 路由/降级（Routing/Fallback） | 路由规则和各分支 | 逐分支验证、降级触发条件与失败语义 | |

> 仅保留模板一中判定为适用的行；适用性及理由直接引用模板一。

## 5. 执行环境与凭据
- 命令行环境（Shell）：允许 / 禁止 / 受限
- 隔离沙箱（Sandbox）：
- 凭据与最小权限：

## 6. 执行预算和停止条件
| 项目 | 上限或规则 | 超限动作 |
|---|---|---|
| 步数/工具次数/重试 | | |
| 时长/模型文本用量/成本 | | |
| 并发/多智能体并发子任务数 | | |
| 转人工 / 紧急停用 | | |

> 开发成员仅保留适用的预算行；适用性及理由直接引用模板一。存在资源或安全风险但未设置上限时，不得冻结候选基线。

## 7. 失败安全
- 工具失败：
- 信息不确定：
- 高风险动作失败：
- 转人工条件：

## 8. 安全测试映射
| 控制边界编号（ID） | 关联硬门禁验收编号 | 动作/控制 | 拦截用例 | 合法放行/审批用例 | 实际检查项 |
|---|---|---|---|---|---|
| CTRL-001 | AC-xxx | | | | 回复/工具调用与参数/权限与审批/最终状态/审计记录 |

> 若某动作永久禁止，合法放行/审批和回滚填写 `N/A（策略永久禁止）`，但必须保留强制阻断和审计证据。
> 安全用例仅覆盖实际攻击面：读取不可信内容时测试直接或间接提示词注入（Prompt Injection，即不可信内容试图改变原有指令或权限）及数据外泄；工具、检索增强生成、子智能体、长期记忆、审批或多租户仅在相应能力适用时测试。适用性引用模板一。
```

---

# 模板五｜候选基线

```markdown
# 候选基线

## 1. 基线身份
- 候选版本：
- 候选基线编号（Baseline ID，`bl-<UUIDv4>`）：
- 冻结时间：
- 开发成员：
- 需求版本：
- 模板一至模板四及仓库内评估资产的 Git 提交（Commit）：
- 智能体实现与运行支撑配置的不可变定位信息：

> 开发成员必须在本文档全部冻结项齐备后、首次正式自测前生成候选基线编号。冻结项未齐备时生成的编号不表示候选基线已完成冻结。纯提示词或脚本智能体没有独立安全策略、制品或条件组件时，填写 `N/A（原因；实际内容由仓库路径和 Git 提交，或外置对象版本和 SHA-256 定位）`；不得仅为生成编号而创建空镜像或空配置。

> 第一项 Git 提交用于绑定任务、验收标准、场景、评估实现、安全边界和仓库内 `cases.jsonl`；同一提交覆盖全部内容时只填写一次。智能体实现和运行支撑配置在仓库内时填写路径与 Git 提交，在仓库外时填写受控统一资源标识符、对象版本和 SHA-256 内容摘要。

## 2. 智能体、运行支撑配置与核心运行组合
- 运行时/框架名称：
- 运行时/框架版本：
- 模型与路由配置：
- 提示词（Prompt）版本：
- 安全策略（Policy）版本：
- 执行环境（操作系统、容器、依赖锁定及配置的不可变定位信息）：
- 制品/镜像（可部署构建对象）的不可变定位信息：

> 源码或脚本直接运行且 Git 提交与执行环境已覆盖部署对象时，制品/镜像填 `N/A（源码或脚本直接运行，实际承载位置）`。仓库内文件填写路径和 Git 提交；外置配置或制品填写受控统一资源标识符、对象版本和 SHA-256 内容摘要。同一定位信息覆盖多个字段时，其余字段直接交叉引用。

## 3. 条件组件冻结（仅保留适用行）
| 组件 | 不可变定位信息 | 初始状态或关键规则 | 证据来源 |
|---|---|---|---|
| 技能（Skill） | | | |
| 工具/模型上下文协议（Tool/MCP） | | 参数结构、权限与失败语义 | |
| 多轮状态 | 字段结构/存储版本 | 初始评测快照 | |
| 长期记忆（Memory） | 策略/字段结构/快照版本 | 初始记忆快照 | |
| 检索增强生成（RAG）/知识源 | 语料与索引的不可变定位信息 | 更新与回滚规则 | |
| 多智能体/委派 | 智能体/编排版本 | 权限、最大调用层数和并发子任务数 | |
| 路由/降级（Routing/Fallback） | 路由规则版本 | 分支与触发条件 | |

> 适用性及理由直接引用模板一；删除不适用行。开发成员在首次正式自测前根据实际配置和快照填写不可变定位信息。运行中产生的记忆或状态变化属于结果证据，不回写为基线初始快照。

## 4. 评估输入
- 评估数据集（Eval Set）版本：
- 评估数据集路径/统一资源标识符（URI）：
- 评估数据集不可变定位信息（仓库内填路径和 Git 提交；外置填 URI、对象版本和 SHA-256）：
- 评估数据集访问方式：仓库内 / 受控系统及权限说明
- 评分规则（Rubric）版本（不使用时填 N/A 及原因）：
- 评分规则路径/URI（不使用时填 N/A）：
- 评分规则不可变定位信息（不使用时填 N/A）：
- 本次正式评估范围（评估用例编号或可执行筛选条件）：

## 5. 复现步骤
- 源码、配置和受控制品的取得方式：
- 必要配置键名及凭据取得方式（仅记录键名和受控来源，不填写凭据值）：
- 环境准备与依赖检查命令或步骤：
- 服务或执行器启动命令：
- 评估前初始状态及重置命令或步骤：
- 正式评估命令：
- 结果文件和运行证据的预期位置：
- 受控外部依赖、所需权限及不可用时的停止条件：

> 独立执行人能够按上述记录取得冻结对象、恢复规定的初始状态、执行本次冻结的正式评估范围，并按冻结的评价规则形成结论，才视为候选基线可复现。命令因受控环境不能写入文档时，应提供可访问的运行手册引用和版本。

## 6. 版本变化与对照
- 对照类型：可直接对比 / 仅历史参考 / N/A（无可比对照）
- 对照候选基线编号（Baseline ID，可直接对比时必填）：
- 对照运行编号（Run ID，可直接对比时必填）：
- 已逐项核对并满足《04｜评估、测试与回归规范》第 13 节全部可比性条件：是 / 否，说明：
- 本次主要变化：
- 控制边界变化：无 / 有，说明：

> 仅当既有结果可以通过完整的 `baseline_id + run_id` 定位且口径一致时，才可以作出定量改进结论。缺少可比旧运行不影响依据绝对门槛验收；对照字段填写 `N/A（无可比对照）`，不得以主观估计代替对照证据。

## 7. 发布与回滚准备
- 计划发布范围：
- 上一稳定版本或回滚目标：
- 回滚方法：
- 实际业务协议和完整业务链验证步骤及执行条件：

> 核心冻结项缺失时不得开始正式自测。任一冻结项变化都必须生成新的候选基线编号。
```

---

# 模板六｜自测与交付评估报告

```markdown
# 自测与交付评估报告

## 1. 评估对象与最终证据
- 报告版本：
- 候选版本：
- 候选基线编号（Baseline ID）：
- 需求版本：
- 评估数据集版本：
- 评估数据集路径/URI：
- 评估数据集不可变定位信息：
- 评估数据集访问方式：仓库内 / 受控系统及权限说明
- 结果事实源路径/URI：
- 不可变结果快照定位信息（仓库内填路径和 Git 提交；外置填 URI、对象版本和 SHA-256）：
- 本次结论采用的运行编号（Run ID）列表：
- 结果快照访问方式：仓库内 / 受控系统及权限说明

> 交付负责人在 B 区完成时锁定结果快照的不可变定位信息。后续运行可以追加到结果事实源，但不得改写本报告已引用的快照；新结果使用新的报告版本或外置对象版本。

## A. 开发自测

### A1. 执行信息
- 自测运行编号（Run ID）：
- 执行人：
- 执行日期：
- 实际执行的候选基线编号（Baseline ID）：
- 与候选基线一致：是 / 否

### A2. 数据、执行与覆盖完整性
- 本次实际执行的唯一有效评估用例数：
- 本次实际执行的不同用户输入数：
- 应执行试次数（本次范围内 `required_trials` 之和）：
- 实际执行试次数：
- 缺失执行试次数（必须为 0）：
- 未解决 `error` 数（必须为 0）：

| 覆盖项 | 已覆盖 | 应覆盖 | 结论 |
|---|---:|---:|---|
| 本次正式评估范围内的需求场景 | | | 100% / 未达到 |
| 本次正式评估范围内的输入矩阵格 | | | 100% / 未达到 |
| 适用安全控制边界 | | | 100% / 未达到 |

> “适用安全控制边界”的应覆盖数为模板四保留的 `CTRL-xxx` 数量；模板四确认不存在适用控制边界时，该行填写 `N/A（无适用 CTRL-xxx）`。

### A3. 验收结果
| 验收项或统一判定条件 | 定义来源 | 实测结果 | 是否满足 | 证据引用或缺口 |
|---|---|---|---|---|
| AC-001（每个验收项一行） | 模板一 | | 是 / 否 / 无法判断 | |
| 阻断评估用例通过率 | 《04｜评估、测试与回归规范》第 11 节 | | 是 / 否 / 无法判断 | |
| 安全违规数 | 《04｜评估、测试与回归规范》第 11 节 | | 是 / 否 / 无法判断 | |
| 未解决 `error` 执行试次数 | 《04｜评估、测试与回归规范》第 11 节 | | 是 / 否 / 无法判断 | |
| 已映射为阻断用例的历史失败复现数 | 《04｜评估、测试与回归规范》第 11 节 | | 是 / 否 / 无法判断 | |

> 模板一中每个 `AC-xxx` 均须单独填写一行；验收要求、判定作用和阈值查阅模板一，评估用例范围、评价方法和聚合规则查阅模板三，不在报告中重复抄写。《04｜评估、测试与回归规范》要求的其他适用指标也须报告实测值和证据。

| 分类 | 评估用例数 | 通过 | 失败 | `error` |
|---|---:|---:|---:|---:|
| `core` | | | | |
| `boundary` | | | | |
| `safety` | | | | |
| `regression` | | | | |

> 标签可以重叠，各类数量不得相加作为有效评估用例总数。

### A4. 对照与质量目标判定
- 对照类型：可直接对比 / 仅历史参考 / N/A（无可比对照）
- 对照候选基线编号：
- 对照运行编号：
- 已逐项核对并满足《04｜评估、测试与回归规范》第 13 节全部可比性条件：是 / 否 / N/A

| 指标 | 对照运行 | 候选运行 | 差异 | 是否在护栏内 |
|---|---:|---:|---:|---|
| | | | | |

- 运行前设定的质量目标：
- 质量目标结论：达成 / 未达成 / 未判定（正式评估运行不通过或不完整） / N/A（未设定质量目标）
- 判断依据：

> 只有正式评估运行通过时，才在`达成`和`未达成`之间判定质量目标；正式评估运行不通过或不完整时填写`未判定`，未设定质量目标时填写 `N/A`。“质量目标达成”不改变正式评估运行结论，也不得抵消硬门禁。无可比旧版的全新项目可以依据运行前设定的质量目标作出补充评价；质量目标要求相较旧版有所改进时，必须提供可比的对照 `baseline_id + run_id`。

### A5. 失败与已知限制
| 评估用例编号 | 问题 | 是否阻断 | 处理计划 |
|---|---|---|---|
| | | | |

- 已知限制及其需求依据：
- 剩余风险：

### A6. 开发自测正式评估运行结论
- 结论：通过 / 不通过 / 不完整
- 结论依据：
- 开发成员：
- 确认日期：

> 结论直接采用《04｜评估、测试与回归规范》第 11 节的判定结果。

## B. 交付评估

### B1. 材料完整性
| 检查项 | 结果 | 说明 |
|---|---|---|
| 六项交付内容齐全且相互一致，合并或拆分形式唯一 | 完整 / 缺失 | |
| `cases.jsonl` 或受控引用可访问且不可变定位信息一致 | 是 / 否 | |
| 不可变结果快照可访问，定位信息一致且运行可定位 | 是 / 否 | |
| 开发自测 A 区的结论、开发成员和日期已确认 | 是 / 否 | |

### B2. 复核信息
- 风险等级：低 / 中 / 高
- 复核等级：R1 / R2 / R3
- 复核等级选择理由：
- 本次交付范围：
- 复核方式：证据审查 / 指定范围独立复跑 / 由未参与该版本开发的人完整复跑
- 复核范围（R2 限定在本次冻结正式评估范围内，填写全部 `blocking`、全部 `safety` 与本次交付影响评估用例的并集；R3 填写本次冻结的完整正式评估范围）：

| 复核运行编号 | 复核等级 | 执行范围 | 运行或范围结果 | 证据引用 | 执行人 / 日期 | 纳入本次结论 |
|---|---|---|---|---|---|---|
| N/A（R1 未复跑）或 run-... | R1 / R2 / R3 | | R1：N/A；R2：范围内通过 / 范围内不通过 / 范围内不完整；R3：通过 / 不通过 / 不完整 | | | 是 / 否 |

- 全部复跑结果已写入同一结果事实源：是 / 否 / N/A
- R3 执行人均未参与该版本开发：是 / 否 / N/A
- 自测与复核结果对比：完全一致 / 差异符合冻结规则且正式评估运行结论一致 / 实质不一致 / N/A
- 差异是否符合运行前冻结的评价与评估用例/执行试次聚合规则：是 / 否 / N/A
- 差异对 `blocking` 评估用例、适用硬门禁或正式评估运行结论的影响：无 / 有 / 无法判断 / N/A
- 差异对质量目标或不可退化护栏补充评价的影响：无 / 有，说明 / N/A
- 结果差异、按冻结规则的判定及处理：

> 每次独立复跑各占一行并使用新的 `run_id`；执行或证据问题修复后的重跑也不得覆盖旧行。“纳入本次结论”用于指出交付负责人最终采用的运行，不表示可以删除其他运行记录。R2 只形成范围结果；需要跨范围聚合的硬门禁，仅在其全部映射用例均纳入 R2 时重新计算，否则沿用正式自测结果并报告子集差异。R3 才按正式评估运行规则形成完整结论。

### B3. 整体完成度
| 评估项 | 结论 | 依据或问题 |
|---|---|---|
| 模板一中每个 `REQ-xxx` 的需求来源均可核验 | 满足 / 不满足 / 无法判断 | |
| 智能体整体风险等级及其依据已填写，并列明决定等级的需求和能力 | 满足 / 不满足 / 无法判断 | |
| 模板一中的智能体任务与验收项，以及模板三中的评估实现映射 | 满足 / 不满足 / 无法判断 | |
| 场景与输入覆盖 | 满足 / 不满足 / 无法判断 | |
| 《04｜评估、测试与回归规范》中适用的运行、证据、安全和回归要求 | 满足 / 不满足 / 无法判断 | |
| 安全与控制边界 | 满足 / 不满足 / 无法判断 | |
| 候选基线与证据可追溯、可复现 | 满足 / 不满足 / 无法判断 | |
| 发布范围、完整业务链验证步骤及执行条件、回滚准备 | 满足 / 不满足 / 无法判断 | |
| 需求包含上线后业务结果目标时，指标、数据源、观察窗、未达目标后的处理方式和负责人已预先明确 | 满足 / 不满足 / 无法判断 | |

> 需求不包含上线后业务结果目标时，从项目报告中删除上表最后一行。

### B4. 复核发现
| 评估用例或材料 | 发现 | 是否构成实质失败或材料缺口 | 处理要求 |
|---|---|---|---|
| | | | |

### B5. 交付评估结论
- 结论：通过 / 退回整改 / 材料不完整
- 结论依据：
- 必须整改或补充项：
- 交付负责人：
- 评估日期：

> “通过”只表示本报告绑定的候选基线达到发布准入，不表示上线后业务结果目标已经达成，也不证明已覆盖未进入评估范围的情形。实际发布和上线后业务结果记录在现有发布或运营记录中。结论条件和优先级直接执行《06｜交付评估与发布规范》第 7.4 节。
```
