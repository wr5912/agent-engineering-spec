# 03｜智能体运行支撑配置（Harness）开发与变更规范

## 1. 智能体运行支撑配置的定位

智能体运行支撑配置是将模型能力组织为稳定任务能力的工程实现层，可以包括：

- 提示词或系统指令；
- 上下文与长期记忆；
- 检索增强生成（RAG，先检索资料再据此生成回答）；
- 技能；
- 工具与模型上下文协议（MCP，用于按统一协议连接模型、工具和数据源）；
- 钩子；
- 智能体与子智能体；
- 工作流；
- 插件；
- 模型路由；
- 运行时配置；
- 结构化输出与校验；
- 重试、超时、恢复和状态管理。

本文件规范智能体运行支撑配置的实现、变更和针对性检查。正式生产交付还需要候选基线、正式自测与正式回归，分别按本文件第 6 节和《04｜评估、测试与回归规范》执行，整体交付裁决按《06｜交付评估与发布规范》执行；研究、修复、复用或迁移且不发布时，只记录与问题相称的来源、版本、前置条件、观察和决策。

工具调用、写操作、多轮状态、长期记忆、检索增强生成、多智能体、路由或降级、外部网络和在线服务等能力，应当先按《02｜需求定义与场景输入规范》判断适用性：正式生产路径逐项记录，其他路径只记录与当前问题有关的能力。以下专项要求仅约束实际启用的能力；能力不适用时，不得创建空配置、空测试或空平台作为替代材料。

## 2. 实现责任与判断依据

正式生产路径中，开发成员对智能体运行支撑配置实现、变更记录、针对性检查和候选基线的完整性负责；交付负责人依据需求、能力、安全和可复现证据判断交付完成度，不逐句审批提示词或指定内部编排方式。其他路径由执行人和决策人按当前问题记录完成判定。

原则：

> **实现方式可以变化，但不得绕过任务边界、安全边界和与当前路径相称的可追溯性要求；正式生产交付另受正式自测门槛约束。**

以下实现特征不得单独作为能力质量的判定依据：

- 提示词行数；
- 技能拆分粒度；
- 智能体数量；
- 等价工作流的节点差异；
- Claude Code、DSH、AgentScope 等框架的目录形态。

正式生产路径中，这些实现一旦影响运行结果，仍必须进入版本控制和候选基线；其他路径至少记录来源、版本和本次变更范围，只有在需要比较或交付时才建立基线。

## 3. 设计优先级

应当依据已确认的任务、真实失败或实际风险选择最小改动：

| 失败类型 | 优先检查或修改 |
|---|---|
| 任务理解偏差 | 需求表达、提示词、任务定义 |
| 缺少知识 | 上下文、检索增强生成、知识源 |
| 执行能力缺失 | 工具、技能 |
| 输出格式不稳定 | 字段结构、校验器 |
| 流程步骤遗漏 | 工作流、状态记录 |
| 工具选择错误 | 工具说明、路由规则 |
| 长时任务的状态丢失 | 恢复检查点、结构化状态 |
| 权限越界 | 安全策略、权限、隔离沙箱 |
| 外部资产已安装但未加载 | 加载入口、依赖和目标运行时配置 |
| 外部资产可调用但任务结果错误 | 任务规则、参数构造和结果消费；按[《10｜外部资产复用与集成》](./10_外部资产复用与集成.md)验证 |
| 外部资产状态、分页、单位或空值被错误解释 | 现有调用边界内的确定性转换和状态处理；保留未知/部分状态 |

用于判断某项变化效果的探索或对比实验，原则上每轮只改变一个主要变量。确实无法分离的关联配置或完成确定性修复所必需的紧密关联项可以同时调整，但应当记录组合变化及无法单独归因的限制。

### 3.1 工具调用与最终业务状态

正式生产路径按评估用例和安全清单记录以下内容；其他路径按当前问题保留必要的调用、状态和最终结果观察，不创建无关的正式评估资产。

启用工具时，开发成员应当在需求、智能体运行支撑配置或变更记录中明确：

- 应调用、禁止调用以及必须先审批的条件；
- 参数字段、类型、必填项和允许取值范围，以及租户、目标对象、数量、时间范围等参数的业务含义和服务端校验；
- 工具返回值如何映射为成功、失败、部分成功和状态未知；
- 超时、重试、补偿和幂等（同一业务请求重复执行时至多产生一次预期影响）边界，尤其是写操作的幂等键或重复调用保护；
- 完成任务所要求的最终业务状态，以及如何独立查询验证。

工具返回成功仅表示调用层结果，不足以证明任务完成；智能体回复“已完成”同样不足以证明业务状态已经改变。正式生产路径的评估用例或其他路径的最小检查，应当同时定义必要的调用或禁止调用规则与最终状态检查。运行证据至少应当保留实际工具、关键参数、返回状态和状态核验结果。出现部分成功或状态未知时，必须停止可能扩大影响的后续动作并核对真实状态，不得对可能产生重复影响的操作继续重试，亦不得将部分成功表述为全部成功。

### 3.2 状态与长期记忆

启用会跨消息、会话（Session）或用户保存状态的能力时，应当明确：

- 状态作用域是消息、会话、用户、租户还是智能体，以及相应读写主体和权限；
- 信息来源、可信度和写入条件，哪些不可信内容不得进入长期记忆；
- 过期、纠正、删除、冲突处理和用户撤回后的处理方式；
- 跨用户、跨租户和不同评估用例之间的隔离及重置方式；
- 污染被发现后的停止使用、清理、恢复和回归检查方式。

正式生产路径冻结的是状态或长期记忆策略、字段结构、固定初始测试数据（Fixture，用于每次测试前恢复同一初始状态的数据）或快照的不可变定位信息，以及用例之间的重置规则。执行试次中产生的状态变化是本次运行的输入输出证据，不因正常写入状态而生成新候选基线；如果策略、字段结构、固定初始测试数据或重置规则发生变化，则必须生成新候选基线。其他路径只需在 `context` 中明确实际前置状态或序列，不得依赖上一次偶然残留。

### 3.3 检索增强生成（RAG）

启用检索增强生成（RAG）时，至少应当记录语料范围与来源、租户过滤、语料或快照版本、索引版本与关键检索配置。检查时应当分别验证：

- 完成任务所需内容是否已被召回，且召回内容是否处于正确权限范围内；
- 最终回答是否忠实于召回证据，引用是否能够定位到实际来源；
- 语料过期、缺失、冲突或检索失败时，智能体是否明确说明证据限制并停止无依据推断；
- 语料或索引更新失败时，是否能够继续使用已知可用版本或按预定方式回滚。

在线语料无法稳定冻结时，应当记录评估时间窗、查询条件和实际返回证据，不得仅记录逻辑知识库名称。未启用检索增强生成的项目无须建立语料版本或检索评估资产。

### 3.4 多智能体

启用智能体与子智能体（Subagent）协作时，至少应当明确：

- 委派任务的输入、输出、完成条件、超时和取消契约；
- 子智能体的有效权限不得因委派而自动扩大，并由权限、安全策略、运行时或隔离沙箱强制控制；
- 结果来源、实际执行者和交接记录如何进入可观察证据；
- 共享状态的所有者、可写范围、冲突处理和隔离方式；
- 子任务失败、超时或取消如何传播，主智能体何时停止、降级或转人工；
- 最大父子调用层数、单次并行发起的子任务数量、总步数、总时长和成本预算。

子智能体返回内容应当按外部输入进行处理。内部智能体来源不得作为跳过字段结构、权限、来源和事实校验的依据。简单任务不得仅为增加智能体数量而拆分为多智能体架构。

### 3.5 路由与降级

只有实际启用路由或降级时才处理该能力。正式生产路径必须冻结路由条件、可选模型或执行分支、触发降级的条件和失败后的停止规则，并将每个可达且影响结果的分支纳入针对性检查和正式评估；其他路径记录实际规则并检查与当前问题相关的分支。运行证据必须记录实际选择的模型或分支，不得以配置中的默认值代替。

降级路径不得扩大权限、绕过审批、静默降低安全控制，或将失败状态转换为成功状态。未定义明确的可恢复路径时，应当保留原始错误并终止执行，不得配置无明确恢复语义的通用降级路径。

### 3.6 执行预算、停止与转人工

应当根据任务风险和运行形态，为可能造成资源耗尽、费用超限或影响扩大的项目设置上限，包括适用的步骤数、工具调用数、持续时间、模型输入与输出用量（Token，用于计量模型处理的数据量）、成本、重试次数和并发数。没有相应风险或业务限制的项目可以省略。至少应当为以下情况定义停止或转人工条件：适用预算耗尽、连续失败、目标对象不明确、业务状态未知、用户取消、审批缺失或失效，以及继续执行可能扩大影响。

调试和评估应当保留可观察的输入输出、工具事件、路由结果、安全策略与审批判定、状态变化、停止原因以及结论所引用的数据来源，不要求记录模型内部推理过程。已有日志或调用链追踪记录（Trace，用于关联同一次任务中的运行事件）可以通过 `evidence_ref` 引用。

## 4. 探索验证与针对性检查

任务、实施路线、影响范围和所需控制已经明确，且不存在会改变实施路线的关键不确定性时，开发成员直接实现并运行针对性检查；正式生产路径还需确认交付验收标准。新增权限或架构变化应提高控制、测试和复核要求，但不自动成为探索验证步骤。新增功能应记录目标行为和影响范围；缺陷修复应记录复现步骤、当前结果和预期结果；迁移应记录前置条件、预期状态和迁移范围。

新方案、新能力或新增复杂度存在会影响实施路线的关键不确定性时，开发成员先选择足以区分问题的真实或高度接近真实的样本开展探索验证，数量由问题和差异覆盖决定，不设统一配额。探索验证的记录和退出条件按《01｜研发流程与按需探索验证》执行。无论是否开展探索验证，实现完成后都应完成与当前问题相称的针对性检查或最小验证。

正式生产路径的针对性检查应当至少覆盖；其他路径选择能够回答当前问题的相关检查：

- 本次变更直接影响的评估用例；
- 与变更共享工具、提示词、安全策略、状态或输出结构的相邻评估用例；
- 曾在相同位置失败的 `regression` 评估用例；
- 涉及权限或动作边界时，对应的允许、审批和阻断评估用例。

针对性检查仅用于判断本次变更的受影响范围是否存在明显问题。正式生产路径不得用它替代冻结正式评估范围的完整自测或正式回归，也不得单独形成交付或发布结论；其他路径可以在记录观察和限制后直接形成当前问题的决策。

## 5. 智能体运行支撑配置变更分类

正式生产路径按评估用例、安全清单和候选基线落实下表要求；其他路径按实际影响记录目标行为、控制边界和必要检查，不因分类表创建空的正式资产。

| 类别 | 典型变化 | 最小处理 |
|---|---|---|
| A：普通优化 | 提示词表达、上下文压缩、技能内部逻辑、同权限工具路由 | 记录变更并运行受影响评估用例的针对性检查 |
| B：能力边界 | 新增工具、业务系统、自动动作或关键输出逻辑 | 更新需求场景和评估用例，重新确认风险等级，并完成针对性检查 |
| C：控制边界 | 扩大数据或写权限、允许命令行环境、取消审批、修改隔离沙箱或网络边界 | 更新安全清单和安全评估用例，完成控制核验及针对性检查；不得仅依据能力结果判定 |

表中“最小处理”是形成候选基线前的开发阶段要求。无法明确分类时按影响更高的一类处理；一个变更同时属于多类时，叠加执行各类要求。正式生产路径的变更拟交付时，按第 6 节形成新的候选基线，并按《04｜评估、测试与回归规范》完整执行本次冻结的正式评估范围；针对性检查不得代替正式自测。其他路径在完成当前问题的必要检查和决策后即可结束，若要发布再转入正式生产路径。

## 6. 候选基线（正式生产路径）

### 6.1 形成时机

正式生产路径中，开发成员在以下条件满足后形成候选基线：

1. 本轮实现已形成不可变版本；仓库内实现使用路径和 Git 提交定位，外置实现使用受控统一资源标识符、对象版本和 SHA-256 内容摘要定位；
2. 智能体任务、交付验收标准和风险等级已确认；需求包含上线后业务结果目标时，其验证计划已按《02｜需求定义与场景输入规范》确认；
3. 受影响范围的针对性检查已完成；
4. 评估数据集、评分规则、安全控制和执行环境已准备完成；
5. 不存在已知会导致 `blocking` 评估用例失败、适用硬门禁不达标或正式自测无法执行的问题。

候选基线仅表示待正式自测的对象已经明确，不代表已经通过自测、交付评估或允许发布。

### 6.2 候选基线冻结项

候选基线至少绑定：

- 需求版本；
- 智能体及其运行支撑配置的不可变定位信息；仓库内填写路径和 Git 提交，外置填写受控统一资源标识符、对象版本和 SHA-256 内容摘要；
- 运行时或框架及版本；
- 模型和关键推理配置；
- 提示词、技能、工具/模型上下文协议版本；
- 安全策略和安全控制版本；
- 适用的状态/长期记忆策略、字段结构、固定初始测试数据或快照及重置规则；
- 适用的检索增强生成语料/索引版本与关键检索配置；
- 适用的多智能体委派契约、路由/降级规则；
- 适用的步骤、工具、时长、模型文本用量、成本、重试和并发预算；
- 评估数据集版本、正式评估范围和不可变定位信息；
- 每个验收项的评估实现，以及评分规则、阈值和执行试次聚合规则；
- 执行环境或可复现环境标识。

完整字段和交付记录方式见《06｜交付评估与发布规范》。不存在的组件不得仅为填写材料而创建。

### 6.3 候选基线与正式评估运行规则

- 任一冻结项变化，必须生成新的 `baseline_id`；
- 同一候选基线在冻结项不变时重跑，仅生成新的 `run_id`；
- 新运行结果不得覆盖旧运行，报告必须能定位采用的 `run_id`；
- 正式自测期间需要修改实现或冻结项时，当前运行不得形成正式评估运行结论；修改后必须建立新候选基线，并重新执行本次正式评估范围。

结果快照的不可变定位信息在运行完成后与报告中的 `baseline_id` 和 `run_id` 关联；仅补充该次运行的结果引用不视为冻结项变化。

## 7. 运行时或执行环境迁移

正式生产路径中，Claude Code、AgentScope、DSH 等框架迁移无须配置逐项对应，但必须保证：

1. 每个 `REQ-xxx` 的需求来源、智能体任务、非目标及关联 `AC-xxx` 保持完整；智能体整体风险等级及其依据，以及适用的上线后业务结果目标保持完整；
2. 场景、用户输入和评估用例资产保持完整且可追溯；
3. 安全边界不得退化；
4. 关键能力能够使用同一判定标准进行比较；
5. 新运行时或执行环境的候选基线能够复现；
6. 迁移引入的新能力和风险已纳入需求、安全清单和评估数据集。

正式生产路径的迁移本身属于候选实现变化，必须生成新候选基线，并完整执行本次冻结的正式评估范围。非生产迁移只需记录前置条件、差异、观察、回退条件和采用决策；配置结构的表面相似性不得替代目标行为证据。

## 8. 过度设计控制

以下能力不得作为研发启动的前置条件：

- 完整多智能体调度中心；
- 通用记忆平台；
- 自行建设的提示词管理平台；
- 完整的可观察性平台；
- 自动自进化系统；
- 大规模智能体注册与治理中心。

应当优先使用版本控制、逐行 JSON 格式（JSONL，每行一个独立 JSON 对象）、逗号分隔值文件（CSV）、脚本和必要运行证据形成完整记录。只有实际研发瓶颈反复出现，且新平台相较现有工具的收益、维护成本和替代方案已经明确时，才可考虑引入。

记录隐藏思维链不得作为调试、审计或验收的前提。无法依据可观察事件判定需求是否满足时，应当补充结构化结果、状态查询或运行事件，不得要求模型输出内部推理过程。

## 9. 提交正式自测前检查（正式生产路径）

- 变更目标、范围和分类已记录；
- 受影响评估用例的针对性检查已完成；
- 需求、场景、安全边界和评估数据集已按影响更新；
- 所有适用的工具、状态/长期记忆、检索增强生成、多智能体、路由/降级契约及执行预算已明确；
- 写操作的最终状态、部分成功、幂等和停止 / 转人工路径已有对应检查；
- 候选基线已创建且冻结项可追溯；
- 未使用局部检查结果替代正式自测结论。
