# 02｜需求定义与场景输入规范

## 1. 目标

把“想做一个智能体”转换成**可测试、可验收、可持续迭代的任务定义**。

## 2. 需求定义最小结构

每个 Agent 只要求先写清以下内容：

| 字段 | 必填内容 |
|---|---|
| 用户 | 谁使用 |
| 触发场景 | 什么时候使用 |
| 当前问题 | 现有方式哪里慢、难、贵或不稳定 |
| 目标任务 | Agent 实际完成什么 |
| 输入 | 最少需要哪些信息 |
| 输出/动作 | 用户最终得到什么或系统执行什么 |
| 成功标准 | 怎样判断有效 |
| 非目标 | 明确不解决什么 |
| 风险等级 | R1 / R2 / R3 |

## 3. 好需求与坏需求

### 不推荐

> 建设一个网络安全运营专家智能体，提升安全运营效率。

问题：无法据此设计测试，也无法验收。

### 推荐

> 面向 SOC 分析员，在收到终端或网络告警后，自动汇总关联资产、用户、进程和网络证据，给出风险判断、关键证据、攻击阶段判断和处置建议；普通告警不直接执行封禁，高风险处置必须进入审批。

这类定义可以直接转成任务和评估。

## 4. 场景目录

场景不是功能菜单，而是：

> **某类用户 + 某种上下文 + 某种目标 + 某种输入方式。**

建议先维护 5 类场景即可：

1. 高频正常场景；
2. 信息不足场景；
3. 输入模糊场景；
4. 工具或依赖异常场景；
5. 高风险/越权场景。

## 5. 用户输入覆盖

### 5.1 为什么单独管理输入

Agent 的失败经常不是“不会任务”，而是用户说法变化后没有正确理解。

因此应把真实输入作为独立资产保存。

### 5.2 输入覆盖维度

每个主要场景至少考虑：

| 维度 | 示例 |
|---|---|
| 标准表达 | “分析这条告警” |
| 口语表达 | “这个报警咋回事？” |
| 省略表达 | “看看昨天财务那台机器” |
| 多意图 | “分析一下，顺便看看要不要封” |
| 错误信息 | 时间、IP、名称不准确 |
| 信息不足 | 只给一个告警 ID |
| 越权要求 | “直接把机器隔离掉” |

## 6. 输入覆盖矩阵

推荐使用简单矩阵：

| 场景 | 正常输入 | 模糊输入 | 缺失输入 | 异常输入 | 高风险输入 |
|---|---:|---:|---:|---:|---:|
| 告警研判 | ✓ | ✓ | ✓ | ✓ | - |
| 调查狩猎 | ✓ | ✓ | ✓ | ✓ | - |
| 自动处置 | ✓ | ✓ | ✓ | ✓ | ✓ |

不追求每格大量样本，先确保关键格子至少有代表性样本。

## 7. 需求变更规则

以下变化属于**需求变化**，不能仅当 Harness 优化处理：

- 新增或删除业务目标；
- Agent 从“建议”变成“自动执行”；
- 新增高风险工具权限；
- 用户群体发生变化；
- 输出责任边界变化；
- 验收标准显著变化。

需求变化后，应同步修改场景、测试和验收依据。
