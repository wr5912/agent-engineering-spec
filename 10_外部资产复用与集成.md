# 10｜外部资产复用与集成

## 1. 目的与适用范围

本章用于把已有的 Skill、工具、模型上下文协议（MCP）服务、Agent、插件、Hook、工作流或数据资产接入当前任务。它回答四个问题：是否适合采用、差异在哪里、应改哪一层、怎样证明目标任务真的获得了能力。

本章不要求把外部项目整体搬入仓库，也不要求为适配建立注册中心、统一网关、审批平台或通用工作流。外部资产属于开发智能体配置还是目标 Harness 资产，按本次任务的实际用途判断，具体对象边界见[《00｜智能体研发总纲》](./00_总纲.md)。

外部资产接入可以是研究、缺陷修复、迁移或生产交付的一部分。生产交付仍按[《06｜交付评估与发布规范》](./06_交付评估与发布规范.md)执行；研究或局部修复不因采用外部资产而自动进入完整交付流程。

## 2. 先确认采用对象

一个外部项目可能同时包含业务 Agent、Skill、命令行工具、MCP 服务、插件和安装 Hook。先列出本次实际采用的部分及其直接依赖，再决定如何接入。

至少记录：

- 采用对象及其服务的任务或角色；
- 来源、版本、提交或可取得的定位信息，以及授权或许可证限制；
- 运行环境、装载位置、入口和必要依赖；
- 本次采用范围，以及明确不采用的部分。

开发者配置、目标业务 Harness、需求与评估资料、运行状态和运行记录不是同一对象。读取外部业务指令是为了理解或编辑资产，不代表开发身份被替换；安装副本或运行缓存也不自动成为源码事实源。

## 3. 做最小适配性分析

适配性分析不是固定评分表。只检查会影响当前目标的差异；没有差异时直接说明已核对范围即可。

| 检查角度 | 要回答的问题 | 通常的修改位置 |
|---|---|---|
| 任务与职责 | 外部能力解决的问题与当前任务一致吗？是否带来不需要的动作或权限？ | Skill、任务说明、编排入口 |
| 识别与组合 | 何时应选择它？是否与已有 Skill、工具或 Agent 重叠？ | 描述、显式绑定、路由或工具名称 |
| 运行时与依赖 | 原装载机制、解释器、路径、SDK 和依赖在目标环境成立吗？ | 配置、依赖声明、路径或装载方式 |
| 参数与数据 | ID、字段、单位、时间、分页、空值和错误语义一致吗？ | 参数构造、已有调用代码、局部转换 |
| 执行与状态 | 调用是同步还是异步？需要轮询、前置状态、重试或补偿吗？ | 现有执行逻辑、状态交接或小函数 |
| 输出与消费 | 任务需要什么结果？外部返回是否足够、可取得且能被下游正确消费？ | 输出整理、引用处理、交接结构 |

用简短记录表达：

```text
原有假设 → 当前任务要求 → 必要差异 → 修改位置 → 验证观察
```

不要把缺少的字段通过改名、包装或润色描述成已有能力；信息或权限不足时缩小任务范围、更换组件或不采用。

## 4. 选择接入方式

| 决定 | 适用情形 | 最小动作 |
|---|---|---|
| 直接复用 | 任务、接口和运行环境已匹配 | 按目标环境验证实际行为，不增加包装层 |
| 配置适配 | 差异仅在路径、端点、别名、启用范围或已支持参数 | 修改现有配置或绑定，不改写上游源码 |
| 局部内容或代码适配 | 任务步骤、数据语义、输出或调用过程不一致 | 修改最直接的 Skill、调用代码或小函数 |
| 部分复用或能力迁移 | 原运行时不兼容，但部分知识、脚本或接口有价值 | 写明保留、替换和不支持范围 |
| 保留远程服务 | 现有服务接口适用，搬迁没有收益 | 记录连接、版本、结果交接和远程限制 |
| 不采用 | 能力不足、来源不可用或适配成本不值得 | 记录主要原因和未验证范围 |

修改落点遵循以下顺序：任务规则放在 Skill 或编排中；确定性字段、单位和状态转换放在已有调用代码；连接、装载和路径差异放在配置。不要让模型在每次调用时临时猜测确定性转换，也不要为一个局部差异建设通用适配服务。

### 4.1 按资产类型补充核对

| 资产类型 | 额外核对 | 最小验证重点 |
|---|---|---|
| Skill | 发现、描述、激活、资源读取和任务边界是否符合目标运行时 | 目标 Agent 是否实际加载，并按新任务规则产生可观察结果 |
| 工具或 MCP 服务 | 工具定义、参数结构、对象 ID、分页、异步状态、错误和权限语义 | 实际调用参数、返回状态、必要的后续查询和最终任务结果 |
| 完整 Agent 或远程服务 | 模型、工具、身份、状态、依赖、输入输出和原运行时假设 | 选择原生迁入、部分迁移或远程调用，并验证交接结果；不宣称整体等价 |
| 插件、Hook 或工作流 | 目标运行时的事件、参数、顺序、异常传播和状态读写 | 现有扩展点能否承载本次路径；不能承载时部分采用或不采用，不靠改目录名伪装兼容 |
| 数据或评估资产 | 来源授权、字段、单位、前置状态、样本范围和评价口径是否匹配 | 目标执行器实际取得所需输入和状态；不把文档中的预置说明当作已注入数据 |

## 5. 分层验证目标行为

外部资产的状态按层记录，不能互相替代：

1. **已安装**：组件文件或包出现在目标环境中；
2. **已装载**：目标运行时已经发现并启用组件；
3. **接口可用**：明确输入可以到达组件并取得可解释返回；
4. **任务适配成立**：目标 Agent 在实际任务中正确选择、调用并解释结果；
5. **效果有效**：在当前研究或交付范围内，目标结果达到既定判断标准。

必要时同时保留以下执行语义：未执行、已受理、执行中、已完成、部分完成、失败、未知。上游任务完成不等于业务目标完成；返回空列表不等于查询结果缺失；状态未知时不得自动当作正常或成功。

最小验证应包含：实际输入、前置状态、目标环境、关键调用或装载观察、用户可见结果，以及没有执行的部分。需要证明任务集成时，必须观察目标任务中的行为和结果；仅检查文件存在、HTTP 状态、工具列表或上游单元测试不够。

真实写操作、敏感数据、审批和外部网络按[《05｜安全与控制边界规范》](./05_安全控制边界规范.md)执行。只读研究可以使用模拟或合成输入，但必须明确其不能证明真实服务、模型或运行时行为。

## 6. 记录、更新与撤下

记录可放入现有研究、变更或交付记录，不要求另建固定台账。建议使用以下标题：

```markdown
## 本次外部能力接入

目标：解决什么问题；本次明确不做什么。
采用对象：实际 Skill、工具、MCP 服务、Agent 或其中一部分。
来源与版本：可取得的实际引用；授权或许可证限制。
目标环境：运行时、依赖和调用位置。
实际差异：任务、接口、数据、状态、输出或装载哪里不一致。
适配决定：直接复用 / 配置适配 / 局部适配 / 部分复用 / 保留远程服务 / 不采用。
修改位置：具体文件、配置或调用函数。
验证：实际输入、前置状态、关键观察和未执行部分。
决定与限制：采用或放弃的理由，以及更新或撤下影响。
```

更新只检查实际采用范围和相关回归；不因上游其他目录变化而复制整套资产。撤下时停止绑定、清理必要配置并核对共享依赖，不删除仍被其他任务使用的内容，也不改写既有运行事实。远程版本、服务可用性或权限无法控制时，明确写出复现限制。
